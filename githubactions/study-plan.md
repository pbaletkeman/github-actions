# GitHub Actions GH-200 Certification - 60-Hour Study Plan

- [GitHub Actions GH-200 Certification - 60-Hour Study Plan](#github-actions-gh-200-certification---60-hour-study-plan)
  - [Study Plan Overview](#study-plan-overview)
  - [🏗️ Week 1: Foundations \& Core Concepts](#️-week-1-foundations--core-concepts)
    - [🔧 Section 1: GitHub Actions Fundamentals \& Tooling (2 hrs)](#-section-1-github-actions-fundamentals--tooling-2-hrs)
    - [🔍 Section 2: Understanding GitHub Contexts - Part 1 (2 hrs)](#-section-2-understanding-github-contexts---part-1-2-hrs)
    - [🔍 Section 3: Understanding GitHub Contexts - Part 2 (2 hrs)](#-section-3-understanding-github-contexts---part-2-2-hrs)
    - [📝 Section 4: YAML Syntax \& Workflow File Structure (2 hrs)](#-section-4-yaml-syntax--workflow-file-structure-2-hrs)
  - [⚡ Week 2: Workflow Triggers \& Events](#-week-2-workflow-triggers--events)
    - [⚡ Section 5: Workflow Trigger Events - Part 1 (2 hrs)](#-section-5-workflow-trigger-events---part-1-2-hrs)
    - [⚡ Section 6: Workflow Trigger Events - Part 2 (2 hrs)](#-section-6-workflow-trigger-events---part-2-2-hrs)
    - [⚙️ Section 7: Environment \& Runtime Configuration (2 hrs)](#️-section-7-environment--runtime-configuration-2-hrs)
    - [🔐 Section 8: Environment Protection Rules \& Secrets (2 hrs)](#-section-8-environment-protection-rules--secrets-2-hrs)
  - [🔄 Week 3: Workflow Features \& Data Flow](#-week-3-workflow-features--data-flow)
    - [🔀 Section 9: Jobs, Steps \& Conditional Logic (2 hrs)](#-section-9-jobs-steps--conditional-logic-2-hrs)
    - [🔢 Section 10: Job Strategy \& Matrix (2 hrs)](#-section-10-job-strategy--matrix-2-hrs)
    - [🐳 Section 11: Service Containers \& Dependencies (2 hrs)](#-section-11-service-containers--dependencies-2-hrs)
    - [📦 Section 12: Workflow Artifacts - Storage \& Retrieval (2 hrs)](#-section-12-workflow-artifacts---storage--retrieval-2-hrs)
    - [💨 Section 13: Workflow Caching - Performance \& Security (2 hrs)](#-section-13-workflow-caching---performance--security-2-hrs)
  - [🚀 Week 4: Advanced Workflow Features](#-week-4-advanced-workflow-features)
    - [📡 Section 14: Inter-Job Communication \& Outputs (2 hrs)](#-section-14-inter-job-communication--outputs-2-hrs)
    - [🐛 Section 15: Workflow Debugging \& Troubleshooting (2 hrs)](#-section-15-workflow-debugging--troubleshooting-2-hrs)
    - [🌐 Section 16: GitHub Workflow REST API (2 hrs)](#-section-16-github-workflow-rest-api-2-hrs)
  - [♻️ Week 5: Reusable Workflows \& Actions](#️-week-5-reusable-workflows--actions)
    - [♻️ Section 17: Reusable Workflows \& Workflow Sharing (2 hrs)](#️-section-17-reusable-workflows--workflow-sharing-2-hrs)
    - [🧩 Section 18: Custom Actions - Types \& Structure (2 hrs)](#-section-18-custom-actions---types--structure-2-hrs)
    - [📢 Section 19: Custom Actions - Distribution \& Publishing (2 hrs)](#-section-19-custom-actions---distribution--publishing-2-hrs)
  - [🖥️ Week 6: Runners \& Enterprise](#️-week-6-runners--enterprise)
    - [🏃 Section 20: GitHub-Hosted Runners (2 hrs)](#-section-20-github-hosted-runners-2-hrs)
    - [🖥️ Section 21: Self-Hosted Runners \& Configuration (2 hrs)](#️-section-21-self-hosted-runners--configuration-2-hrs)
    - [🏢 Section 22: Enterprise-Level Management (2 hrs)](#-section-22-enterprise-level-management-2-hrs)
  - [🔒 Week 7: Security \& Optimization](#-week-7-security--optimization)
    - [🛡️ Section 23: GitHub Actions Security Best Practices - Part 1 (2 hrs)](#️-section-23-github-actions-security-best-practices---part-1-2-hrs)
    - [🔒 Section 24: GitHub Actions Security Best Practices - Part 2 (2 hrs)](#-section-24-github-actions-security-best-practices---part-2-2-hrs)
    - [⚡ Section 25: Workflow Performance Optimization (2 hrs)](#-section-25-workflow-performance-optimization-2-hrs)
    - [🔦 Section 26: Troubleshooting \& Common Failures - Part 2 (2 hrs)](#-section-26-troubleshooting--common-failures---part-2-2-hrs)
  - [📊 Week 8: Job Summaries \& Advanced Features](#-week-8-job-summaries--advanced-features)
    - [📊 Section 27: Job Summaries \& Workflow Status (2 hrs)](#-section-27-job-summaries--workflow-status-2-hrs)
    - [📝 Section 28: Comprehensive Practice Assessment (2 hrs)](#-section-28-comprehensive-practice-assessment-2-hrs)
  - [🎯 Week 9: Final Review \& Assessment](#-week-9-final-review--assessment)
    - [📋 Section 29: Mock Exam - Full Practice Test (2 hrs)](#-section-29-mock-exam---full-practice-test-2-hrs)
    - [🎓 Section 30: Final Consolidation \& Exam Prep (2 hrs)](#-section-30-final-consolidation--exam-prep-2-hrs)
  - [🎓 Curriculum Complete](#-curriculum-complete)
  - [Study Strategy \& Tips](#study-strategy--tips)
    - [Daily Checklist](#daily-checklist)
    - [Knowledge Retention](#knowledge-retention)
    - [Weak Area Focus](#weak-area-focus)
    - [Time Management](#time-management)
    - [Success Criteria](#success-criteria)
  - [Resources \& Materials](#resources--materials)
  - [Success Tracking Template](#success-tracking-template)

**Total Duration:** 60 hours
**Sections:** 30 sections × 2 hours each
**Exam Pass Target:** >71%
**Format:** ~2 hours per section (mix of reading, practical work, and review)

---

## Study Plan Overview

This plan is designed to progressively build mastery from fundamentals to advanced enterprise concepts. Each section includes reading, practical exercises where applicable, and knowledge consolidation.

**Recommended Pace:** 2–3 sections per day over 10–15 days for intensive study, or 1–2 sections per day over 3–4 weeks for balanced learning.

---

## 🏗️ Week 1: Foundations & Core Concepts

### 🔧 Section 1: GitHub Actions Fundamentals & Tooling (2 hrs)

**Topics:** [01-GitHub-Actions-VS-Code-Extension.md](01-GitHub-Actions-VS-Code-Extension.md)
**Overview:**

- GitHub Actions platform capabilities and use cases
- GitHub Actions VS Code Extension installation and setup
- YAML schema validation and IntelliSense features
- Local workflow validation with act or extension tools
- Best practices for local development and testing

**Deliverables:**

- Install and configure the VS Code extension
- Validate a sample workflow locally
- Understand extension features and troubleshooting


<details>
<summary>🔧 Scenario 1-A: Install & Configure the VS Code GitHub Actions Extension</summary>


**Objective:** Install the GitHub Actions VS Code extension, verify YAML IntelliSense activates, and confirm schema validation catches errors.

**Prerequisites:** VS Code v1.60+; Git installed; a cloned or initialized Git repository open as a workspace folder.

**Context:** Without the extension, YAML typos in workflow files are invisible until the run fails on GitHub. The extension provides real-time schema validation and IntelliSense for all GitHub Actions keys, reducing push-and-pray debugging cycles.

**Detailed Steps:**

1. Open VS Code → Extensions panel (`Ctrl+Shift+X`) → search `GitHub Actions` → verify publisher is **GitHub** → **Install**.
2. Create the workflows directory: `mkdir -p .github/workflows` (or `New-Item -ItemType Directory -Force .github\workflows` on Windows).
3. Create `.github/workflows/hello.yml` with this content:

```yaml
name: Hello World

on:
  push:
    branches: [main]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Print greeting
        run: echo "Hello, ${{ github.actor }}!"
```

4. Hover over `runs-on` → verify a tooltip describing the key appears.
5. Type `runs-on: invalid-runner-name` → verify a red squiggly warning appears.
6. Restore the valid value and confirm the warning disappears.
7. Inside the `steps:` block start typing `- uses: actions/` → verify IntelliSense suggests action names.

**Success Criteria:**

- [ ] Extension appears in the Extensions panel as "GitHub Actions" by GitHub.
- [ ] Hovering YAML keys shows documentation tooltips.
- [ ] An invalid `runs-on` value produces a visible schema validation warning.
- [ ] IntelliSense completes action names when typing `uses:`.

**Expected Output:**

Hovering over `runs-on` shows: *"The type of machine to run the job on. The machine can be either a GitHub-hosted runner or a self-hosted runner."*

**Troubleshooting:**

- **IntelliSense not appearing** → Ensure the file is inside `.github/workflows/` with a `.yml` or `.yaml` extension. Opening a standalone file (no workspace folder) will not activate the schema.
- **No schema warnings** → Reload window (`Ctrl+Shift+P` → "Developer: Reload Window") after installing the extension.

**Time Estimate:** 10–15 minutes

**Cross-Section Connections:** → Section 4 (YAML Syntax) for YAML anchors and advanced structure; → Section 15 (Debugging) for interpreting workflow error messages.

</details>

---

<details>
<summary>✅ Scenario 1-B: Validate a Workflow Locally with `act`</summary>


**Objective:** Install the `act` CLI and run a GitHub Actions workflow on your local machine without pushing to GitHub.

**Prerequisites:** Docker Desktop installed and running; Scenario 1-A complete (workflow file exists).

**Context:** `act` uses Docker to simulate the GitHub Actions runner environment locally. This shortens the feedback loop from "push → wait → fail → push again" to an immediate local test cycle. It is especially useful for catching logic errors and environment misconfigurations.

**Detailed Steps:**

1. Install `act`:
   - **Windows (winget):** `winget install nektos.act`
   - **Windows (choco):** `choco install act-cli`
   - **macOS:** `brew install act`
   - **Linux:** `curl https://raw.githubusercontent.com/nektos/act/master/install.sh | sudo bash`
2. Verify installation: `act --version`
3. From the repository root run `act -l` to list detected workflows.
4. Run the push event locally: `act push`
5. Observe the Docker image pull (first run only) and step execution in the terminal.
6. Add `.actrc` in the repo root to avoid the image prompt on future runs:

```text
-P ubuntu-latest=catthehacker/ubuntu:act-latest
```

7. Run `act push --dry-run` to preview execution order without running steps.

**Success Criteria:**

- [ ] `act --version` prints a version string.
- [ ] `act -l` lists the `Hello World` workflow.
- [ ] `act push` executes the `greet` job and prints the greeting.
- [ ] `act push --dry-run` prints the plan without executing Docker containers.

**Expected Output:**

```text
[Hello World/greet] 🐳 docker run image=catthehacker/ubuntu:act-latest
[Hello World/greet]   ✅  Success - Checkout code
[Hello World/greet]   ✅  Success - Print greeting
```

**Troubleshooting:**

- **"Cannot connect to Docker daemon"** → Start Docker Desktop; verify `docker ps` works.
- **act pulls wrong image** → Add `.actrc` as shown above to specify the correct image tag.
- **Step output shows wrong actor** → `act` uses `nektos/act` as the actor by default; this is expected in local runs.

**Time Estimate:** 15–20 minutes

**Variations/Extensions:**

- Pass a secret to `act`: `act push -s MY_SECRET=value`
- Test multiple events: `act pull_request` to simulate PR triggers.

**Cross-Section Connections:** → Section 15 (Debugging) for enabling debug logging inside `act`; → Section 5 (Triggers) for simulating different event types locally.

</details>

---

<details>
<summary>🔍 Scenario 1-C: Advanced Workflow Validation — Linting and Schema Enforcement</summary>


**Objective:** Set up automated workflow validation beyond the VS Code extension using actionlint, schema linters, and pre-commit hooks to catch errors before pushing to GitHub.

**Prerequisites:** Scenario 1-A and 1-B complete; basic shell/bash knowledge.

**Context:** While the VS Code extension catches obvious errors, enterprise teams need consistent validation across all developers. Tools like `actionlint` provide deeper analysis: unused variables, missing outputs, unreachable steps, and deprecated syntax. Pre-commit hooks enforce validation automatically.

**Detailed Steps:**

1. Install `actionlint` (requires Go ≥1.16):
   - **macOS/Linux:** `brew install actionlint`
   - **Windows:** Download from [rhysd/actionlint releases](https://github.com/rhysd/actionlint/releases) or use `scoop install actionlint`
   - Verify: `actionlint --version`

2. Run `actionlint` on your workflow:

   ```bash
   actionlint .github/workflows/hello.yml
   ```

3. Create an intentionally flawed workflow at `.github/workflows/flawed.yml`:

   ```yaml
   name: Flawed Workflow
   on: push
   jobs:
     test:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v4
         - name: Use undefined variable
           run: echo "Value is ${{ env.UNDEFINED_VAR }}"
         - name: Unreachable step
           if: false
           run: echo "Never runs"
   ```

4. Run `actionlint .github/workflows/flawed.yml` and observe the warnings.

5. Install `pre-commit` framework:
   - `pip install pre-commit` (or `brew install pre-commit` on macOS)

6. Create `.pre-commit-config.yaml` in repo root:

   ```yaml
   repos:
     - repo: https://github.com/rhysd/actionlint
       rev: v1.6.27
       hooks:
         - id: actionlint
   ```

7. Install the git hook: `pre-commit install`

8. Try committing `.github/workflows/flawed.yml` — the pre-commit hook will block it with validation errors.

9. Fix the flawed workflow and commit again successfully.

**Success Criteria:**

- [ ] `actionlint --version` runs without error.
- [ ] Running actionlint on flawed.yml reports 2+ warnings.
- [ ] Pre-commit hook is installed and active (`git hook list` shows pre-commit).
- [ ] Committing flawed workflow is blocked; committing corrected workflow succeeds.

**Expected Output:**

```bash
.github/workflows/flawed.yml:7:15: "env.UNDEFINED_VAR" is not defined. available env variables are ["GITHUB_*", ...]
.github/workflows/flawed.yml:8:5: unreachable step detected. step cannot run because condition is always false
```

**Troubleshooting:**

- **actionlint not found** → Add to PATH or use full path `/usr/local/bin/actionlint`
- **Pre-commit hook not blocking commits** → Verify hook is installed: `cat .git/hooks/pre-commit`
- **Hook runs too slowly** → Use `pre-commit run --all-files --hook-stage commit` to test specific hooks

**Time Estimate:** 20–25 minutes

**Variations/Extensions:**

- Add `yamllint` for YAML syntax validation
- Integrate with GitHub Actions to run actionlint on every workflow change (meta-workflow)
- Configure actionlint in strict mode: `actionlint -config-file .actionlintrc.yaml`

**Cross-Section Connections:** → Section 4 (YAML Syntax) for schema rules; → Section 15 (Debugging) for actionlint error interpretation.

</details>

---

<details>
<summary>🐛 Scenario 1-D: Troubleshooting Local Workflow Validation — Diagnosing and Fixing Common Issues</summary>


**Objective:** Diagnose and fix common problems when using the VS Code extension, `act`, and actionlint. Learn systematic troubleshooting methodology.

**Prerequisites:** Scenario 1-C complete; exposure to typical validation failures.

**Context:** Real-world development involves misconfigurations: Docker not running, act using wrong image, extension not loading schemas, pre-commit hooks conflicting. Learning to diagnose these systematically saves hours of frustration.

**Detailed Steps:**

1. **Simulate Extension Loading Failure:**
   - Close VS Code completely
   - Delete VS Code cache: `rm -rf ~/.config/Code/CachedData/*` (Linux/macOS) or `rmdir "%APPDATA%\Code\CachedData" /s /q` (Windows)
   - Reopen VS Code and wait 30 seconds for extension initialization
   - If IntelliSense still doesn't appear, check Output panel: **View → Output → GitHub Actions**
   - Expected: Log shows "Schema loaded successfully"

2. **Simulate and Fix Docker Connection Error with act:**
   - Stop Docker Desktop or stop Docker daemon: `systemctl stop docker` (Linux)
   - Try running `act push` — observe error: "Cannot connect to Docker daemon"
   - Check Docker status: `docker ps` (fails as expected)
   - Restart Docker and verify: `docker ps` (shows running containers)
   - Retry `act push` — succeeds

3. **Fix act Image Mismatch:**
   - Run `act -l` and note the detected workflows
   - Run `act push 2>&1 | head -20` to see image being pulled
   - Delete `.actrc` if it exists: `rm .actrc`
   - Run `act push` again — note it prompts for image selection or uses default
   - Create `.actrc` with explicit image:

     ```bash
     -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:act-latest
     ```

   - Run `act push` again — verify correct image is used

4. **Diagnose actionlint Configuration Issues:**
   - Create malformed `.actionlintrc.yaml`:

     ```yaml
     invalid: yaml: [structure
     ```

   - Run `actionlint .github/workflows/hello.yml` — observe error
   - Fix YAML syntax in `.actionlintrc.yaml` — retry

5. **Resolve Pre-commit Hook Bypass Issues:**
   - Verify hook is active: `ls -la .git/hooks/pre-commit`
   - Run pre-commit manually: `pre-commit run --all-files`
   - If blocked by actionlint errors, note them and create an issue or fix the workflow

**Success Criteria:**

- [ ] Extension Output panel shows schema initialization logs.
- [ ] Docker connectivity restored and `act` runs successfully.
- [ ] `.actrc` points to correct image; `act -l` confirms.
- [ ] actionlint produces clear error messages for invalid YAML configs.
- [ ] Pre-commit hook can be manually invoked and blocks invalid workflows.

**Expected Output:**

```bash
$ docker ps
Error response from daemon: Cannot connect to Docker daemon

$ systemctl start docker
$ docker ps
CONTAINER ID   IMAGE   COMMAND   STATUS   PORTS
[containers list]

$ act push
[Hello World/greet] 🐳 docker run image=ghcr.io/catthehacker/ubuntu:act-latest
[Hello World/greet]   ✅  Success - Checkout code
[Hello World/greet]   ✅  Success - Print greeting
```

**Troubleshooting:**

- **Extension logs not appearing** → Ensure Output panel is visible; check dropdown is set to "GitHub Actions"
- **Docker still not found** → Verify Docker installation: `which docker` (Unix) or `where docker` (Windows)
- **Pre-commit hook still not blocking** → Manually run `pre-commit run --all-files` to test

**Time Estimate:** 25–30 minutes

**Variations/Extensions:**

- Enable `act` debug logging: `act push --verbose`
- Test on a separate branch to avoid blocking main development
- Create a "validation dashboard" script that checks all three tools in sequence

**Cross-Section Connections:** → Section 15 (Debugging) for advanced log inspection; → Section 5 (Triggers) for testing different event types locally.

</details>

---

<details>
<summary>🏢 Scenario 1-E: Enterprise-Scale Setup — Shared IDE Configuration and Team Workflow Templates</summary>


**Objective:** Establish organizational standards for GitHub Actions local development: shared VS Code settings, standardized `.actrc` configuration, and reusable workflow templates enforced across the team.

**Prerequisites:** Scenario 1-D complete; team/organization collaboration model.

**Context:** Solo developers can validate workflows individually, but teams need consistency. Shared IDE settings, pre-configured act environments, and enforced workflow standards reduce onboarding time and prevent misconfiguration-related bugs. This scenario models enterprise practices: committing configuration to version control, using DevContainers, and defining team standards.

**Detailed Steps:**

1. **Create Shared VS Code Workspace Settings:**
   - In repo root, create `.vscode/settings.json`:

     ```json
     {
       "[yaml]": {
         "editor.formatOnSave": true,
         "editor.defaultFormatter": "esbenp.prettier-vscode"
       },
       "yaml.schemas": {
         "https://json.schemastore.org/github-workflow.json": ".github/workflows/*.yml"
       },
       "editor.codeActionsOnSave": {
         "source.fixAll": "explicit"
       }
     }
     ```

2. **Create Shared Extensions Recommendation:**
   - Create `.vscode/extensions.json`:

     ```json
     {
       "recommendations": [
         "github.vscode-github-actions",
         "esbenp.prettier-vscode",
         "redhat.vscode-yaml",
         "ms-vscode.makefile-tools"
       ]
     }
     ```

3. **Commit Team `.actrc` Configuration:**
   - In repo root, create `.github/.actrc.template`:

     ```bash
     -P ubuntu-latest=ghcr.io/catthehacker/ubuntu:act-latest
     -P windows-latest=ghcr.io/catthehacker/windows:latest
     -P macos-latest=ghcr.io/catthehacker/macos:latest
     --artifact-server-path /tmp/artifacts
     ```

   - Add documentation: **SETUP.md** → "Copy `.github/.actrc.template` to repo root as `.actrc`"

4. **Create Enforced Workflow Template:**
   - Add `.github/workflows/TEMPLATE.yml` with required structure:

     ```yaml
     # Copy this template to create new workflows
     # Required sections: name, on, jobs with proper structure
     name: "[Workflow Name]"
     on: [push, pull_request]
     jobs:
       build:
         runs-on: ubuntu-latest
         steps:
           - uses: actions/checkout@v4
           - name: "[Step description]"
             run: echo "Add your step here"
     ```

5. **Create Pre-commit Policy Enforcement:**
   - Extend `.pre-commit-config.yaml` to include filename validation:

     ```yaml
     repos:
       - repo: https://github.com/rhysd/actionlint
         rev: v1.6.27
         hooks:
           - id: actionlint
       - repo: local
         hooks:
           - id: check-workflow-template
             name: Check workflow template compliance
             entry: bash -c 'for f in .github/workflows/*.yml; do grep -q "jobs:" "$f" || exit 1; done'
             language: system
             files: ^\.github/workflows/
     ```

6. **Create Team Onboarding Guide (DEVELOPMENT.md):**

   ```markdown
   # Local Development Setup

   ## Prerequisites
   - VS Code with GitHub Actions extension
   - Docker Desktop
   - actionlint and pre-commit

   ## Setup Steps
   1. Clone repository
   2. Install pre-commit: `pip install pre-commit && pre-commit install`
   3. Copy `.github/.actrc.template` to `.actrc`
   4. Run `act -l` to verify workflows are detected
   5. VS Code will auto-load shared settings from `.vscode/settings.json`

   ## Validating Workflows
   - Pre-commit hook runs on every commit
   - Manual validation: `actionlint .github/workflows/*.yml`
   - Local simulation: `act push`
   ```

7. **Test Team Setup on Fresh Clone:**
   - In a temporary directory: `git clone <repo> test-clone && cd test-clone`
   - Follow the DEVELOPMENT.md steps
   - Verify all tools work correctly and shared settings load

**Success Criteria:**

- [ ] `.vscode/settings.json` exists and is committed to repo
- [ ] `.vscode/extensions.json` recommends GitHub Actions and yaml extensions
- [ ] `.github/.actrc.template` provides team-standard act configuration
- [ ] `TEMPLATE.yml` enforces consistent workflow structure
- [ ] Pre-commit policy includes actionlint and custom template checks
- [ ] Fresh clone successfully loads shared settings and passes all validations
- [ ] Team members can follow DEVELOPMENT.md and validate workflows locally

**Expected Output:**

When a new team member clones the repo:

```bash
$ git clone <repo>
$ cd <repo>
$ pre-commit install
pre-commit installed at .git/hooks/pre-commit

$ cp .github/.actrc.template .actrc
$ act -l
[workflows detected]

$ act push
[workflow runs successfully with shared configuration]
```

**Troubleshooting:**

- **Settings not loading** → Verify `.vscode/` directory is committed: `git ls-files .vscode/`
- **Extensions not recommended** → Restart VS Code to trigger recommendation prompt
- **Pre-commit policy too strict** → Adjust regex patterns to balance safety and flexibility
- **Team bypass validation** → Use branch protection rules (Section 23) to enforce pre-commit passes

**Time Estimate:** 30–40 minutes

**Variations/Extensions:**

- Add GitHub Actions workflow to validate all workflows on push: "meta-validation"
- Use DevContainers (`.devcontainer/devcontainer.json`) for complete standardized environment
- Integrate with GitHub Advanced Security for policy enforcement

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for template distribution; → Section 22 (Enterprise Management) for org-wide policies; → Section 23 (Security) for branch protection and enforcement.

</details>

---

### 🔍 Section 2: Understanding GitHub Contexts - Part 1 (2 hrs)

**Topics:** [02-Contextual-Information.md](02-Contextual-Information.md) (first half)
**Overview:**

- What are contexts and why they matter in workflows
- The `github` context: repository info, ref, event data, actor
- The `env` context: environment variables at workflow/job/step level
- The `secrets` context: encrypted secret handling
- Context interpolation and expression syntax

**Practical Work:**

- Create a test workflow using `github.event_name` and `github.ref`
- Access repository owner and branch information
- Practice secret masking and context limitations


<details>
<summary>🔍 Scenario 2-A: Explore the `github` Context in a Workflow</summary>


**Objective:** Write a workflow that prints key `github` context values to understand what information is available at runtime, and observe how these values differ across trigger types.

**Prerequisites:** Section 1 complete; a GitHub repository where you can push commits and open pull requests.

**Context:** The `github` context is the richest context in GitHub Actions — it contains information about the triggering event, the repository, the actor, and more. Knowing which properties are available (and when) is essential for writing conditional logic and building robust workflows.

**Detailed Steps:**

1. Create `.github/workflows/explore-contexts.yml`:

```yaml
name: Explore Contexts

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  show-github-context:
    runs-on: ubuntu-latest
    steps:
      - name: Print core github context values
        run: |
          echo "=== Repository Info ==="
          echo "Repo           : ${{ github.repository }}"
          echo "Owner          : ${{ github.repository_owner }}"
          echo "Default branch : ${{ github.event.repository.default_branch }}"

          echo "=== Trigger Info ==="
          echo "Event name     : ${{ github.event_name }}"
          echo "Ref            : ${{ github.ref }}"
          echo "Ref name       : ${{ github.ref_name }}"
          echo "Ref type       : ${{ github.ref_type }}"
          echo "SHA            : ${{ github.sha }}"

          echo "=== Actor Info ==="
          echo "Actor          : ${{ github.actor }}"
          echo "Triggering actor: ${{ github.triggering_actor }}"

          echo "=== PR-specific (only on pull_request events) ==="
          echo "Head ref       : ${{ github.head_ref }}"
          echo "Base ref       : ${{ github.base_ref }}"

      - name: Print full github context as JSON (for inspection)
        env:
          # Assign context to env var to safely print JSON
          GITHUB_CTX: ${{ toJSON(github) }}
        run: echo "$GITHUB_CTX"
```

2. Push to `main` and examine the "Trigger Info" output — note `ref` = `refs/heads/main`.
3. Open a pull request and inspect the same job — note `head_ref` and `base_ref` are now populated.
4. Trigger via `workflow_dispatch` — note `event_name` = `workflow_dispatch` and `head_ref` is empty.

**Success Criteria:**

- [ ] `github.repository` prints `owner/repo-name` format.
- [ ] `github.event_name` correctly reflects the trigger type for each run.
- [ ] `github.head_ref` is populated on `pull_request` events and empty on `push` events.
- [ ] The full JSON context dump completes without redaction errors.

**Troubleshooting:**

- **`${{ github.head_ref }}` is empty on push** → This is correct behavior; `head_ref` only exists on PR events.
- **JSON dump shows redacted values for secrets** → Expected; the `github` context itself does not contain secrets, but `toJSON(secrets)` would be redacted.

**Time Estimate:** 15–20 minutes

</details>

---

<details>
<summary>🔐 Scenario 2-B: Secrets Context — Masking and Limitations</summary>


**Objective:** Add a repository secret, use it in a workflow, and observe how GitHub automatically masks the value in logs to prevent accidental exposure.

**Prerequisites:** Repository admin access to add secrets; Scenario 2-A complete.

**Context:** Secrets are encrypted at rest and automatically masked in runner logs. Understanding the masking behavior — and its limits — is required exam knowledge. The `secrets` context is only available in certain workflow scopes and is not accessible in pull requests from forks by default.

**Detailed Steps:**

1. Navigate to **Repository → Settings → Secrets and variables → Actions → New repository secret**.
   - Name: `DEMO_API_KEY`
   - Value: `super-secret-value-abc123`

2. Create `.github/workflows/secrets-demo.yml`:

```yaml
name: Secrets Demo

on: workflow_dispatch

jobs:
  secrets-test:
    runs-on: ubuntu-latest
    steps:
      - name: Use secret safely (masked in logs)
        env:
          # Best practice: map secret to env var rather than inline in run:
          API_KEY: ${{ secrets.DEMO_API_KEY }}
        run: |
          echo "API key length: ${#API_KEY}"
          echo "Using key in command (value is masked): $API_KEY"
          # The value 'super-secret-value-abc123' will appear as *** in logs

      - name: Demonstrate accidental exposure is prevented
        env:
          API_KEY: ${{ secrets.DEMO_API_KEY }}
        run: |
          # Even if you try to echo the value, GitHub masks it
          VAL="$API_KEY"
          echo "Attempting to print: $VAL"

      - name: Secrets unavailable without mapping
        run: |
          # ${{ secrets.DEMO_API_KEY }} inline in run: works but is not best practice
          echo "Inline ref: ${{ secrets.DEMO_API_KEY }}"
```

3. Trigger via `workflow_dispatch` and view the logs.
4. Observe that `super-secret-value-abc123` never appears — it is replaced with `***`.
5. Verify the length step prints the correct length (22 characters) — length is not masked.

**Success Criteria:**

- [ ] Secret value is masked as `***` in all log lines.
- [ ] `${#API_KEY}` (length) prints correctly — masking does not prevent computation.
- [ ] No plain-text secret appears anywhere in the workflow log or annotation.

**Troubleshooting:**

- **`secrets.DEMO_API_KEY` is empty** → Verify the secret name matches exactly (case-sensitive). Re-check under Settings → Secrets.
- **Secret not masked in log** → If the secret value is very short (< 3 characters), GitHub does not mask it to avoid masking common strings. Use realistic secret values in testing.
- **Secret unavailable in fork PR** → By design. Fork PRs do not have access to repository secrets by default. Use environment protection rules to enable this if necessary.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 7 (Environment Variables) for `GITHUB_ENV` and the difference between env vars and secrets; → Section 8 (Environment Protection) for per-environment secrets; → Section 23 (Security) for script injection via untrusted input.

</details>

---

<details>
<summary>🧩 Scenario 2-C: Advanced Context Interpolation — Nested Objects and Conditional Logic</summary>


**Objective:** Work with complex `github` context nested properties (event payload data), and implement multi-branch conditional logic using context values.

**Prerequisites:** Scenario 2-B complete; comfort with YAML syntax.

**Context:** The `github` context often requires nested property access: `github.event.pull_request.title`, `github.event.repository.full_name`, etc. Additionally, real workflows need to make decisions based on these values using expressions with logical operators. Debugging broken expressions requires understanding how GitHub evaluates them.

**Detailed Steps:**

1. Create `.github/workflows/advanced-contexts.yml`:

```yaml
name: Advanced Context Usage

on:
  pull_request:
    types: [opened, synchronize, reopened]
  issues:
    types: [opened, edited]
  workflow_dispatch:
    inputs:
      debug_mode:
        description: 'Enable debug output'
        required: false
        default: 'false'

jobs:
  analyze-event:
    runs-on: ubuntu-latest
    steps:
      - name: Analyze PR context
        if: github.event_name == 'pull_request'
        run: |
          echo "=== PR Details ==="
          echo "PR Title       : ${{ github.event.pull_request.title }}"
          echo "PR Author      : ${{ github.event.pull_request.user.login }}"
          echo "PR Base Branch : ${{ github.event.pull_request.base.ref }}"
          echo "PR Head Branch : ${{ github.event.pull_request.head.ref }}"
          echo "PR Draft?      : ${{ github.event.pull_request.draft }}"
          echo "PR Labels      : ${{ join(github.event.pull_request.labels.*.name, ', ') }}"

      - name: Analyze Issue context
        if: github.event_name == 'issues'
        run: |
          echo "=== Issue Details ==="
          echo "Issue Number   : ${{ github.event.issue.number }}"
          echo "Issue Title    : ${{ github.event.issue.title }}"
          echo "Issue Author   : ${{ github.event.issue.user.login }}"
          echo "Issue State    : ${{ github.event.issue.state }}"

      - name: Complex conditional - Route based on trigger type and labels
        if: |
          (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'bug')) ||
          (github.event_name == 'issues' && github.event.action == 'opened')
        run: echo "This should run on PR labeled 'bug' OR any new issue"

      - name: Use workflow dispatch input with context
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.debug_mode == 'true'
        run: echo "Debug mode enabled! Full context dump:"

      - name: Safe context access with defaults
        run: |
          TITLE="${{ github.event.pull_request.title || 'No title' }}"
          AUTHOR="${{ github.event.issue.user.login || 'Unknown' }}"
          echo "PR Title with default: $TITLE"
          echo "Issue author with default: $AUTHOR"
```

2. Push the workflow to your repository.

3. Trigger via different event types and observe the output:
   - Open a PR with the `bug` label → "Complex conditional" step should run
   - Create an issue → "Analyze Issue context" runs; "Complex conditional" runs (new issue)
   - Go to **Actions → Advanced Context Usage → Run workflow** → set `debug_mode=true` → "Use workflow dispatch input" runs

4. Modify the conditional on the "Complex conditional" step and test:

   ```yaml
   if: contains(github.event.pull_request.labels.*.name, 'enhancement')
   ```

**Success Criteria:**

- [ ] PR context shows correct branch names and author.
- [ ] Issue context correctly shows issue number and state.
- [ ] Complex conditional expression with `&&` / `||` operators evaluates correctly.
- [ ] The `join()` function correctly formats PR labels as comma-separated list.
- [ ] Workflow dispatch input conditionals work as expected.

**Expected Output (on PR with 'bug' label):**

```bash
=== PR Details ===
PR Title       : Fix authentication bug
PR Author      : teammate
PR Base Branch : main
PR Head Branch : fix/auth-bug
PR Draft?      : false
PR Labels      : bug, urgent
This should run on PR labeled 'bug' OR any new issue
```

**Troubleshooting:**

- **Nested property access returns empty** → Check event type; not all events populate all fields. Use `github.event_name` to guard access.
- **`contains()` returns false unexpectedly** → Label names may contain whitespace or special characters. Use `join()` and log the output to debug.
- **Complex conditional never evaluates to true** → Test each sub-expression individually using separate steps to identify which part fails.

**Time Estimate:** 20–25 minutes

**Variations/Extensions:**

- Build a label-based router: create multiple jobs, each with conditionals for specific labels
- Implement auto-labeling: use GitHub CLI within a step to add labels based on context analysis
- Create a "debug context dump" action that logs the entire `github`, `env`, and `job` contexts as JSON

**Cross-Section Connections:** → Section 3 (Job & needs context) for multi-job orchestration; → Section 9 (Conditional Logic) for more complex `if:` expressions; → Section 16 (REST API) for comparing context-based decisions with API queries.

</details>

---

<details>
<summary>🐛 Scenario 2-D: Troubleshooting Context Mismatches — Diagnosing Hidden Context Issues</summary>


**Objective:** Identify and fix scenarios where context values don't match expectations, including permission issues, event payload mismatches, and scope problems.

**Prerequisites:** Scenario 2-C complete; exposure to workflow failures.

**Context:** Subtle context problems cause silent failures: a conditional that should fire doesn't, an expected `secrets` value is `null`, or `github.event` is missing fields. Learning to diagnose these systematically is critical for real-world troubleshooting.

**Detailed Steps:**

1. **Scenario: PR labels context returns empty list**
   - Create a PR and add a label (`bug`).
   - Create `.github/workflows/diagnose-labels.yml`:

     ```yaml
     name: Diagnose Labels
     on: pull_request
     jobs:
       check-labels:
         runs-on: ubuntu-latest
         steps:
           - name: Print PR labels (should be non-empty)
             run: echo "Labels: ${{ github.event.pull_request.labels }}"

           - name: Check if empty
             run: |
               LABELS="${{ github.event.pull_request.labels }}"
               if [ -z "$LABELS" ]; then
                 echo "ERROR: Labels are empty! This may indicate wrong event type."
               fi
     ```

   - Push and create a PR. Labels should print as JSON array.
   - **Fix if empty:** Verify PR actually has labels applied; verify event type is `pull_request`.

2. **Scenario: Secrets context returns null**
   - Add a repository secret: `TEST_SECRET=value123`.
   - Create `.github/workflows/diagnose-secrets.yml`:

     ```yaml
     name: Diagnose Secrets
     on: workflow_dispatch
     jobs:
       check-secret:
         runs-on: ubuntu-latest
         steps:
           - name: Try to access secret
             run: echo "Secret length: ${{ secrets.TEST_SECRET != '' && 'set' || 'empty' }}"

           - name: Verify in env
             env:
               MY_SECRET: ${{ secrets.TEST_SECRET }}
             run: |
               if [ -z "$MY_SECRET" ]; then
                 echo "ERROR: Secret not available in this job!"
                 echo "Checking permissions..."
               fi
     ```

   - Push and manually trigger. If secret is empty, check:
     - [ ] Secret name matches exactly (case-sensitive)
     - [ ] Job doesn't have `permissions` block that restricts access
     - [ ] Workflow run is from a fork (fork PRs can't access secrets by default)

3. **Scenario: `github.event.pull_request` is null on wrong event**
   - Create `.github/workflows/diagnose-event.yml`:

     ```yaml
     name: Diagnose Event
     on:
       pull_request:
       push:
     jobs:
       check-event:
         runs-on: ubuntu-latest
         steps:
           - name: Print event type and PR availability
             run: |
               echo "Event: ${{ github.event_name }}"
               echo "PR: ${{ github.event.pull_request.number || 'NULL' }}"

               if [ "${{ github.event_name }}" = "push" ]; then
                 echo "On push: PR context is not available"
               fi
     ```

   - Push a commit (event type = `push`) and create a PR (event type = `pull_request`).
   - Observe: PR number is only available on `pull_request` events.
   - **Fix:** Guard context access with `if: github.event_name == 'pull_request'`.

4. **Scenario: Context differs between job and workflow level**
   - Create `.github/workflows/diagnose-scope.yml`:

     ```yaml
     name: Diagnose Scope
     env:
       WORKFLOW_VAR: from-workflow
     on: workflow_dispatch
     jobs:
       job1:
         env:
           JOB_VAR: from-job
         runs-on: ubuntu-latest
         steps:
           - name: Print scope
             env:
               STEP_VAR: from-step
             run: |
               echo "Workflow var: $WORKFLOW_VAR"
               echo "Job var: $JOB_VAR"
               echo "Step var: $STEP_VAR"
               # WORKFLOW_VAR and JOB_VAR visible here; step-level overrides all
     ```

   - Push and observe all three variables print correctly.
   - Modify step to remove `STEP_VAR` — `JOB_VAR` takes precedence.

**Success Criteria:**

- [ ] Label context returns correct label array when PR has labels.
- [ ] Secret context is available; if empty, you can identify the cause (name mismatch, wrong event, etc.).
- [ ] Event-specific context (e.g., `github.event.pull_request`) is only accessed on correct event types.
- [ ] Environment variable scope (workflow > job > step) is understood and predictable.

**Troubleshooting:**

- **Still can't access expected context** → Run `act push` or `act pull_request` locally with verbose logging to simulate the exact event.
- **Secret available locally but not in GitHub** → Verify secret is committed in `.github/workflows/` and not in `.gitignore`.

**Time Estimate:** 25–30 minutes

**Variations/Extensions:**

- Create a "context validator" action that checks for common misconfigurations and reports them
- Build a workflow that tests all supported event types and reports which context fields are available

**Cross-Section Connections:** → Section 15 (Debugging) for log inspection; → Section 8 (Secrets) for secret availability in different contexts.

</details>

---

<details>
<summary>🏢 Scenario 2-E: Enterprise Secret Management — Rotation, Organizational Secrets, and Protected Context Handling</summary>


**Objective:** Implement enterprise-grade secret management: using organizational secrets, automating secret rotation workflows, and ensuring secrets are accessed only by authorized workflows.

**Prerequisites:** Scenario 2-D complete; organization admin access.

**Context:** Large organizations must manage dozens of secrets across many repositories. GitHub supports organization-level secrets, environment secrets, and private secrets with restricted access. This scenario covers designing a secure, scalable secret management strategy.

**Detailed Steps:**

1. **Create Organization-Level Secret (Org Admin):**
   - Navigate to **Organization → Settings → Secrets and variables → Actions**.
   - Create `ORG_API_KEY` with value `org-secret-value-xyz`.
   - Set access to: "Private repositories" or "Selected repositories" (add your test repo).

2. **Create Environment Secret (Repo Admin):**
   - In your repository, go to **Settings → Environments → Create environment** named `production`.
   - Add environment secret: `PROD_DATABASE_URL = db.prod.internal`.
   - Set "Deployment branches" to only allow `main`.

3. **Create a Workflow Using Organization + Environment Secrets:**

```yaml
name: Secret Rotation Demo

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        default: 'production'
        type: choice
        options:
          - staging
          - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    steps:
      - name: Authenticate with org API key
        run: |
          echo "Authenticating with organization secret..."
          # In real scenario, use secret for API calls
          KEY_LENGTH=$(echo -n "${{ secrets.ORG_API_KEY }}" | wc -c)
          echo "API key is $KEY_LENGTH chars (value masked in logs)"

      - name: Access environment-specific secret
        run: |
          echo "Connecting to database..."
          # Real scenario: mysql -h ${{ secrets.PROD_DATABASE_URL }} ...
          echo "Database URL length: $(echo -n '${{ secrets.PROD_DATABASE_URL }}' | wc -c) chars"

      - name: Verify secret is masked
        run: |
          # Attempt to print the secret - GitHub auto-masks it
          echo "Secret attempt: ${{ secrets.ORG_API_KEY }}"
          # Output will show *** instead of actual value
```

4. **Implement Secret Rotation Automation:**
   - Create `.github/workflows/rotate-secrets.yml` (manually triggered):

```yaml
name: Rotate Secrets

on:
  workflow_dispatch:
    inputs:
      secret_name:
        description: 'Secret to rotate'
        required: true
        type: choice
        options:
          - ORG_API_KEY
          - PROD_DATABASE_URL

jobs:
  rotate:
    runs-on: ubuntu-latest
    steps:
      - name: Generate new secret value
        id: generate
        run: |
          NEW_VALUE=$(openssl rand -base64 32)
          echo "old_value_placeholder=***" >> $GITHUB_OUTPUT
          echo "new_value=$NEW_VALUE" >> $GITHUB_OUTPUT
          echo "rotation_timestamp=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_OUTPUT

      - name: Update secret (requires GitHub CLI + proper permissions)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # In real scenario, update via GitHub API or third-party secret manager
          echo "Secret rotation logged:"
          echo "  Secret: ${{ github.event.inputs.secret_name }}"
          echo "  Timestamp: ${{ steps.generate.outputs.rotation_timestamp }}"
          echo "  Status: PENDING (awaits manual update in Settings)"

      - name: Send notification
        run: echo "Secret rotation initiated. Update via Settings → Secrets within 5 minutes."
```

5. **Implement Secret Access Audit (via Workflow Logs):**
   - Create `.github/workflows/audit-secrets.yml`:

```yaml
name: Secret Access Audit

on:
  workflow_run:
    workflows: [Secret Rotation Demo, Deploy]
    types: [completed]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Log access attempt
        run: |
          echo "Workflow: ${{ github.event.workflow_run.name }}"
          echo "Conclusion: ${{ github.event.workflow_run.conclusion }}"
          echo "Secrets used: ORG_API_KEY, PROD_DATABASE_URL"
          echo "Timestamp: $(date -u)"
          # In real scenario, send to centralized audit log
```

6. **Test the Workflow:**
   - Trigger `Secret Rotation Demo` with `production` environment.
   - Verify it runs only on `main` branch (deployment branch restriction).
   - Verify secrets are masked in logs.
   - Run `Rotate Secrets` to simulate rotation (outputs will show placeholders).

**Success Criteria:**

- [ ] Organization secret is accessible to all authorized repositories.
- [ ] Environment secret is accessible only in the specified environment.
- [ ] Deployment branch restriction prevents `production` environment access from non-`main` branches.
- [ ] All secret values are masked in workflow logs.
- [ ] Rotation workflow provides audit trail (timestamps, secret names).
- [ ] Fresh checkout can access both org and environment secrets without additional auth.

**Expected Output:**

```bash
Authenticating with organization secret...
API key is 24 chars (value masked in logs)

Connecting to database...
Database URL length: 18 chars

Secret attempt: *** (masked by GitHub)

Secret rotation logged:
  Secret: ORG_API_KEY
  Timestamp: 2024-04-19T14:23:45Z
  Status: PENDING (awaits manual update in Settings)
```

**Troubleshooting:**

- **Organization secret not accessible** → Verify repository is in the "Selected repositories" list for the secret.
- **Environment deployment restriction not blocking** → Ensure you're pushing from a non-`main` branch; restriction only applies when workflow specifies `environment: production`.
- **Manual secret update tedious at scale** → In production, integrate with AWS Secrets Manager, HashiCorp Vault, or GitHub's native secret scanning APIs.

**Time Estimate:** 30–40 minutes

**Variations/Extensions:**

- Implement Vault integration using third-party actions for automatic secret retrieval
- Build a secret audit dashboard that tracks which workflows access which secrets
- Create a "secret expiration" workflow that alerts when secrets are old and need rotation
- Integrate GitHub Advanced Security to scan workflows for hardcoded secrets

**Cross-Section Connections:** → Section 8 (Environment Protection) for deployment approval workflows; → Section 23 (Security) for least-privilege GITHUB_TOKEN and secret scanning; → Section 22 (Enterprise Management) for organization-wide policies.

</details>

---

### 🔍 Section 3: Understanding GitHub Contexts - Part 2 (2 hrs)

**Topics:** [02-Contextual-Information.md](02-Contextual-Information.md) (second half) + [03-Context-Availability-Reference.md](03-Context-Availability-Reference.md)
**Overview:**

- The `job`, `runner`, `steps`, and `matrix` contexts
- The `inputs` context for workflow_dispatch and workflow_call
- The `needs` context for inter-job communication
- The `strategy` context for matrix configurations
- Static vs runtime evaluation of expressions

**Practical Work:**

- Use matrix context to access axis values
- Pass data between jobs using needs context
- Implement conditional expressions with context data
- Test context availability at different workflow scopes


<details>
<summary>📡 Scenario 3-A: Pass Data Between Jobs Using the `needs` Context</summary>


**Objective:** Create a two-job workflow where the `deploy` job consumes structured outputs produced by the `build` job via the `needs` context.

**Prerequisites:** Sections 1–2 complete; a GitHub repository where you can push and trigger Actions.

**Context:** Real CI/CD pipelines frequently need to hand data from one job to the next — a build number, a generated version string, or an artifact path. The `GITHUB_OUTPUT` file and the `needs.<job>.outputs` context are the canonical mechanism. Using the deprecated `set-output` command will fail on current runners.

**Detailed Steps:**

1. Create `.github/workflows/job-outputs.yml`:

```yaml
name: Job Outputs Demo

on:
  workflow_dispatch: # trigger manually from Actions tab

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      # Expose step outputs as job outputs
      version: ${{ steps.set-version.outputs.version }}
      artifact-name: ${{ steps.set-name.outputs.name }}
    steps:
      - name: Generate version string
        id: set-version
        # Write key=value pairs to $GITHUB_OUTPUT to expose step outputs
        run: echo "version=1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT

      - name: Generate artifact name
        id: set-name
        run: echo "name=app-${{ github.run_number }}" >> $GITHUB_OUTPUT

  deploy:
    runs-on: ubuntu-latest
    needs: build  # declares dependency; also makes build outputs accessible
    steps:
      - name: Consume build outputs
        run: |
          echo "Deploying version : ${{ needs.build.outputs.version }}"
          echo "Artifact name     : ${{ needs.build.outputs.artifact-name }}"
          echo "Triggered by      : ${{ github.actor }}"
```

2. Push the file and navigate to **Actions → Job Outputs Demo → Run workflow**.
3. In the run detail, click the `build` job → expand the "Generate version string" step → note the `version` output value.
4. Click the `deploy` job and verify the values in the "Consume build outputs" step match exactly.
5. Confirm the dependency arrow: the `deploy` job box appears only after `build` finishes.

**Success Criteria:**

- [ ] `build` job completes and its outputs appear in the Actions run summary.
- [ ] `deploy` job does not start until `build` finishes successfully.
- [ ] `deploy` logs print the correct version and artifact name from `build`.
- [ ] `needs.build.outputs.version` value matches `1.0.<run_number>` format.

**Expected Output (deploy job logs):**

```text
Deploying version : 1.0.42
Artifact name     : app-42
Triggered by      : your-username
```

**Troubleshooting:**

- **`needs.build.outputs.version` is empty** → Verify the step writing to `$GITHUB_OUTPUT` has an `id:` field. Without `id:`, you cannot reference that step's output at the job level.
- **`deploy` job is skipped** → If `build` fails, `deploy` is skipped by default. Check `build` logs for errors.
- **`>> $GITHUB_OUTPUT` vs `> $GITHUB_OUTPUT`** → Always append (`>>`); a single `>` overwrites the file and deletes previous outputs from the same job.

**Time Estimate:** 15–20 minutes

**Variations/Extensions:**

- Add a third `notify` job that needs `deploy` and prints `needs.deploy.outputs.*`.
- Store a JSON string in an output and parse it with `fromJSON()` in the consuming job.

**Cross-Section Connections:** → Section 9 (Conditional Logic) for using `needs.build.outputs.version` inside an `if:` expression; → Section 14 (Inter-Job Communication) for matrix output aggregation patterns.

</details>

---

<details>
<summary>🔀 Scenario 3-B: Implement Conditional Steps Using the `if:` Expression</summary>


**Objective:** Use `if:` expressions with `github` context values so that individual steps execute only in appropriate trigger scenarios.

**Prerequisites:** Section 2 (github context) completed; basic workflow structure understood.

**Context:** Without `if:` guards, every step runs regardless of trigger type, branch, or actor. Adding conditions prevents accidental deployments on PRs, avoids redundant notifications on scheduled runs, and blocks manual overrides on protected branches.

**Detailed Steps:**

1. Create `.github/workflows/conditional-steps.yml`:

```yaml
name: Conditional Steps Demo

on:
  push:
    branches: [main, develop]
  pull_request:
  workflow_dispatch:

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Always runs — print trigger info
        run: |
          echo "Event     : ${{ github.event_name }}"
          echo "Ref       : ${{ github.ref }}"
          echo "Head ref  : ${{ github.head_ref }}"

      - name: Only on push to main
        # Evaluates at runtime; no quotes needed around the expression
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: echo "✅ Production push detected — would trigger deploy"

      - name: Only on pull_request events
        if: github.event_name == 'pull_request'
        run: echo "📋 PR from '${{ github.head_ref }}' into '${{ github.base_ref }}'"

      - name: Only on workflow_dispatch
        if: github.event_name == 'workflow_dispatch'
        run: echo "🖱️ Manually triggered by ${{ github.actor }}"

      - name: Skip on forks — avoid leaking secrets
        if: github.repository_owner == github.event.repository.owner.login
        run: echo "Running in the origin repo — safe to use secrets"
```

2. Push to `develop` → verify only the "Always runs" step and none of the branch-specific steps execute.
3. Push to `main` → verify the "Only on push to main" step executes.
4. Open a PR → verify the pull_request step executes.
5. Trigger via `workflow_dispatch` → verify the manual step executes.

**Success Criteria:**

- [ ] Correct steps fire on each trigger type; incorrect ones show as **Skipped** (grey) in the UI.
- [ ] No step shows as **Failed** due to a condition syntax error.
- [ ] The "Skip on forks" step logic is understood and could be applied to protect secrets.

**Troubleshooting:**

- **All steps run regardless of condition** → Check for typos in the `if:` value. An expression syntax error causes the condition to evaluate as `true` (permissive default).
- **`github.head_ref` is empty on push** → `head_ref` is only populated on `pull_request` events. Use `github.ref` for branch name on push events.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 9 (Jobs & Steps) for `success()`, `failure()`, and `cancelled()` status functions in `if:` expressions; → Section 5 (Trigger Filters) for combining trigger conditions with `if:` guards.

</details>

---

<details>
<summary>🧠 Scenario 3-C: Advanced Conditional Logic — Nested Conditions, Status Checks, and Multi-Output Routing</summary>


**Objective:** Master complex conditional expressions using GitHub's built-in functions (`success()`, `failure()`, `cancelled()`, `always()`) and multi-branch routing based on step outputs and job status.

**Prerequisites:** Scenario 3-B complete; familiarity with basic `if:` expressions.

**Context:** Real workflows need sophisticated routing: run cleanup on failure, skip on cancellation, or branch logic based on step outputs. Mastering these patterns enables resilient, self-healing workflows.

**Detailed Steps:**

1. Create `.github/workflows/advanced-conditionals.yml`:

```yaml
name: Advanced Conditionals

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build application
        id: build
        run: |
          echo "🔨 Building..."
          # Simulate successful build
          echo "build_output=success" >> $GITHUB_OUTPUT
          echo "build_version=1.0.0" >> $GITHUB_OUTPUT

      - name: Run tests
        id: tests
        # Fail the tests to demonstrate failure handling
        run: |
          echo "🧪 Testing..."
          # Comment out the next line to make tests pass
          exit 1  # Simulate test failure

      - name: Report test results (runs only if tests pass)
        if: success()
        run: echo "✅ All tests passed!"

      - name: Collect failure info (runs only if any step failed)
        if: failure()
        run: |
          echo "❌ Build failed. Collecting debug info..."
          echo "Build output: ${{ steps.build.outputs.build_output }}"

      - name: Cleanup (always runs, even on cancellation)
        if: always()
        run: echo "🧹 Cleanup step (always runs)"

      - name: Determine next action based on status
        if: always()
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            echo "Job succeeded — would proceed to deployment"
          elif [ "${{ job.status }}" = "failure" ]; then
            echo "Job failed — would trigger rollback"
          fi

  deploy-on-success:
    # This entire job only runs if build-and-test succeeded
    if: success()
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to production
        run: echo "🚀 Deploying version ${{ needs.build-and-test.outputs.build_version }}"

  notify-on-failure:
    # This job only runs if build-and-test failed
    if: failure()
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Send failure notification
        run: echo "📧 Sent failure alert to team"
```

2. Push the workflow and trigger manually. Observe:
   - Build step succeeds → outputs `build_version`
   - Tests fail → "Report test results" is **Skipped**
   - Cleanup always runs
   - Deploy job is **Skipped** (because `success()` is false)
   - Notify job runs (because `failure()` is true)

3. Comment out `exit 1` to make tests pass, and re-trigger:
   - Cleanup runs (always)
   - Deploy job now runs
   - Notify job is **Skipped**

4. Add another job that uses `cancelled()`:

```yaml
  handle-cancellation:
    if: cancelled()
    runs-on: ubuntu-latest
    steps:
      - name: Log cancellation
        run: echo "⚠️ Workflow was cancelled by user"
```

**Success Criteria:**

- [ ] Step-level `if: success()` correctly skips when previous steps fail.
- [ ] Step-level `if: failure()` correctly activates when previous steps fail.
- [ ] `if: always()` runs regardless of previous step status.
- [ ] Job-level `if: success()` prevents deploy job from running when build fails.
- [ ] Job-level `if: failure()` activates notification job on build failure.
- [ ] Cancelling the workflow activates the `handle-cancellation` job.

**Troubleshooting:**

- **`success()` behaves unexpectedly** → Remember: `success()` checks if all previous steps succeeded, not just the prior step. For single-step checks, use explicit conditions.
- **Step output not visible in dependent step** → Ensure output is set via `id:` field and referenced as `${{ steps.STEPID.outputs.KEY }}`.

**Time Estimate:** 25–30 minutes

**Variations/Extensions:**

- Create a conditional matrix: apply different conditions to different matrix combinations
- Implement automatic retry logic: use a custom action that retries on specific failure patterns
- Build a "health check" job that runs after critical steps and can trigger rollback workflows

**Cross-Section Connections:** → Section 9 (Job Dependencies & needs) for coordinating multi-job workflows; → Section 15 (Debugging) for inspecting job status in logs.

</details>

---

<details>
<summary>🐛 Scenario 3-D: Troubleshooting Conditional Failures — Why Conditions Don't Fire and How to Debug Them</summary>


**Objective:** Diagnose and fix scenarios where conditions silently fail, misfire, or evaluate unexpectedly.

**Prerequisites:** Scenario 3-C complete; exposure to workflow runs.

**Context:** Silent failures in conditionals cause entire sections of workflows to be skipped without error messages. Learning systematic debugging techniques prevents days of frustration.

**Detailed Steps:**

1. **Scenario: Condition syntax error causes unexpected permissive behavior**
   - Create `.github/workflows/debug-conditional.yml`:

     ```yaml
     name: Debug Conditional
     on: workflow_dispatch
     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - name: Set output
             id: check
             run: echo "status=ready" >> $GITHUB_OUTPUT

           - name: Intentional syntax error (typo in function name)
             if: succes()  # Typo: missing final 's'
             run: echo "This should run but won't"
     ```

   - Push and trigger. The step is **Skipped** (not failed).
   - **Fix:** Change `succes()` to `success()`. Step now runs.
   - **Lesson:** GitHub doesn't error on typos; it defaults to `if: false`.

2. **Scenario: Using wrong output reference syntax**
   - Create `.github/workflows/debug-outputs.yml`:

     ```yaml
     name: Debug Outputs
     on: workflow_dispatch
     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - name: Set output
             id: check
             run: echo "status=ready" >> $GITHUB_OUTPUT

           - name: Wrong reference (should fail)
             if: steps.check.output.status == 'ready'  # Wrong: .output instead of .outputs
             run: echo "This won't run because reference syntax is wrong"

           - name: Correct reference
             if: steps.check.outputs.status == 'ready'  # Correct: .outputs (plural)
             run: echo "This will run"
     ```

   - First step with wrong reference is **Skipped**. Second step runs.
   - **Fix:** Always use `.outputs` (plural).

3. **Scenario: Condition references non-existent step**
   - Create `.github/workflows/debug-step-id.yml`:

     ```yaml
     name: Debug Step ID
     on: workflow_dispatch
     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - name: No ID assigned
             run: echo "no_id_here=value" >> $GITHUB_OUTPUT

           - name: Reference missing step ID
             if: steps.check.outputs.status == 'ready'  # Step ID 'check' doesn't exist
             run: echo "This step is skipped due to failed condition evaluation"

           - name: Correct reference
             id: check
             run: echo "status=ready" >> $GITHUB_OUTPUT
     ```

   - Push and observe: Condition evaluates to false; step skipped.
   - **Fix:** Ensure every step you reference in `if:` has an `id:` field.

4. **Scenario: Complex condition is hard to debug; need step-by-step validation**
   - Create `.github/workflows/debug-complex-condition.yml`:

     ```yaml
     name: Debug Complex
     on: workflow_dispatch
     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - name: Check condition components
             run: |
               echo "Event: ${{ github.event_name }}"
               echo "Ref: ${{ github.ref }}"
               echo "Actor: ${{ github.actor }}"
               echo "Repository: ${{ github.repository }}"

           - name: Break complex condition into steps
             run: |
               COND1=${{ github.event_name == 'workflow_dispatch' }}
               COND2=${{ github.ref == 'refs/heads/main' }}
               COND3=${{ github.actor == 'pbaletkeman' }}
               echo "Condition 1 (event): $COND1"
               echo "Condition 2 (ref): $COND2"
               echo "Condition 3 (actor): $COND3"
     ```

   - Run this to understand which part of a complex condition is false.

5. **Scenario: Job-level `if:` condition references outputs that don't exist yet**
   - Create `.github/workflows/debug-job-condition.yml`:

     ```yaml
     name: Debug Job Condition
     on: workflow_dispatch
     jobs:
       build:
         runs-on: ubuntu-latest
         outputs:
           version: ${{ steps.get-version.outputs.version }}
         steps:
           - name: Get version
             id: get-version
             run: echo "version=1.0.0" >> $GITHUB_OUTPUT

       deploy:
         # This condition evaluates BEFORE build job runs, so it can't access build's outputs
         if: needs.build.outputs.version == '1.0.0'
         needs: build
         runs-on: ubuntu-latest
         steps:
           - name: Deploy
             run: echo "Deploying version ${{ needs.build.outputs.version }}"
     ```

   - The deploy job won't run if the condition is wrong. Fix: ensure `build` job declares outputs with exact syntax.

**Success Criteria:**

- [ ] Can identify and fix syntax errors in conditions (typos in function names).
- [ ] Understand `.outputs` (plural) vs `.output` (wrong) reference syntax.
- [ ] Know how to reference step outputs using `steps.STEPID.outputs.KEY` format.
- [ ] Can break down complex conditions into sub-expressions to debug them.
- [ ] Understand the difference between step-level and job-level condition evaluation timing.

**Troubleshooting:**

- **Still can't see why condition fails** → Add explicit `run:` steps that print each condition component separately, then use those values in `if:`.
- **Job outputs not available to downstream job** → Ensure upstream job has `outputs:` section mapping step output to job output.

**Time Estimate:** 25–30 minutes

**Variations/Extensions:**

- Create a "condition validator" action that tests conditions before the workflow runs
- Implement conditional logging: only log detailed output when debug mode is enabled

**Cross-Section Connections:** → Section 15 (Debugging) for using `runner.debug` to enable verbose output; → Section 9 (Job Dependencies) for understanding `needs:` and job-level conditions.

</details>

---

<details>
<summary>🏢 Scenario 3-E: Enterprise-Scale Conditional Routing — Multi-Environment Deployments and Self-Healing Workflows</summary>


**Objective:** Design enterprise-grade workflows that intelligently route deployments across multiple environments based on branch, approval status, and health checks, with automatic rollback on failure.

**Prerequisites:** Scenario 3-D complete; understanding of job dependencies and outputs.

**Context:** Large organizations need safe, auditable deployment pipelines that prevent errors, enable team approval, and support automatic recovery. This scenario builds a production-grade workflow.

**Detailed Steps:**

1. **Create a Multi-Environment Deployment Workflow:**

```yaml
name: Enterprise Multi-Environment Deployment

on:
  push:
    branches:
      - main
      - staging
      - develop
  workflow_dispatch:
    inputs:
      target_env:
        description: 'Target environment (dev/staging/prod)'
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod

jobs:
  determine-environment:
    runs-on: ubuntu-latest
    outputs:
      target_env: ${{ steps.set-env.outputs.target_env }}
      approval_required: ${{ steps.set-env.outputs.approval_required }}
    steps:
      - name: Determine target environment from branch
        id: set-env
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TARGET="${{ github.event.inputs.target_env }}"
            APPROVAL="false"
          elif [ "${{ github.ref }}" = "refs/heads/main" ]; then
            TARGET="prod"
            APPROVAL="true"  # Production requires approval
          elif [ "${{ github.ref }}" = "refs/heads/staging" ]; then
            TARGET="staging"
            APPROVAL="false"
          else
            TARGET="dev"
            APPROVAL="false"
          fi
          echo "target_env=$TARGET" >> $GITHUB_OUTPUT
          echo "approval_required=$APPROVAL" >> $GITHUB_OUTPUT

  build:
    runs-on: ubuntu-latest
    outputs:
      build_tag: ${{ steps.build.outputs.image_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build container image
        id: build
        run: |
          IMAGE_TAG="ghcr.io/myorg/app:${{ github.sha }}"
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "🐳 Would build: $IMAGE_TAG"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Run test suite
        run: |
          echo "🧪 Running tests on ${{ needs.build.outputs.build_tag }}"
          # Simulate test pass/fail randomly
          [ $((RANDOM % 2)) -eq 0 ] && exit 0 || exit 1

  approve-production:
    # Only required for production deployments
    if: |
      needs.determine-environment.outputs.approval_required == 'true' &&
      needs.test.result == 'success'
    needs: [determine-environment, test]
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.determine-environment.outputs.target_env }}-approval
    steps:
      - name: Wait for manual approval
        run: echo "⏳ Waiting for production approval (via GitHub Environment)"

  deploy:
    if: needs.test.result == 'success'
    needs: [determine-environment, build, test, approve-production]
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.determine-environment.outputs.target_env }}
      url: https://${{ needs.determine-environment.outputs.target_env }}.myapp.com
    steps:
      - name: Deploy to ${{ needs.determine-environment.outputs.target_env }}
        run: |
          echo "🚀 Deploying ${{ needs.build.outputs.build_tag }} to ${{ needs.determine-environment.outputs.target_env }}"

      - name: Wait for rollout (simulate)
        run: sleep 10 && echo "✅ Deployment complete"

      - name: Health check
        id: health
        run: |
          echo "🏥 Running health checks..."
          # Simulate health check pass/fail
          [ $((RANDOM % 2)) -eq 0 ] && exit 0 || exit 1

  rollback:
    # Only runs if deploy fails
    if: failure() && needs.deploy.result == 'failure'
    needs: [determine-environment, deploy]
    runs-on: ubuntu-latest
    steps:
      - name: Initiate rollback
        run: |
          echo "⚠️ Deployment failed! Rolling back ${{ needs.determine-environment.outputs.target_env }}"
          echo "Using previous stable version..."

      - name: Notify team
        run: echo "📧 Sent rollback notification to on-call team"

  notify:
    if: always()
    needs: [determine-environment, deploy]
    runs-on: ubuntu-latest
    steps:
      - name: Send deployment summary
        run: |
          STATUS="${{ needs.deploy.result }}"
          ENV="${{ needs.determine-environment.outputs.target_env }}"
          echo "📊 Deployment Summary:"
          echo "  Environment: $ENV"
          echo "  Status: $STATUS"
          echo "  Timestamp: $(date -u)"
```

2. **Create Environment Protection Rules (via Settings):**
   - Navigate to **Settings → Environments → prod-approval** (or via UI create it).
   - Enable "Required reviewers": add team members.
   - Set "Deployment branches and tags" to `main` only.
   - This enforces approval before the `approve-production` job completes.

3. **Trigger the Workflow:**
   - Push to `main` → workflow waits for approval before deploying to production.
   - Push to `staging` → workflow deploys to staging without approval.
   - Push to `develop` → workflow deploys to dev.
   - Manually trigger → choose target environment.

4. **Monitor and Handle Failures:**
   - If tests fail, deploy is **Skipped**.
   - If deploy fails health check, rollback job runs.
   - Notify job always summarizes outcome.

**Success Criteria:**

- [ ] Workflow correctly routes to dev/staging/prod based on branch.
- [ ] Production deployments require manual approval (via environment protection rule).
- [ ] Tests must pass before deployment; deploy is skipped on test failure.
- [ ] Rollback job only runs on deployment failure.
- [ ] Health check determines deployment success.
- [ ] Notification job always runs regardless of outcome.

**Troubleshooting:**

- **Approve job never completes** → Verify environment protection rule is configured in Settings; GitHub won't enforce approvals without the rule.
- **Deploy job always skipped** → Check `needs.test.result == 'success'` syntax; ensure test job has `id:` if needed.
- **Rollback logic doesn't trigger** → Test job must succeed for deploy to run; if test fails, entire downstream chain is skipped.

**Time Estimate:** 35–45 minutes

**Variations/Extensions:**

- Implement canary deployments: deploy to 10% of production first, then full rollout
- Add feature flag toggles: use context to enable/disable features per environment
- Integrate with incident management: auto-page on-call team if rollback is triggered
- Build approval analytics: track approval times and approval rates per environment

**Cross-Section Connections:** → Section 8 (Environment Protection) for manual approval setup; → Section 15 (Debugging) for understanding job results in complex workflows; → Section 22 (Enterprise Runners) for organizational deployment policies.

</details>

---

### 📝 Section 4: YAML Syntax & Workflow File Structure (2 hrs)

**Topics:** [04-Workflow-File-Structure.md](04-Workflow-File-Structure.md)
**Overview:**

- GitHub Actions workflow file anatomy
- Top-level keys: name, on, env, jobs, defaults, concurrency
- YAML anchors (&), aliases (*), and merge operators (<<)
- Proper indentation and YAML nesting
- Workflow file naming and placement (.github/workflows/)
- Schema validation and common YAML errors

**Practical Work:**

- Create a workflow file from scratch with proper structure
- Implement YAML anchors to reduce duplication
- Fix syntax errors in sample workflows
- Use schema validation to catch mistakes early


<details>
<summary>🏗️ Scenario 4-A: Build a Workflow from Scratch with Correct Structure</summary>


**Objective:** Author a complete, valid workflow file from memory, correctly using every top-level key and understanding the relationship between `on`, `env`, `defaults`, `concurrency`, and `jobs`.

**Prerequisites:** Sections 1–3 complete; VS Code with GitHub Actions extension installed.

**Context:** The GH-200 exam tests whether you know the exact YAML structure of a workflow file — valid key names, nesting levels, and which keys are optional vs required. Writing one from scratch without autocomplete is the best way to internalize the schema.

**Detailed Steps:**

1. Without using IntelliSense, write `.github/workflows/full-structure.yml` from scratch:

```yaml
name: Full Structure Demo

# Top-level trigger configuration
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Workflow-level environment variables (available to all jobs)
env:
  NODE_VERSION: '20'
  BUILD_ENV: production

# Default shell and working directory for all run steps
defaults:
  run:
    shell: bash
    working-directory: ./src

# Prevent concurrent runs of the same workflow on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Application
    runs-on: ubuntu-latest

    # Job-level env overrides workflow-level env
    env:
      BUILD_ENV: development  # overrides workflow-level BUILD_ENV for this job

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install and build
        # working-directory defaults to ./src from the defaults block
        run: |
          echo "Building in $(pwd)"
          echo "Environment: $BUILD_ENV"

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: build  # runs after build completes

    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        # Step-level env overrides all higher scopes
        env:
          BUILD_ENV: test
        run: echo "Test env: $BUILD_ENV"
```

2. Validate with the VS Code extension — confirm no red squiggles.
3. Push and verify the `test` job waits for `build` to finish in the Actions UI.
4. Inspect the Actions run and confirm `concurrency` prevents a second run if you push twice quickly.

**Success Criteria:**

- [ ] File has correct top-level keys: `name`, `on`, `env`, `defaults`, `concurrency`, `jobs`.
- [ ] `defaults.run` applies to all `run:` steps unless overridden.
- [ ] `env` precedence: step > job > workflow — confirmed by log output.
- [ ] `concurrency` cancels an in-progress run when a new push arrives.

**Troubleshooting:**

- **`defaults.run.working-directory` causes "no such directory" error** → The `src/` directory must exist. Remove the `working-directory` setting or create the directory in a prior step.
- **`concurrency` not cancelling** → The `group` expression must be identical for the cancellation to apply. Verify `${{ github.ref }}` resolves consistently between runs.

**Time Estimate:** 20–25 minutes

</details>

---

<details>
<summary>🔧 Scenario 4-B: YAML Anchors and Fixing Common Syntax Errors</summary>


**Objective:** Use YAML anchors and merge keys to eliminate step duplication, then practice identifying and fixing the five most common GitHub Actions YAML errors.

**Prerequisites:** Scenario 4-A complete; basic understanding of YAML data types.

**Context:** YAML anchors (`&`) and aliases (`*`) let you define a block once and reference it multiple times. This is useful for repeated `env:` blocks or common steps across jobs. Knowing how to spot indentation errors, wrong key names, and type mismatches is essential for both the exam and real-world debugging.

**Detailed Steps:**

1. Create `.github/workflows/yaml-anchors.yml` demonstrating anchor usage:

```yaml
name: YAML Anchors Demo

on: workflow_dispatch

# Define a reusable environment block with an anchor
.common-env: &common-env
  NODE_ENV: production
  LOG_LEVEL: info
  TIMEOUT: '30'

jobs:
  job-a:
    runs-on: ubuntu-latest
    env:
      <<: *common-env  # merge all keys from common-env anchor
      EXTRA_VAR: job-a-only
    steps:
      - run: env | grep -E 'NODE_ENV|LOG_LEVEL|TIMEOUT|EXTRA_VAR'

  job-b:
    runs-on: ubuntu-latest
    env:
      <<: *common-env  # same anchor, different job
      EXTRA_VAR: job-b-only
    steps:
      - run: env | grep -E 'NODE_ENV|LOG_LEVEL|TIMEOUT|EXTRA_VAR'
```

2. Push and verify both jobs show identical values for `NODE_ENV`, `LOG_LEVEL`, and `TIMEOUT` but different `EXTRA_VAR` values.

3. Now practice fixing these intentionally broken YAML snippets. For each, identify the error before reading the fix:

**Error 1 — Wrong indentation:**

```yaml
# BROKEN
jobs:
  build:
  runs-on: ubuntu-latest  # ❌ should be indented under build:
```

Fix: indent `runs-on:` two more spaces under `build:`.

**Error 2 — Missing required key:**

```yaml
# BROKEN
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run tests
        # ❌ missing 'run:' or 'uses:' — a step must have one of them
```

Fix: add `run: pytest` or `uses: actions/...`.

**Error 3 — Incorrect boolean type:**

```yaml
# BROKEN
- name: Continue on error
  run: false-command
  continue-on-error: True  # ❌ YAML True ≠ GitHub Actions boolean true
```

Fix: use lowercase `true` (YAML boolean, not string).

**Error 4 — Expression in wrong context:**

```yaml
# BROKEN
on:
  push:
    branches: [${{ github.ref }}]  # ❌ expressions not evaluated in on: block
```

Fix: expressions are only evaluated at runtime inside `jobs:`; use a literal branch name.

**Error 5 — Duplicate key:**

```yaml
# BROKEN
jobs:
  build:
    runs-on: ubuntu-latest
    runs-on: windows-latest  # ❌ duplicate key; last value silently wins
```

Fix: remove the duplicate `runs-on:` line.

**Success Criteria:**

- [ ] Anchors workflow runs without errors; both jobs show identical shared env vars.
- [ ] All 5 broken snippets identified and corrected before viewing fixes.
- [ ] VS Code extension flags at least Errors 1 and 2 with visible warnings.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for reading YAML parse errors in workflow run logs; → Section 4 (YAML Structure) self-referential — this is the deepest dive into the topic.

</details>

---

<details>
<summary>♻️ Scenario 4-C: Multi-File Workflow Composition and DRY Principles — Reusable Workflow Components</summary>


**Objective:** Organize complex workflows using composition patterns: splitting large workflows into smaller reusable units, using defaults, and eliminating duplication.

**Prerequisites:** Scenario 4-B complete; comfort with YAML anchors and aliases.

**Context:** Large workflows become unmaintainable as they grow. Professional teams break workflows into logical components using GitHub's reusable workflow feature combined with YAML composition patterns and defaults blocks.

**Detailed Steps:**

1. **Create a Shared Defaults Workflow:**
   - Create `.github/workflows/defaults-base.yml`:

     ```yaml
     # This is not a callable reusable workflow, but a documentation of standard defaults
     # Teams use this as a template for consistency

     name: Defaults Template
     on: [push, pull_request]

     env:
       # Organization-wide defaults
       NODE_VERSION: '20'
       CACHE_KEY_PREFIX: ci-cache
       ARTIFACT_RETENTION_DAYS: 5

     defaults:
       run:
         shell: bash
         working-directory: ./src

     jobs:
       build:
         runs-on: ubuntu-latest
         steps:
           - name: Checkout
             uses: actions/checkout@v4

           - name: Setup Node
             uses: actions/setup-node@v4
             with:
               node-version: ${{ env.NODE_VERSION }}
     ```

2. **Create Reusable Workflow for Common Tasks:**
   - Create `.github/workflows/reusable-build.yml`:

     ```yaml
     name: Reusable Build Workflow

     on:
       workflow_call:
         inputs:
           build-flags:
             description: 'Build command flags'
             required: false
             type: string
             default: '--production'
         outputs:
           artifact-path:
             description: 'Path to build artifact'
             value: ${{ jobs.build.outputs.artifact }}

     jobs:
       build:
         runs-on: ubuntu-latest
         outputs:
           artifact: ./dist/app.zip
         steps:
           - name: Checkout
             uses: actions/checkout@v4

           - name: Build
             run: |
               npm install
               npm run build ${{ inputs.build-flags }}

           - name: Upload artifact
             uses: actions/upload-artifact@v3
             with:
               name: build-artifact
               path: dist/
     ```

3. **Create Reusable Workflow for Testing:**
   - Create `.github/workflows/reusable-test.yml`:

     ```yaml
     name: Reusable Test Workflow

     on:
       workflow_call:
         inputs:
           test-type:
             description: 'Type of tests (unit|integration|all)'
             required: false
             type: string
             default: 'all'

     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - name: Checkout
             uses: actions/checkout@v4

           - name: Setup
             uses: actions/setup-node@v4
             with:
               node-version: '20'
               cache: npm

           - name: Run tests
             run: npm test -- --type=${{ inputs.test-type }}
     ```

4. **Compose Main Workflow Using Reusable Components:**
   - Create `.github/workflows/main-composed.yml`:

     ```yaml
     name: Main Composed Workflow
     on:
       push:
         branches: [main]
       pull_request:

     jobs:
       # Reuse the test workflow
       run-tests:
         uses: ./.github/workflows/reusable-test.yml
         with:
           test-type: unit

       # Only build if tests pass
       build-app:
         needs: run-tests
         uses: ./.github/workflows/reusable-build.yml
         with:
           build-flags: '--production --minify'

       # Downstream job
       verify-artifact:
         needs: build-app
         runs-on: ubuntu-latest
         steps:
           - name: Verify artifact
             run: echo "Artifact at: ${{ needs.build-app.outputs.artifact-path }}"
     ```

5. **Test the Composed Workflow:**
   - Push to `main` → workflow runs composed structure
   - Modify reusable workflow input → main workflow inherits changes
   - Verify job dependencies and output passing work as expected

**Success Criteria:**

- [ ] Reusable workflows are callable and parameterized.
- [ ] Main workflow composes multiple reusable workflows using `uses:` and `with:`.
- [ ] Outputs from reusable workflows are accessible to downstream jobs.
- [ ] Modifications to reusable workflow are inherited by all callers.
- [ ] Job dependencies are maintained correctly across reusable boundaries.

**Troubleshooting:**

- **Reusable workflow not found** → Ensure workflow file is in `.github/workflows/` directory.
- **Output not available** → Ensure reusable workflow declares `outputs:` at job level and job level.

**Time Estimate:** 20–25 minutes

**Variations/Extensions:**

- Create a library of 5+ reusable workflows covering all standard tasks
- Implement workflow versioning: use git tags to pin reusable workflow versions
- Build a workflow discovery system: document all reusable workflows in a catalog

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for detailed cross-repo workflow sharing; → Section 25 (Performance) for composition patterns that improve workflow performance.

</details>

---

<details>
<summary>🔄 Scenario 4-D: Migrating Legacy Workflow to Modern YAML Syntax and Validating Structure</summary>


**Objective:** Take an outdated or poorly-structured workflow and modernize it using current best practices, then validate it with tooling.

**Prerequisites:** Scenario 4-C complete; exposure to YAML syntax rules.

**Context:** Older workflows often lack structure, use deprecated features, or have suboptimal patterns. Learning to refactor them teaches best practices while preserving functionality.

**Detailed Steps:**

1. **Start with a Legacy Workflow (Anti-Patterns):**
   - Create `.github/workflows/legacy-broken.yml`:

     ```yaml
     name: Old Workflow
     on: [push]
     jobs:
       job1:
         runs-on: ubuntu-latest
         steps:
           - uses: actions/checkout@v2  # Outdated version
           - run: |
               echo "Step 1"
               npm install
               npm test
               npm run build
               # No separation of concerns

           - run: |
               echo "Uploading..."
               # Hard-coded paths; not maintainable
               ls -la /home/runner/work/repo/repo/dist/

       job2:
         needs: job1  # But no outputs defined
         runs-on: ubuntu-latest
         steps:
           - run: echo "Deploy"
           # No artifact retrieval
     ```

2. **Identify Anti-Patterns:**
   - ✗ Outdated action versions (`@v2`)
   - ✗ Multiple commands in single `run:` step (no separation)
   - ✗ Hard-coded paths instead of `env:` or `$GITHUB_WORKSPACE`
   - ✗ Job dependencies without output passing
   - ✗ No error handling or conditional steps
   - ✗ Missing artifact upload/download

3. **Refactor to Modern Syntax:**
   - Create `.github/workflows/modern-refactored.yml`:

     ```yaml
     name: Refactored Workflow

     # Modern best practices
     on:
       push:
         branches: [main]
       pull_request:

     env:
       NODE_VERSION: '20'
       ARTIFACT_DIR: dist

     defaults:
       run:
         shell: bash

     jobs:
       build:
         name: Build & Test
         runs-on: ubuntu-latest
         outputs:
           build-status: ${{ job.status }}
           artifact-size: ${{ steps.build.outputs.size }}
         steps:
           - name: Checkout repository
             uses: actions/checkout@v4  # Updated version

           - name: Setup Node.js
             uses: actions/setup-node@v4
             with:
               node-version: ${{ env.NODE_VERSION }}
               cache: npm

           - name: Install dependencies
             run: npm ci  # Better than npm install for CI

           - name: Run tests
             run: npm test -- --coverage

           - name: Build application
             id: build
             run: |
               npm run build
               SIZE=$(du -sh ${{ env.ARTIFACT_DIR }} | cut -f1)
               echo "size=$SIZE" >> $GITHUB_OUTPUT

           - name: Upload artifact
             uses: actions/upload-artifact@v3
             with:
               name: build-${{ github.sha }}
               path: ${{ env.ARTIFACT_DIR }}
               retention-days: 5

       deploy:
         name: Deploy to Production
         needs: build
         if: success() && github.ref == 'refs/heads/main'
         runs-on: ubuntu-latest
         environment:
           name: production
           url: https://myapp.com
         steps:
           - name: Download artifact
             uses: actions/download-artifact@v3
             with:
               name: build-${{ github.sha }}
               path: ${{ env.ARTIFACT_DIR }}

           - name: Deploy
             run: |
               echo "Deploying ${{ env.ARTIFACT_DIR }}"
               ls -la ${{ env.ARTIFACT_DIR }}
     ```

4. **Validate Refactored Workflow:**
   - Use `actionlint` locally to check for issues:

     ```bash
     actionlint modern-refactored.yml
     ```

   - Use VS Code extension to verify no syntax errors
   - Run `act` locally to simulate execution:

     ```bash
     act push -j build
     ```

5. **Compare Both Workflows:**
   - Document improvements made (versions, structure, artifact handling, etc.)
   - Test modern workflow to verify it produces same result as legacy

**Success Criteria:**

- [ ] All action versions are updated to v4 or latest stable.
- [ ] Steps are logically separated (one task per step).
- [ ] Artifact upload/download is properly implemented.
- [ ] Job outputs are defined and used by downstream jobs.
- [ ] Conditional logic (`if:`) is used appropriately.
- [ ] Refactored workflow passes `actionlint` validation.
- [ ] Local `act` simulation succeeds without errors.

**Troubleshooting:**

- **Build artifact not found** → Ensure artifact upload path matches build output path.
- **Downstream job doesn't receive artifact** → Verify artifact name matches exactly between upload and download.

**Time Estimate:** 25–30 minutes

**Variations/Extensions:**

- Create a "workflow modernization checklist" for teams upgrading workflows
- Automate detection of outdated action versions across repos
- Build a script that converts legacy workflows to modern syntax automatically

**Cross-Section Connections:** → Section 15 (Debugging) for using `act` to test workflows locally; → Section 12 (Artifacts) for proper artifact handling patterns.

</details>

---

<details>
<summary>⚙️ Scenario 4-E: Programmatically Generate Workflow YAML from Schema and Validation Automation</summary>


**Objective:** Build a system to generate workflow files from a schema definition, enabling consistency, validation, and rapid workflow creation.

**Prerequisites:** Scenario 4-D complete; comfort with JSON/YAML schema concepts.

**Context:** Organizations managing 100+ workflows benefit from programmatic generation: consistency is enforced at creation time, and updates can be rolled out automatically. This scenario teaches infrastructure-as-code thinking for CI/CD.

**Detailed Steps:**

1. **Define a Workflow Schema (JSON Schema):**
   - Create `workflow-schema.json`:

     ```json
     {
       "$schema": "http://json-schema.org/draft-07/schema#",
       "title": "GitHub Actions Workflow Schema",
       "type": "object",
       "properties": {
         "name": {"type": "string", "description": "Workflow name"},
         "trigger": {
           "type": "string",
           "enum": ["push", "pull_request", "schedule", "workflow_dispatch"],
           "description": "Primary trigger type"
         },
         "node_version": {"type": "string", "default": "20"},
         "include_tests": {"type": "boolean", "default": true},
         "include_build": {"type": "boolean", "default": true},
         "include_deploy": {"type": "boolean", "default": false},
         "deploy_env": {"type": "string", "enum": ["staging", "production"]},
         "artifact_retention": {"type": "integer", "default": 5}
       },
       "required": ["name", "trigger"]
     }
     ```

2. **Create a Workflow Configuration File:**
   - Create `workflow-config.json`:

     ```json
     {
       "name": "Build, Test, Deploy",
       "trigger": "push",
       "node_version": "20",
       "include_tests": true,
       "include_build": true,
       "include_deploy": true,
       "deploy_env": "staging",
       "artifact_retention": 7
     }
     ```

3. **Create a Generator Script (Node.js):**
   - Create `generate-workflow.js`:

     ```javascript
     #!/usr/bin/env node

     const fs = require('fs');
     const path = require('path');
     const Ajv = require('ajv');

     // Load schema
     const schema = JSON.parse(fs.readFileSync('workflow-schema.json', 'utf8'));
     const config = JSON.parse(fs.readFileSync('workflow-config.json', 'utf8'));

     // Validate config against schema
     const ajv = new Ajv();
     const validate = ajv.compile(schema);
     const valid = validate(config);

     if (!valid) {
       console.error('Config validation failed:', validate.errors);
       process.exit(1);
     }

     // Generate workflow YAML
     const workflow = {
       name: config.name,
       on: {
         [config.trigger]: config.trigger === 'schedule' ?
           [{ cron: '0 0 * * *' }] :
           config.trigger === 'workflow_dispatch' ?
           {} :
           { branches: ['main'] }
       },
       env: {
         NODE_VERSION: config.node_version,
         ARTIFACT_RETENTION_DAYS: config.artifact_retention
       },
       jobs: {}
     };

     // Build job
     if (config.include_build) {
       workflow.jobs.build = {
         'runs-on': 'ubuntu-latest',
         steps: [
           { uses: 'actions/checkout@v4' },
           {
             uses: 'actions/setup-node@v4',
             with: {
               'node-version': '${{ env.NODE_VERSION }}',
               cache: 'npm'
             }
           },
           { run: 'npm ci' },
           {
             run: 'npm run build',
             if: config.include_tests ? 'success()' : 'always()'
           },
           {
             uses: 'actions/upload-artifact@v3',
             with: {
               name: 'build-${{ github.sha }}',
               path: 'dist/',
               'retention-days': config.artifact_retention
             }
           }
         ]
       };
     }

     // Test job
     if (config.include_tests) {
       workflow.jobs.test = {
         'runs-on': 'ubuntu-latest',
         steps: [
           { uses: 'actions/checkout@v4' },
           {
             uses: 'actions/setup-node@v4',
             with: {
               'node-version': '${{ env.NODE_VERSION }}',
               cache: 'npm'
             }
           },
           { run: 'npm ci' },
           { run: 'npm test' }
         ]
       };
     }

     // Deploy job
     if (config.include_deploy) {
       const build_needs = config.include_build ? 'build' : undefined;
       workflow.jobs.deploy = {
         'runs-on': 'ubuntu-latest',
         needs: build_needs,
         environment: {
           name: config.deploy_env,
           url: `https://${config.deploy_env}.myapp.com`
         },
         if: 'success()',
         steps: [
           ...(config.include_build ? [{
             uses: 'actions/download-artifact@v3',
             with: {
               name: 'build-${{ github.sha }}',
               path: 'dist/'
             }
           }] : []),
           {
             run: `echo "Deploying to ${config.deploy_env}"`
           }
         ]
       };
     }

     // Convert to YAML and write
     const yaml = require('js-yaml');
     const output = yaml.dump(workflow, { lineWidth: -1 });

     fs.writeFileSync(
       path.join('.github/workflows/generated-workflow.yml'),
       output
     );

     console.log('✅ Workflow generated successfully!');
     console.log(`📋 Output: .github/workflows/generated-workflow.yml`);
     ```

4. **Create Validation Script:**
   - Create `validate-workflow.js`:

     ```javascript
     #!/usr/bin/env node

     const fs = require('fs');
     const { spawnSync } = require('child_process');

     // Validate with actionlint
     const result = spawnSync('actionlint', [
       '.github/workflows/generated-workflow.yml'
     ], { encoding: 'utf8' });

     if (result.error || result.status !== 0) {
       console.error('Validation failed:');
       console.error(result.stderr || result.stdout);
       process.exit(1);
     }

     console.log('✅ Workflow validation passed!');
     ```

5. **Generate and Validate:**
   - Run:

     ```bash
     node generate-workflow.js
     node validate-workflow.js
     ```

   - Review generated `.github/workflows/generated-workflow.yml`
   - Modify `workflow-config.json` and regenerate to see changes

**Success Criteria:**

- [ ] Config file validates against schema.
- [ ] Generated workflow is valid YAML.
- [ ] `actionlint` validates generated workflow without errors.
- [ ] Changing config generates different workflow correctly.
- [ ] Generated workflow structure matches expected organization standards.

**Troubleshooting:**

- **Schema validation fails** → Check config JSON against schema requirements.
- **Generated workflow has syntax errors** → Verify generator logic handles all config combinations.

**Time Estimate:** 30–40 minutes

**Variations/Extensions:**

- Extend schema to support matrix jobs, multiple triggers, and custom steps
- Integrate generator into CI/CD pipeline to auto-generate workflows on config change
- Create a web UI for non-technical users to configure and generate workflows
- Build a workflow template library with pre-configured generators

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for template-based workflow creation; → Section 25 (Performance) for parameterized workflow optimization.

</details>

---

## ⚡ Week 2: Workflow Triggers & Events

### ⚡ Section 5: Workflow Trigger Events - Part 1 (2 hrs)

**Topics:** [05-Workflow-Trigger-Events.md](05-Workflow-Trigger-Events.md) (first half)
**Overview:**

- Repository events: `push`, `pull_request`, `pull_request_review`
- Scheduled events: `schedule` with cron syntax
- Manual trigger: `workflow_dispatch` and inputs validation
- Repository lifecycle events: create, delete, fork, public
- Status events: workflow_run, deployment, deployment_status

**Practical Work:**

- Configure workflows to trigger on push to specific branches
- Set up scheduled workflows with cron expressions
- Validate cron syntax for different time zones
- Create workflow_dispatch with typed inputs


<details>
<summary>⚡ Scenario 5-A: Configure Push Triggers with Branch and Path Filtering</summary>


**Objective:** Create a workflow that triggers only when relevant files are changed on relevant branches, preventing unnecessary CI runs on documentation-only commits.

**Prerequisites:** Sections 1–4 complete; repository has at least a `main` branch and some source files.

**Context:** In a monorepo or active open-source project, triggering every workflow on every push wastes CI minutes and creates noise. `paths` filters ensure the frontend pipeline only runs when frontend code changes, and `branches` filters prevent feature branches from triggering production deploy workflows.

**Detailed Steps:**

1. Create two directories to simulate a project structure:

   ```text
   src/app.js
   docs/README.md
   package.json
   ```

2. Create `.github/workflows/filtered-ci.yml`:

```yaml
name: Filtered CI

on:
  push:
    branches:
      - main
      - 'release/**'     # matches release/v1.0, release/v2.0, etc.
    paths:
      - 'src/**'         # any change under src/
      - 'package.json'   # root package manifest
      - '!src/**/*.md'   # exclude markdown inside src/ (negation)
  pull_request:
    branches: [main]
    paths:
      - 'src/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Show what triggered this run
        run: |
          echo "Event  : ${{ github.event_name }}"
          echo "Branch : ${{ github.ref_name }}"
          echo "Actor  : ${{ github.actor }}"
```

3. Push a change to `src/app.js` on `main` → verify workflow triggers.
4. Push a change to `docs/README.md` on `main` → verify workflow is **not** triggered.
5. Push to a branch named `feature/login` → verify workflow is **not** triggered.
6. Create a branch `release/v1.0` and push a `src/` change → verify workflow triggers.

**Success Criteria:**

- [ ] `src/` changes on `main` trigger the workflow.
- [ ] `docs/` or `README.md` changes on `main` do **not** trigger the workflow.
- [ ] Pushes to `feature/*` branches do **not** trigger the workflow.
- [ ] `release/v1.0` branch pushes with `src/` changes **do** trigger the workflow.

**Troubleshooting:**

- **Workflow ignores path filter (runs on every push)** → Check YAML indentation; `paths:` must be indented under the event key (`push:`), not at the top `on:` level.
- **`release/**` pattern not matching** → The `**` glob in branch patterns matches any characters including `/`. Use it consistently; `release/*` only matches one level deep.
- **Negation `!src/**/*.md` not working** → Negation patterns only exclude files when at least one non-negated path pattern already matches.

**Time Estimate:** 15–20 minutes

**Variations/Extensions:**

- Add a `paths-ignore:` list instead of negation patterns for clarity.
- Combine `tags` trigger for release tagging: `on: { push: { tags: ['v*'] } }`.

**Cross-Section Connections:** → Section 6 (Trigger Events Part 2) for `paths-ignore` vs negation and tag triggers; → Section 9 (Conditional Logic) for adding `if:` to individual jobs as a secondary filter.

</details>

---

<details>
<summary>🎮 Scenario 5-B: Create a `workflow_dispatch` with Typed and Validated Inputs</summary>


**Objective:** Build a manually-triggered workflow with `choice`, `boolean`, and `string` typed inputs, including server-side validation logic.

**Prerequisites:** Section 5-A complete; familiarity with `run:` steps and `if:` conditions.

**Context:** `workflow_dispatch` inputs replace fragile "magic branch names" or commit message conventions as a way to pass parameters to manual runs. Typed inputs render as a form in the GitHub UI, reducing human error and eliminating the need for documentation about what values are acceptable.

**Detailed Steps:**

1. Create `.github/workflows/manual-deploy.yml`:

```yaml
name: Manual Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options:
          - development
          - staging
          - production
        default: development

      version:
        description: 'Version tag to deploy (e.g. 1.0.0)'
        required: true
        type: string

      dry-run:
        description: 'Preview changes without deploying'
        required: false
        type: boolean
        default: true

      send-notification:
        description: 'Notify Slack after deploy'
        required: false
        type: boolean
        default: false

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate production requires explicit version
        # Prevent accidental production deploys with empty/default versions
        if: inputs.environment == 'production' && inputs.version == ''
        run: |
          echo "ERROR: A specific version is required for production deployments."
          exit 1

      - name: Print resolved inputs
        run: |
          echo "Environment       : ${{ inputs.environment }}"
          echo "Version           : ${{ inputs.version }}"
          echo "Dry run           : ${{ inputs.dry-run }}"
          echo "Send notification : ${{ inputs.send-notification }}"

  deploy:
    runs-on: ubuntu-latest
    needs: validate
    steps:
      - name: Simulate deployment
        run: |
          if [ "${{ inputs.dry-run }}" == "true" ]; then
            echo "DRY RUN: would deploy v${{ inputs.version }} → ${{ inputs.environment }}"
          else
            echo "DEPLOYING v${{ inputs.version }} → ${{ inputs.environment }}"
          fi

      - name: Notify (conditional)
        if: inputs.send-notification == true && inputs.dry-run == false
        run: echo "📢 Notification sent for ${{ inputs.environment }} deploy"
```

2. Push the file. Navigate to **Actions → Manual Deploy → Run workflow**.
3. Verify the form shows a **dropdown** for environment, **text fields** for version, and **checkboxes** for the boolean inputs.
4. Run with `dry-run: true` → confirm the simulation message appears.
5. Run with `environment: production` and an empty version → confirm the validate job fails with the error message.
6. Run with `environment: staging`, `version: 1.0.0`, `dry-run: false` → confirm deploy output appears.

**Success Criteria:**

- [ ] Input form renders with a dropdown, text box, and checkboxes.
- [ ] Production + empty version produces a non-zero exit and fails the `validate` job.
- [ ] `deploy` job skips when `validate` fails (due to `needs: validate`).
- [ ] Dry-run and non-dry-run produce different output messages.
- [ ] Notification step skips when `dry-run: true`.

**Troubleshooting:**

- **`inputs.environment` evaluates as empty inside a job** → Use `${{ inputs.environment }}` with the expression syntax; bare `inputs.environment` is not valid in `run:` scripts.
- **Boolean checkbox sends string `"true"/"false"`** → In shell scripts compare with `== "true"`, not `== true`. In `if:` expressions, compare with `inputs.dry-run == true` (without quotes).

**Time Estimate:** 20–25 minutes

**Variations/Extensions:**

- Add an `environment:` key to the `deploy` job to link it to a GitHub Environment for approval gates (see Section 8).
- Pass `inputs.version` to a reusable workflow via `workflow_call` (see Section 17).

**Cross-Section Connections:** → Section 6 (Trigger Events Part 2) for passing `workflow_dispatch` inputs to called workflows; → Section 8 (Environment Protection Rules) for adding required-reviewer gates on the `production` environment.

</details>

---

<details>
<summary>🔀 Scenario 5-C: Complex Trigger Combinations — Paths, Branches, Tags, and Ignore Filters</summary>


**Objective:** Combine multiple trigger filters (branches, paths, tags, ignore patterns) to create sophisticated routing workflows that run only under specific conditions.

**Prerequisites:** Scenario 5-B complete; understanding of YAML filter syntax.

**Context:** Real projects need nuanced triggering: run tests on all changes, but only deploy on tagged releases to main. Mastering filter combinations enables efficient, targeted automation.

**Detailed Steps:**

1. Create `.github/workflows/complex-triggers.yml`:

```yaml
name: Complex Trigger Combinations

on:
  push:
    # Run on main or version branches, but only if src/ or .github/ changed
    branches:
      - main
      - develop
      - 'release/v*'
    paths:
      - 'src/**'
      - '.github/workflows/**'
      - 'package.json'
    # But never run if only docs changed
    paths-ignore:
      - '*.md'
      - 'docs/**'

  pull_request:
    branches: [main, develop]
    paths:
      - 'src/**'
      - 'tests/**'
    paths-ignore:
      - 'docs/**'

  # Trigger on semantic version tags
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-rc*'

jobs:
  # Runs on all push/PR to src or .github
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm run lint

  # Runs only on version tags
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Create GitHub release
        run: echo "Release: ${{ github.ref }}"

  # Runs only on main + tag or version branch push
  deploy:
    if: |
      github.event_name == 'push' && (
        github.ref == 'refs/heads/main' ||
        startsWith(github.ref, 'refs/heads/release/') ||
        startsWith(github.ref, 'refs/tags/v')
      )
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying ${{ github.ref }}"
```

2. Test each trigger:
   - Push to `develop` with only docs changed → no run
   - Push to `develop` with src/ change → lint runs
   - Push to `release/v1.0` with src/ change → lint + deploy run
   - Create tag `v1.0.0` → all jobs run

**Success Criteria:**

- [ ] Push to non-target branch with no path changes = workflow skipped
- [ ] Push to target branch with relevant path = workflow runs
- [ ] Path-ignore correctly prevents runs
- [ ] Tag-based trigger fires only on matching tags
- [ ] Complex job conditionals combine branch + tag logic correctly

**Time Estimate:** 20–25 minutes

**Variations/Extensions:**

- Create a matrix of branches and tags, with different behavior for each
- Implement monorepo patterns: trigger different workflows based on which subdirectory changed

**Cross-Section Connections:** → Section 6 (Event Filters) for deeper dive into path matching; → Section 28 (Practice Assessment) for real-world multi-trigger scenarios.

</details>

---

<details>
<summary>🐛 Scenario 5-D: Troubleshooting Trigger Events — Why Workflows Don't Fire</summary>


**Objective:** Diagnose and fix workflows that don't trigger when expected using event inspection and testing.

**Prerequisites:** Scenario 5-C complete; access to GitHub workflow run history.

**Context:** Silent trigger failures frustrate teams. A workflow that never runs is harder to debug than one that fails. Learning systematic diagnosis prevents hours of wasted time.

**Detailed Steps:**

1. **Scenario: Push filter expects multiple branches**
   - Create `.github/workflows/trigger-debug.yml`:

     ```yaml
     name: Trigger Debug
     on:
       push:
         branches:
           - main
           - staging
         paths:
           - 'src/**'
     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - run: echo "Workflow triggered"
     ```

   - Push to `develop` branch with src/ change → workflow does NOT run (expected: main/staging only)
   - Push to `main` with only README.md change → workflow does NOT run (expected: paths filter)
   - Push to `main` with src/ change → workflow DOES run ✓

2. **Scenario: Event type mismatch**
   - Create `.github/workflows/pull-request-only.yml`:

     ```yaml
     name: PR Only
     on:
       pull_request:
         branches: [main]
         types: [opened, synchronize]
     jobs:
       pr-test:
         runs-on: ubuntu-latest
         steps:
           - run: echo "PR event only"
     ```

   - Comment on an existing PR → no run (event is `issue_comment`, not `pull_request`)
   - Push to main → no run (event is `push`, not `pull_request`)
   - Open new PR → runs ✓ (event type is `opened`)

3. **Scenario: Draft PR is skipped**
   - Create `.github/workflows/draft-debug.yml`:

     ```yaml
     name: Draft Check
     on:
       pull_request:
     jobs:
       check:
         if: ${{ !github.event.pull_request.draft }}
         runs-on: ubuntu-latest
         steps:
           - run: echo "Not a draft PR"
     ```

   - Open as draft → job skipped (if condition prevents it)
   - Convert to ready → runs ✓

4. **Scenario: Tag filter pattern doesn't match**
   - Create `.github/workflows/tag-debug.yml`:

     ```yaml
     name: Tag Debug
     on:
       push:
         tags:
           - 'v[0-9]+.[0-9]+.[0-9]+'  # Only matches semantic versions
     jobs:
       tag-job:
         runs-on: ubuntu-latest
         steps:
           - run: echo "Tag: ${{ github.ref }}"
     ```

   - Create tag `release-1.0` → no run (pattern doesn't match)
   - Create tag `v1.0.0` → runs ✓

5. **Investigate Using GitHub UI:**
   - Go to **Actions → [Workflow] → [Run]**
   - Check "Trigger details" (button at top) for reason workflow ran or skipped
   - Log shows: "Triggered via push", "Skipped due to path filter", etc.

**Success Criteria:**

- [ ] Can explain why each workflow did or didn't trigger
- [ ] Understand event types (push ≠ pull_request ≠ pull_request_target)
- [ ] Recognize path/branch/tag filter mismatches
- [ ] Use GitHub UI to inspect trigger reason
- [ ] Know when `draft: true` skips workflow due to conditions

**Troubleshooting:**

- **Trigger debug info not visible** → Go to **Actions → [Workflow] → Choose recent run → View trigger details**
- **Pattern matches locally but not GitHub** → Test regex with [regex101.com](https://regex101.com/)

**Time Estimate:** 25–30 minutes

**Variations/Extensions:**

- Create a diagnostic workflow that logs all triggering events and contexts
- Build a tool that predicts whether a workflow will trigger based on code changes

**Cross-Section Connections:** → Section 15 (Debugging) for reading GitHub Actions logs; → Section 3 (Conditionals) for job-level trigger guards.

</details>

---

<details>
<summary>🏢 Scenario 5-E: Enterprise Multi-Trigger Orchestration — Cross-Repository Event Coordination</summary>


**Objective:** Build sophisticated, organization-scale workflows that coordinate across multiple repositories using webhook events and API-driven triggers.

**Prerequisites:** Scenario 5-D complete; understanding of GitHub App webhooks.

**Context:** Large organizations have interdependent workflows: when Repo A builds, test suites in Repos B, C, and D should also run. This scenario teaches enterprise-scale automation patterns.

**Detailed Steps:**

1. **Create a Webhook-Triggered Workflow (Repo A):**
   - Create `.github/workflows/trigger-dependent-repos.yml`:

     ```yaml
     name: Trigger Dependent Tests
     on:
       push:
         branches: [main]
         paths:
           - 'src/**'
           - 'package.json'

     jobs:
       notify-downstream:
         runs-on: ubuntu-latest
         steps:
           - name: Trigger tests in Repo B
             run: |
               curl -X POST \
                 -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -H "Accept: application/vnd.github.v3+json" \
                 https://api.github.com/repos/YOUR_ORG/repo-b/dispatches \
                 -d '{"event_type":"upstream-build","client_payload":{"branch":"main","sha":"${{ github.sha }}"}}'

           - name: Trigger tests in Repo C
             run: |
               curl -X POST \
                 -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
                 -H "Accept: application/vnd.github.v3+json" \
                 https://api.github.com/repos/YOUR_ORG/repo-c/dispatches \
                 -d '{"event_type":"upstream-build"}'
     ```

2. **Create Listener Workflows (Repos B & C):**
   - In Repo B, create `.github/workflows/run-on-upstream.yml`:

     ```yaml
     name: Run Tests on Upstream Build
     on:
       repository_dispatch:
         types: [upstream-build]

     jobs:
       test:
         runs-on: ubuntu-latest
         steps:
           - name: Checkout
             uses: actions/checkout@v4

           - name: Run tests (against main from Repo A)
             run: |
               echo "Testing compatibility with Repo A commit: ${{ github.event.client_payload.sha }}"
               npm test
     ```

3. **Implement Status Tracking:**
   - Create `.github/workflows/track-status.yml`:

     ```yaml
     name: Track Build Status
     on:
       workflow_run:
         workflows: ['Run Tests on Upstream Build']
         types: [completed]

     jobs:
       report:
         runs-on: ubuntu-latest
         steps:
           - name: Report status
             run: |
               if [ "${{ github.event.workflow_run.conclusion }}" = "success" ]; then
                 echo "✅ Downstream tests passed"
               else
                 echo "❌ Downstream tests failed"
               fi
     ```

4. **Test the Coordination:**
   - Push to Repo A main → triggers curl POST to Repos B & C
   - Repos B & C receive `repository_dispatch` event → run tests
   - `workflow_run` workflow in each captures completion

**Success Criteria:**

- [ ] Push to Repo A triggers repository_dispatch events in Repos B & C
- [ ] Client_payload passes context between workflows
- [ ] Downstream workflows can access upstream commit SHA and branch
- [ ] Status tracking captures success/failure of downstream runs

**Troubleshooting:**

- **repository_dispatch not triggering** → Verify GITHUB_TOKEN has appropriate scopes and cross-repo permissions
- **Client payload not visible** → Use `${{ github.event.client_payload.KEY }}` syntax to access

**Time Estimate:** 30–40 minutes

**Variations/Extensions:**

- Implement a retry mechanism: if Repo B tests fail, automatically retry
- Create a dashboard that shows status of all dependent runs
- Integrate with Slack to notify on downstream test failures

**Cross-Section Connections:** → Section 16 (REST API) for advanced event coordination; → Section 22 (Enterprise Management) for organization-scale workflow policies.

</details>

---

### ⚡ Section 6: Workflow Trigger Events - Part 2 (2 hrs)

**Topics:** [05-Workflow-Trigger-Events.md](05-Workflow-Trigger-Events.md) (second half)
**Overview:**

- Advanced filtering: paths, branches, tags
- Webhook event patterns (push, pull request, release)
- Workflow_dispatch input types: string, choice, boolean, environment
- Default input values and required inputs
- Passing inputs to reusable workflows

**Practical Work:**

- Create workflows with branch and path filtering
- Implement sophisticated workflow_dispatch input validation
- Pass inputs from parent to reusable workflows
- Test trigger behavior with different event types


<details>
<summary>🏷️ Scenario 6-A: Advanced `paths-ignore` and Tag-Based Triggers</summary>


**Objective:** Combine `paths-ignore`, tag triggers, and `workflow_run` to build a layered trigger strategy that fires at the right times without redundant runs.

**Prerequisites:** Sections 1–5 complete; understanding of branches and tags in Git.

**Context:** Section 5 covered basic path filtering. Section 6 extends this with `paths-ignore` (the inverse approach), tag-based release triggers, and the `workflow_run` event that chains workflows together — a critical pattern for post-build deployment pipelines.

**Detailed Steps:**

1. Create `.github/workflows/release-pipeline.yml`:

```yaml
name: Release Pipeline

on:
  push:
    # Trigger on semantic version tags only
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'    # matches v1.0.0, v2.3.14
      - 'v[0-9]+.[0-9]+.[0-9]+-rc*' # matches v1.0.0-rc1

  # Trigger when the 'CI' workflow completes successfully on main
  workflow_run:
    workflows: ['CI']
    branches: [main]
    types: [completed]

jobs:
  release:
    runs-on: ubuntu-latest
    # Only proceed if triggered by tag OR if the upstream workflow succeeded
    if: |
      github.event_name == 'push' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    steps:
      - uses: actions/checkout@v4

      - name: Extract version from tag
        if: github.event_name == 'push'
        run: |
          TAG="${{ github.ref_name }}"
          echo "Releasing version: $TAG"
          echo "VERSION=$TAG" >> $GITHUB_ENV

      - name: Release triggered by workflow_run
        if: github.event_name == 'workflow_run'
        run: |
          echo "Triggered by workflow: ${{ github.event.workflow_run.name }}"
          echo "Source commit: ${{ github.event.workflow_run.head_sha }}"
```

2. Create a test workflow `.github/workflows/ci.yml` (referenced by `workflow_run`):

```yaml
name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'          # ignore all markdown files anywhere
      - 'docs/**'        # ignore entire docs directory
      - '.github/CODEOWNERS'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI tests passed"
```

3. Push a commit touching only `README.md` → verify CI workflow is **not** triggered.
4. Push a commit touching `src/main.js` → verify CI triggers, which then triggers the Release Pipeline.
5. Create and push a tag `v1.0.0` → verify Release Pipeline triggers directly from the tag.

**Success Criteria:**

- [ ] `paths-ignore: ['**.md']` prevents CI from running on markdown-only changes.
- [ ] `workflow_run` triggers Release Pipeline after CI succeeds on `main`.
- [ ] Tag push `v1.0.0` triggers Release Pipeline with `github.ref_name = v1.0.0`.
- [ ] `workflow_run` conditional correctly skips the release if CI failed.

**Troubleshooting:**

- **`workflow_run` not triggering** → The triggering workflow (`CI`) must exist on the default branch at the time the `workflow_run` listener is evaluated.
- **Tag pattern not matching** → Test regex patterns at [crontab.guru](https://crontab.guru) or GitHub's filter pattern documentation. Remember `*` in YAML glob patterns requires quoting if the line starts with `*`.

**Time Estimate:** 20–25 minutes

</details>

---

<details>
<summary>📤 Scenario 6-B: Pass `workflow_dispatch` Inputs to a Reusable Workflow</summary>


**Objective:** Create a caller workflow that accepts `workflow_dispatch` inputs and forwards them to a reusable `workflow_call` workflow without redefining each input twice.

**Prerequisites:** Section 5-B (workflow_dispatch inputs); Section 17 preview is helpful but not required.

**Context:** A common enterprise pattern is a top-level "deployment orchestrator" that collects user input via `workflow_dispatch` and delegates execution to standardized reusable workflows. Understanding how inputs flow from `workflow_dispatch` → caller → callee is a GH-200 exam topic.

**Detailed Steps:**

1. Create the reusable workflow `.github/workflows/deploy-reusable.yml`:

```yaml
name: Deploy Reusable

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string
      version:
        required: true
        type: string
      dry-run:
        required: false
        type: boolean
        default: true
    secrets:
      DEPLOY_TOKEN:
        required: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Execute deployment
        run: |
          echo "=== Reusable Workflow Received ==="
          echo "Environment : ${{ inputs.environment }}"
          echo "Version     : ${{ inputs.version }}"
          echo "Dry run     : ${{ inputs.dry-run }}"
          echo "Token set   : ${{ secrets.DEPLOY_TOKEN != '' }}"
```

2. Create the caller workflow `.github/workflows/deploy-orchestrator.yml`:

```yaml
name: Deploy Orchestrator

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment'
        required: true
        type: choice
        options: [development, staging, production]
      version:
        description: 'Version to deploy'
        required: true
        type: string
      dry-run:
        description: 'Dry run mode'
        type: boolean
        default: true

jobs:
  orchestrate:
    # Pass workflow_dispatch inputs directly to the reusable workflow
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      # Map dispatch inputs to reusable workflow inputs
      environment: ${{ inputs.environment }}
      version: ${{ inputs.version }}
      dry-run: ${{ inputs.dry-run }}
    secrets:
      DEPLOY_TOKEN: ${{ secrets.DEPLOY_TOKEN }}
```

3. Trigger the orchestrator via `workflow_dispatch` with `environment: staging`, `version: 1.2.3`.
4. Verify the reusable workflow job receives and prints all three values correctly.
5. Confirm the jobs appear under the `Deploy Orchestrator` workflow run, not as a separate run.

**Success Criteria:**

- [ ] Caller workflow `jobs` block uses `uses:` with a `.yml` path (not `run:`).
- [ ] `with:` block correctly maps `inputs.*` to the reusable workflow's declared inputs.
- [ ] Secret is forwarded with `secrets:` block (not `with:`).
- [ ] Reusable workflow job is shown as a nested job under the caller's run.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for full coverage of `workflow_call` including outputs and versioning; → Section 8 (Environments) for adding required reviewer gates to the `deploy` job inside the reusable workflow.

</details>

---

<details>
<summary>📨 Scenario 6-C: Advanced Repository Dispatch — Custom Event Types and Complex Payloads</summary>


**Objective:** Master `repository_dispatch` event type for triggering workflows programmatically with rich payloads, enabling CI orchestration from scripts or external systems.

**Prerequisites:** Scenario 6-B complete; familiarity with JSON payloads.

**Context:** `repository_dispatch` enables external systems (monitoring tools, deployment platforms, scripts) to trigger GitHub Actions workflows with custom data, bridging GitHub with enterprise tools.

**Detailed Steps:**

1. Create `.github/workflows/repository-dispatch-listener.yml`:

```yaml
name: Repository Dispatch Listener

on:
  repository_dispatch:
    types:
      - 'custom-build'
      - 'integration-test'
      - 'rollback'

jobs:
  handle-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Log event details
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Type: ${{ github.event.action }}"
          echo "Payload: ${{ toJSON(github.event.client_payload) }}"

      - name: Route by event type
        run: |
          case "${{ github.event.action }}" in
            custom-build)
              echo "Building: ${{ github.event.client_payload.build-target }}"
              ;;
            integration-test)
              echo "Testing: ${{ github.event.client_payload.test-suite }}"
              ;;
            rollback)
              echo "Rolling back: ${{ github.event.client_payload.previous-version }}"
              ;;
          esac
```

2. Trigger via curl with custom payload:

```bash
curl -X POST \
  -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
  -H "Accept: application/vnd.github.v3+json" \
  https://api.github.com/repos/YOUR_ORG/YOUR_REPO/dispatches \
  -d '{
    "event_type": "custom-build",
    "client_payload": {
      "build-target": "production",
      "rebuild-cache": false
    }
  }'
```

3. Test event routing and payload access.

**Success Criteria:**

- [ ] Repository dispatch events trigger workflow correctly
- [ ] Multiple event types are routed to correct handlers
- [ ] Client payload is accessible and properly parsed
- [ ] External curl request triggers workflow successfully

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 5 (Trigger Events Part 1) for basic trigger concepts; → Section 16 (REST API) for advanced API integration.

</details>

---

<details>
<summary>🐛 Scenario 6-D: Debugging Event Trigger Failures in Monorepo — Path-Based Workflow Selection</summary>


**Objective:** Diagnose and fix workflows in monorepo structures where path changes should trigger specific workflows but fail silently.

**Prerequisites:** Scenario 6-C complete; understanding of monorepo layouts.

**Context:** In monorepo projects with multiple services (frontend/, backend/, infra/), different services require different CI workflows. Incorrect path filters cause workflows to skip unexpectedly.

**Detailed Steps:**

1. Create monorepo structure:

```bash
/frontend
  /.github/workflows/frontend-tests.yml
/backend
  /.github/workflows/backend-tests.yml
/shared
  /components
```

2. Create `.github/workflows/frontend-tests.yml`:

```yaml
name: Frontend Tests
on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'shared/**'
      - '.github/workflows/frontend-tests.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test --prefix frontend
```

3. Create `.github/workflows/backend-tests.yml`:

```yaml
name: Backend Tests
on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'shared/**'
      - '.github/workflows/backend-tests.yml'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test --prefix backend
```

4. Test scenarios:
   - Push only backend/ changes → Backend Tests runs, Frontend Tests skipped ✓
   - Push only shared/ changes → Both run ✓
   - Push .md files only → Both skipped ✓

5. Debug failures using `git diff`:

```bash
git diff HEAD~1 --name-only | grep -E "^frontend/" && echo "Frontend trigger"
```

**Success Criteria:**

- [ ] Path filters correctly trigger only affected services
- [ ] Shared path changes trigger all affected workflows
- [ ] Non-code changes skip all workflows
- [ ] Can manually verify which files changed using git diff

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 5 (Trigger Events) for path filter syntax; → Section 28 (Practice Assessment) for real monorepo scenarios.

</details>

---

<details>
<summary>🏢 Scenario 6-E: Enterprise Event Coordination — Multi-Trigger Gating and Approval Chains</summary>


**Objective:** Build sophisticated workflows combining multiple trigger types with approvals and conditional routing for enterprise-safe deployments.

**Prerequisites:** Scenario 6-D complete; understanding of environment protection.

**Context:** Enterprises need multi-step gates: PR code review + approval + scheduled security scan before production deployment. This scenario chains triggers with manual gates.

**Detailed Steps:**

1. Create `.github/workflows/enterprise-deployment-gate.yml`:

```yaml
name: Enterprise Deployment Gate

on:
  workflow_dispatch:
    inputs:
      target_env:
        description: 'Deployment target'
        type: choice
        options: [staging, production]
      skip_tests:
        description: 'Skip test suite (requires approval)'
        type: boolean
        default: false
  schedule:
    - cron: '0 2 * * 1'  # Monday 2am - production health check
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'infra/**'

jobs:
  determine-action:
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ steps.logic.outputs.deploy }}
      target: ${{ steps.logic.outputs.target }}
    steps:
      - name: Routing logic
        id: logic
        run: |
          if [ "${{ github.event_name }}" = "schedule" ]; then
            # Scheduled: prod health check only
            echo "deploy=true" >> $GITHUB_OUTPUT
            echo "target=production" >> $GITHUB_OUTPUT
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # Manual dispatch: use input
            echo "deploy=true" >> $GITHUB_OUTPUT
            echo "target=${{ inputs.target_env }}" >> $GITHUB_OUTPUT
          else
            # Push: staging only (needs approval for prod)
            echo "deploy=true" >> $GITHUB_OUTPUT
            echo "target=staging" >> $GITHUB_OUTPUT
          fi

  test:
    needs: determine-action
    if: needs.determine-action.outputs.should-deploy == 'true' && !inputs.skip_tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test

  security-scan:
    needs: determine-action
    if: needs.determine-action.outputs.target == 'production'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Running security scan..."

  approval-gate:
    needs: [determine-action, test, security-scan]
    if: needs.determine-action.outputs.target == 'production' && github.event_name == 'push'
    runs-on: ubuntu-latest
    environment:
      name: prod-approval-required
    steps:
      - run: echo "⏳ Awaiting production approval..."

  deploy:
    needs: [determine-action, approval-gate]
    if: always() && needs.determine-action.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    environment:
      name: ${{ needs.determine-action.outputs.target }}
      url: https://${{ needs.determine-action.outputs.target }}.myapp.com
    steps:
      - name: Deploy to ${{ needs.determine-action.outputs.target }}
        run: echo "🚀 Deploying to ${{ needs.determine-action.outputs.target }}"

  notify:
    if: always()
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment status: ${{ needs.deploy.result }}"
```

2. Test each trigger path:
   - Manual dispatch to staging → skips approval
   - Manual dispatch to production → requires approval
   - Monday 2am → health check only
   - Push to main → stages to staging, waits for approval if promoting to prod

**Success Criteria:**

- [ ] Workflow routes correctly based on trigger type
- [ ] Scheduled runs skip approval gates
- [ ] Manual dispatch to production requires environment approval
- [ ] Test suite can be skipped (with trace audit)
- [ ] All paths eventually deploy to correct environment

**Time Estimate:** 30–40 minutes

**Cross-Section Connections:** → Section 8 (Environment Protection) for approval gate setup; → Section 3 (Conditional Logic) for complex if: expressions.

</details>

---

### ⚙️ Section 7: Environment & Runtime Configuration (2 hrs)

**Topics:** [06-Custom-Environment-Variables.md](06-Custom-Environment-Variables.md), [07-Default-Environment-Variables.md](07-Default-Environment-Variables.md)
**Overview:**

- Custom environment variables at workflow, job, and step levels
- Default environment variables (GITHUB_*, CI, RUNNER_*, etc.)
- Setting variables dynamically with GITHUB_ENV
- Variable scoping and precedence rules
- Secret-safe variable handling
- GitHub Actions context interpolation in env vars

**Practical Work:**

- Define custom environment variables at workflow scope
- Set runtime environment variables in steps
- Access default environment variables in scripts
- Implement environment-based configuration (dev/prod)
- Verify variable scope and precedence


<details>
<summary>⚙️ Scenario 7-A: Environment Variable Scope and Precedence</summary>


**Objective:** Demonstrate that step-level `env:` overrides job-level, which overrides workflow-level, and verify the exact precedence order in logs.

**Prerequisites:** Sections 1–4 complete; understanding of YAML structure.

**Context:** Environment variable scoping is a common source of confusion and a confirmed GH-200 exam topic. Variables at different levels serve different purposes: workflow-level sets defaults, job-level configures a specific job context, step-level enables targeted overrides without affecting sibling steps.

**Detailed Steps:**

1. Create `.github/workflows/env-precedence.yml`:

```yaml
name: Env Var Precedence

on: workflow_dispatch

# Workflow-level env: available to all jobs and steps
env:
  APP_ENV: workflow-level
  SHARED_VAR: from-workflow

jobs:
  precedence-demo:
    runs-on: ubuntu-latest

    # Job-level env: overrides workflow-level for this job
    env:
      APP_ENV: job-level  # overrides workflow-level APP_ENV

    steps:
      - name: Step 1 — inherits job-level APP_ENV
        run: |
          echo "APP_ENV    = $APP_ENV"       # expects: job-level
          echo "SHARED_VAR = $SHARED_VAR"    # expects: from-workflow

      - name: Step 2 — step-level override
        env:
          APP_ENV: step-level  # overrides job-level APP_ENV for this step only
        run: |
          echo "APP_ENV    = $APP_ENV"       # expects: step-level
          echo "SHARED_VAR = $SHARED_VAR"    # expects: from-workflow (unchanged)

      - name: Step 3 — back to job-level after step 2
        run: |
          echo "APP_ENV    = $APP_ENV"       # expects: job-level (step override gone)
```

2. Trigger via `workflow_dispatch` and verify each step's output matches expectations.
3. Add `APP_ENV` as a **Repository Variable** (Settings → Secrets and variables → Variables) and observe it does NOT override the YAML-defined env vars (YAML env wins over repository variables that are set via `vars.` context, not plain env vars).

**Success Criteria:**

- [ ] Step 1 prints `APP_ENV = job-level`.
- [ ] Step 2 prints `APP_ENV = step-level`.
- [ ] Step 3 prints `APP_ENV = job-level` (step override scoped to Step 2 only).
- [ ] `SHARED_VAR` is `from-workflow` in all three steps.

**Time Estimate:** 15–20 minutes

</details>

---

<details>
<summary>🔄 Scenario 7-B: Dynamic Variables with `GITHUB_ENV` and Default Environment Variables</summary>


**Objective:** Use `$GITHUB_ENV` to set environment variables dynamically in one step and consume them in subsequent steps, and catalog the most important default GitHub-provided environment variables.

**Prerequisites:** Section 7-A complete.

**Context:** `$GITHUB_ENV` is the runtime mechanism for passing computed values between steps without using step outputs. It is commonly used to set a version number, a computed path, or a build timestamp that later steps need. Default environment variables like `GITHUB_WORKSPACE`, `GITHUB_SHA`, and `CI` are always available and their values are exam-tested.

**Detailed Steps:**

1. Create `.github/workflows/github-env-demo.yml`:

```yaml
name: GITHUB_ENV Demo

on: workflow_dispatch

jobs:
  env-demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Step 1 — Compute and set dynamic variables
        run: |
          # Set multiple variables by appending to $GITHUB_ENV
          echo "BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_ENV
          echo "SHORT_SHA=${GITHUB_SHA:0:7}" >> $GITHUB_ENV
          echo "BUILD_TAG=build-${{ github.run_number }}" >> $GITHUB_ENV

      - name: Step 2 — Use variables set in Step 1
        run: |
          echo "Build date : $BUILD_DATE"
          echo "Short SHA  : $SHORT_SHA"
          echo "Build tag  : $BUILD_TAG"

      - name: Step 3 — Default GitHub-provided environment variables
        run: |
          echo "=== Runner Info ==="
          echo "CI              : $CI"
          echo "RUNNER_OS       : $RUNNER_OS"
          echo "RUNNER_ARCH     : $RUNNER_ARCH"
          echo "RUNNER_TEMP     : $RUNNER_TEMP"

          echo "=== Repository Info ==="
          echo "GITHUB_REPOSITORY      : $GITHUB_REPOSITORY"
          echo "GITHUB_SHA             : $GITHUB_SHA"
          echo "GITHUB_REF             : $GITHUB_REF"
          echo "GITHUB_REF_NAME        : $GITHUB_REF_NAME"
          echo "GITHUB_WORKSPACE       : $GITHUB_WORKSPACE"
          echo "GITHUB_RUN_ID          : $GITHUB_RUN_ID"
          echo "GITHUB_RUN_NUMBER      : $GITHUB_RUN_NUMBER"
          echo "GITHUB_ACTIONS         : $GITHUB_ACTIONS"
```

2. Trigger and verify Step 2 uses the values set in Step 1 (not empty).
3. Note that `GITHUB_ENV` values are **not** available in the same step that sets them — only in subsequent steps.
4. Verify `$CI` = `true` and `$GITHUB_ACTIONS` = `true` — useful for branching logic in scripts.

**Success Criteria:**

- [ ] `BUILD_DATE` in Step 2 shows a UTC ISO timestamp.
- [ ] `SHORT_SHA` is a 7-character hex string matching the first 7 chars of `GITHUB_SHA`.
- [ ] `RUNNER_OS` shows `Linux` on `ubuntu-latest` runners.
- [ ] `GITHUB_WORKSPACE` shows the path where the repository is checked out.

**Troubleshooting:**

- **`BUILD_DATE` is empty in Step 2** → Variable set in Step 1 via `>> $GITHUB_ENV` is only available in *subsequent* steps. If you read it in the same step, it will be empty.
- **`GITHUB_ENV` not found** → This is a file path, not an env var itself. Use `>> $GITHUB_ENV` (append to file); do not `export` the variable directly.

**Time Estimate:** 15–20 minutes

**Variations/Extensions:**

- Use multiline value syntax: `echo "MULTILINE_VAR<<EOF" >> $GITHUB_ENV && echo "line1" >> $GITHUB_ENV && echo "EOF" >> $GITHUB_ENV`
- Compare `GITHUB_ENV` (sets env vars for later steps) with `GITHUB_OUTPUT` (sets step outputs for other jobs).

**Cross-Section Connections:** → Section 8 (Environments) for environment-specific variables (not the same as `env:`); → Section 14 (Inter-Job Communication) for `GITHUB_OUTPUT` patterns.

</details>

---

<details>
<summary>🖥️ Scenario 7-C: Platform-Specific Paths and Cross-Platform Configuration</summary>


**Objective:** Handle environment variable differences across platforms (Linux, macOS, Windows) for truly portable workflows.

**Prerequisites:** Scenario 7-B complete; understanding of runner platforms.

**Context:** Professional workflows must run on multiple runners (ubuntu-latest, macos-latest, windows-latest). Path separators, shell differences, and environment variables differ per platform.

**Detailed Steps:**

1. Create `.github/workflows/cross-platform-env.yml`:

```yaml
name: Cross-Platform Environment

on: [push]

jobs:
  env-vars:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash  # bash on all platforms (even Windows)
    steps:
      - name: Print platform-specific vars
        run: |
          echo "OS: ${{ runner.os }}"
          echo "Path separator: $([ "${{ runner.os }}" = "Windows" ] && echo "\\" || echo "/")"
          echo "Shell: $SHELL"
          echo "Temp dir: $RUNNER_TEMP"

      - name: Set platform-specific paths
        run: |
          if [ "${{ runner.os }}" = "Windows" ]; then
            echo "ARTIFACT_PATH=$(pwd)\\dist" >> $GITHUB_ENV
          else
            echo "ARTIFACT_PATH=$(pwd)/dist" >> $GITHUB_ENV
          fi

      - name: Use platform-aware paths
        run: mkdir -p "$ARTIFACT_PATH"
```

2. Run on all platforms; verify paths are correct for each.

**Success Criteria:**

- [ ] Runner OS correctly detected for each platform
- [ ] Path separators handled correctly
- [ ] Shell defaults to bash on Windows
- [ ] Artifact paths are valid for each platform

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 20 (GitHub-Hosted Runners) for runner environment details; → Section 15 (Debugging) for cross-platform troubleshooting.

</details>

---

<details>
<summary>🔧 Scenario 7-D: Fix Environment Variable Pollution — Isolating Variables Between Jobs</summary>


**Objective:** Diagnose and fix scenarios where environment variables leak between jobs or persist unexpectedly.

**Prerequisites:** Scenario 7-C complete; understanding of job isolation.

**Context:** Environment variables are job-scoped but some values unintentionally leak via GITHUB_ENV or global settings, causing confusing failures.

**Detailed Steps:**

1. Create `.github/workflows/env-pollution-debug.yml`:

```yaml
name: Environment Pollution Debug

on: workflow_dispatch

env:
  GLOBAL_VAR: "global"

jobs:
  job1:
    env:
      JOB1_VAR: "from-job1"
    runs-on: ubuntu-latest
    steps:
      - name: Set via GITHUB_ENV
        run: echo "LEAKED_VAR=should-not-leak" >> $GITHUB_ENV

  job2:
    needs: job1
    runs-on: ubuntu-latest
    steps:
      - name: Try to access job1 var
        run: |
          echo "LEAKED_VAR: ${LEAKED_VAR:-NOT_FOUND}"
          echo "JOB1_VAR: ${JOB1_VAR:-NOT_FOUND}"
          echo "GLOBAL_VAR: $GLOBAL_VAR"
          # Job2 should see GLOBAL_VAR but NOT LEAKED_VAR or JOB1_VAR
```

2. Run and verify: job2 has access ONLY to GLOBAL_VAR.
3. Fix: Use job outputs instead of GITHUB_ENV for inter-job communication.

**Success Criteria:**

- [ ] Understand job-level isolation
- [ ] Know that GITHUB_ENV is job-scoped, not global
- [ ] Can use outputs for safe inter-job communication

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 14 (Inter-Job Communication) for outputs; → Section 2 (Contexts) for context scope rules.

</details>

---

<details>
<summary>🏢 Scenario 7-E: Enterprise Environment Configuration — Shared Org Settings and Team Defaults</summary>


**Objective:** Build reusable, organization-wide environment variable configurations deployed across all workflows.

**Prerequisites:** Scenario 7-D complete; understanding of reusable workflows.

**Context:** Large organizations want consistent environment variables (NODE_VERSION, CACHE_PREFIX, etc.) across all repos without copying. This scenario teaches centralized defaults.

**Detailed Steps:**

1. Create a reusable "env setup" workflow at `org/.github/workflows/reusable-env-setup.yml`:

```yaml
name: Reusable Env Setup

on:
  workflow_call:
    inputs:
      environment:
        type: string
        default: 'staging'

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      node-version: '20'
      npm-cache: 'npm-cache-${{ github.sha }}'
    steps:
      - name: Setup org defaults
        run: |
          echo "Org Defaults:"
          echo "  Node: 20"
          echo "  Cache: npm-cache-${{ github.sha }}"
          echo "  Env: ${{ inputs.environment }}"
```

2. Use in main workflows:

```yaml
name: Main Workflow

on: push

jobs:
  setup:
    uses: .org/.github/workflows/reusable-env-setup.yml
    with:
      environment: production

  build:
    needs: setup
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: ${{ needs.setup.outputs.node-version }}
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
```

**Success Criteria:**

- [ ] Reusable workflow defines org-standard env vars
- [ ] Outputs from reusable workflow are used in dependent jobs
- [ ] Environment-specific values switch based on input
- [ ] Changes to reusable workflow affect all consumers

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for workflow composition; → Section 22 (Enterprise Management) for org-wide policies.

</details>

---

### 🔐 Section 8: Environment Protection Rules & Secrets (2 hrs)

**Topics:** [08-Environment-Protection-Rules.md](08-Environment-Protection-Rules.md)
**Overview:**

- Environment concepts and deployment targets
- Environment-specific secrets and variables
- Protection rules: required reviewers, deployment branches
- Manual approval gates in workflows
- GITHUB_TOKEN permissions and scoping
- PAT (Personal Access Token) vs GITHUB_TOKEN lifecycle

**Practical Work:**

- Create environments with different configurations
- Set up environment-specific secrets
- Implement required reviewer protection rules
- Configure deployment branch restrictions
- Test approval workflows


<details>
<summary>🛡️ Scenario 8-A: Create Environments with Protection Rules and Secrets</summary>


**Objective:** Set up three GitHub Environments (`development`, `staging`, `production`) with escalating protection, environment-specific secrets, and branch deployment restrictions.

**Prerequisites:** Repository admin access; Sections 1–7 complete; a GitHub repository (personal or organization).

**Context:** GitHub Environments are named deployment targets that carry their own secrets, variables, and protection rules. They are referenced in workflow `jobs` via the `environment:` key. The `production` environment commonly requires manual approval before a deploy job runs — a gate that cannot be bypassed by pushing code.

**Detailed Steps:**

1. Navigate to **Repository → Settings → Environments → New environment**.
2. Create three environments with these settings:

   | Environment | Deployment branch rule | Required reviewers |
   |---|---|---|
   | `development` | Any branch | None |
   | `staging` | Selected branches: `main` | None |
   | `production` | Selected branches: `main`, `release/**` | Add yourself as reviewer |

3. For each environment, add an environment-specific secret:
   - `development`: `DATABASE_URL = postgres://dev-host:5432/devdb`
   - `staging`: `DATABASE_URL = postgres://stg-host:5432/stgdb`
   - `production`: `DATABASE_URL = postgres://prod-host:5432/proddb`

4. Create `.github/workflows/multi-env-deploy.yml`:

```yaml
name: Multi-Environment Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      target-env:
        description: 'Target environment'
        type: choice
        options: [development, staging, production]
        default: development

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    environment: development  # links job to the 'development' environment
    steps:
      - name: Deploy to development
        env:
          DB_URL: ${{ secrets.DATABASE_URL }}  # resolves from development environment
        run: |
          echo "Deploying to development"
          echo "DB URL length: ${#DB_URL}"  # print length, not value

  deploy-staging:
    runs-on: ubuntu-latest
    needs: deploy-dev
    environment: staging
    steps:
      - name: Deploy to staging
        env:
          DB_URL: ${{ secrets.DATABASE_URL }}  # resolves from staging environment
        run: echo "Deployed to staging — DB: ${#DB_URL} chars"

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    # Named environment with URL shown in the Actions UI
    environment:
      name: production
      url: https://myapp.example.com
    steps:
      - name: Deploy to production
        env:
          DB_URL: ${{ secrets.DATABASE_URL }}  # resolves from production environment
        run: echo "Deployed to production — DB: ${#DB_URL} chars"
```

5. Push to `main` and watch the workflow run.
6. When the `deploy-production` job appears, it should pause and show **"Waiting for review"**.
7. Click "Review pending deployments" → approve → verify the job runs.

**Success Criteria:**

- [ ] Each job uses a different `DATABASE_URL` secret from its respective environment.
- [ ] `deploy-production` pauses for reviewer approval before executing.
- [ ] After approval, the production job runs and shows the environment URL.
- [ ] Rejecting the review marks the job as "Rejected" and skips execution.

**Troubleshooting:**

- **Approval not appearing** → Verify the `production` environment has at least one required reviewer. The reviewer cannot be the same user who triggered the workflow (for org-level; personal repos allow self-review).
- **`DATABASE_URL` is empty** → Confirm the secret is added to the **environment** (not the repository). Environment secrets are separate from repository secrets.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🔑 Scenario 8-B: GITHUB_TOKEN Permissions and Scoping</summary>


**Objective:** Understand the default `GITHUB_TOKEN` permissions, restrict them using the `permissions:` key, and observe the difference between repo-level default and job-level explicit scoping.

**Prerequisites:** Scenario 8-A complete; understanding of GitHub API scopes.

**Context:** `GITHUB_TOKEN` is automatically provisioned for every workflow run and expires when the run completes. Its default permissions vary by repository settings (permissive vs restrictive default). For security, workflows should declare only the permissions they need — a requirement that some organizations enforce via policy.

**Detailed Steps:**

1. Create `.github/workflows/token-permissions.yml`:

```yaml
name: GITHUB_TOKEN Permissions

on: workflow_dispatch

# Restrict workflow-level permissions to read-only baseline
permissions:
  contents: read
  metadata: read

jobs:
  read-only:
    runs-on: ubuntu-latest
    # Job inherits workflow-level permissions unless overridden
    steps:
      - uses: actions/checkout@v4

      - name: Attempt to read repo (should succeed)
        run: |
          echo "SHA: ${{ github.sha }}"
          git log --oneline -3

  write-issues:
    runs-on: ubuntu-latest
    # Job-level permissions override workflow-level for this job only
    permissions:
      issues: write
      contents: read
    steps:
      - name: Create a test issue comment
        uses: actions/github-script@v7
        with:
          script: |
            // github.token automatically uses the job's scoped GITHUB_TOKEN
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: 1,  // assumes issue #1 exists
              body: `Test comment from workflow run #${context.runNumber}`
            })

  no-write-attempt:
    runs-on: ubuntu-latest
    permissions:
      contents: read  # intentionally no 'issues: write'
    steps:
      - name: This step cannot write issues
        run: |
          # Using curl to demonstrate the 403 you would get
          echo "Token permissions: contents=read only"
          echo "Any write operation to issues would return 403"
```

2. Trigger via `workflow_dispatch`.
3. Verify the `write-issues` job creates a comment on Issue #1.
4. Note that `no-write-attempt` acknowledges the limitation (for exam understanding).
5. Navigate to the run details and click the token information icon to see granted permissions.

**Success Criteria:**

- [ ] `write-issues` job successfully posts a comment (proving `issues: write` scope works).
- [ ] `read-only` job can read repo content but has no write capabilities.
- [ ] Workflow-level `permissions:` restricts both jobs unless overridden.

**Troubleshooting:**

- **Issue comment creation fails with 403** → Check that Issue #1 exists, or change the issue number. Also verify `issues: write` is in the `write-issues` job's `permissions:` block.
- **`permissions:` key not recognized** → Only available at workflow-level and job-level; not at step-level.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 23 (Security) for principle of least privilege applied to GITHUB_TOKEN; → Section 24 (Security Part 2) for OIDC as an alternative to long-lived tokens.

</details>

---

<details>
<summary>🔐 Scenario 8-C: OpenID Connect (OIDC) Token Generation and Short-Lived Credential Exchange</summary>


**Objective:** Replace long-lived personal access tokens with short-lived OIDC tokens for enhanced security in cloud provider authentication.

**Prerequisites:** Scenario 8-B complete; AWS/Azure account setup (or understanding of cloud provider OIDC).

**Context:** Long-lived credentials are security risks. OIDC enables GitHub to issue short-lived tokens that expire within 15 minutes, significantly reducing blast radius of credential compromise.

**Detailed Steps:**

1. Create `.github/workflows/oidc-aws-auth.yml` (AWS example):

```yaml
name: OIDC AWS Authentication

on: [push]

permissions:
  id-token: write  # Allow OIDC token generation

jobs:
  deploy-to-aws:
    runs-on: ubuntu-latest
    steps:
      - name: Generate OIDC token
        id: oidc
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::123456789012:role/GitHubActionsRole
          aws-region: us-east-1

      - name: List S3 buckets (via OIDC auth)
        run: aws s3 ls

      - name: View token expiry (check in browser CloudTrail)
        run: |
          echo "Token expires in ~15 minutes"
          echo "No long-lived credentials stored"
```

2. Configure AWS IAM role to trust GitHub:
   - Create trust policy accepting OIDC issuer: `token.actions.githubusercontent.com`
   - Scope to specific org/repo combinations

3. Compare token lifetime: OIDC (15 min) vs PAT (never expires).

**Success Criteria:**

- [ ] OIDC token successfully authenticates to AWS
- [ ] No PAT/credentials stored in GitHub secrets
- [ ] AWS CloudTrail shows temporary role assumption
- [ ] Token is automatically renewed per job if job runs longer

**Time Estimate:** 25–30 minutes

**Cross-Section Connections:** → Section 16 (REST API) for programmatic credential handling; → Section 23 (Security) for secure credential patterns.

</details>

---

<details>
<summary>🔍 Scenario 8-D: Audit and Recover from Excessive GITHUB_TOKEN Permissions</summary>


**Objective:** Identify workflows with over-permissioned tokens and remediate security risks through permission auditing.

**Prerequisites:** Scenario 8-C complete; exposure to GitHub security alerts.

**Context:** Workflows sometimes grant `contents: write` when only `contents: read` is needed. Over-permissioned tokens increase risk if compromised. Learning to audit and correct improves security posture.

**Detailed Steps:**

1. Create `.github/workflows/audit-permissions.yml` (intentionally over-permissioned):

```yaml
name: Audit Permissions

on: push

permissions:
  contents: write  # Should probably be 'read'
  issues: write    # Not used in this workflow
  pull-requests: write  # Overkill

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run linter (read-only operation)
        run: echo "Linting code..."
        # ^ This only needs 'contents: read', not 'write'

      - name: Print actual scope needed
        run: |
          echo "This workflow only reads repo content."
          echo "Scope should be: permissions: { contents: read }"
```

2. Audit using GitHub's built-in security scans:
   - Go to Repo → **Security → Secret scanning → Code scanning**
   - Look for warnings about over-permissioned workflows

3. Refactor workflow to least privilege:

```yaml
permissions:
  contents: read  # Minimal required
```

4. Verify GitHub Actions still work with reduced permissions.

**Success Criteria:**

- [ ] Identify unnecessary permissions in workflow
- [ ] Reduce permissions to minimum required
- [ ] Workflow still succeeds with reduced permissions
- [ ] GitHub security scanner no longer flags the workflow

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 23 (Security) for least-privilege principles; → Section 2 (Contexts) for available permission scopes.

</details>

---

<details>
<summary>🔄 Scenario 8-E: Organization-Wide Secret Rotation and Secure Cross-Repository Access</summary>


**Objective:** Implement secrets management across an organization: centralizing secret provisioning, enforcing rotation policies, and enabling safe cross-repo access.

**Prerequisites:** Scenario 8-D complete; organization admin access.

**Context:** Managing secrets at scale requires central control: versioning, rotation schedules, and audit trails. This scenario teaches enterprise-grade secrets architecture.

**Detailed Steps:**

1. **Create Organization Secret (Org Admin):**
   - Go to Org **Settings → Secrets and variables → Actions**
   - Create `ORG_DATABASE_URL = postgresql://prod.db.internal`
   - Set **Repository access** to "All repositories"

2. **Create Rotation Automation:**
   - Create `.github/workflows/rotate-org-secrets.yml`:

     ```yaml
     name: Rotate Org Secrets
     on:
       schedule:
         - cron: '0 0 1 * *'  # Monthly
     jobs:
       rotate:
         runs-on: ubuntu-latest
         steps:
           - name: Generate new secret
             id: gen
             run: |
               NEW_VALUE=$(openssl rand -base64 32 | tr -d "=+/" | cut -c1-25)
               echo "new_secret=$NEW_VALUE" >> $GITHUB_OUTPUT

           - name: Update via GitHub API
             env:
               GH_TOKEN: ${{ secrets.ROTATION_TOKEN }}
             run: |
               gh secret set ORG_DATABASE_URL -b "${NEW_VALUE}" --org YOUR_ORG
     ```

3. **Enable Across Organization:**
   - Repository-level workflows inherit org secret: `${{ secrets.ORG_DATABASE_URL }}`
   - All repos see rotated value automatically

4. **Audit Via API:**
   - Query secret access logs via GitHub GraphQL
   - Track which workflows accessed which secrets

**Success Criteria:**

- [ ] Organization secret is defined and available to member repos
- [ ] Monthly rotation workflow updates secret value
- [ ] All repos pick up new value without individual updates
- [ ] Rotation audit trail is maintained

**Time Estimate:** 30–40 minutes

**Cross-Section Connections:** → Section 2 (Contexts) for secret masking; → Section 22 (Enterprise Management) for org-wide policies.

</details>

---

## 🔄 Week 3: Workflow Features & Data Flow

### 🔀 Section 9: Jobs, Steps & Conditional Logic (2 hrs)

**Topics:** [04-Workflow-File-Structure.md](04-Workflow-File-Structure.md) (jobs section)
**Overview:**

- Job definition and identification
- Step structure: name, id, uses, run, with, env
- Sequential vs parallel job execution
- Job dependencies with `needs`
- Conditional execution: if expressions with success/failure functions
- Failure handling: continue-on-error, failure markers

**Practical Work:**

- Create multi-job workflows with dependencies
- Implement conditional steps using if expressions
- Use success(), failure(), and cancelled() functions
- Handle errors and continue execution strategically
- Test job ordering and failure scenarios


<details>
<summary>🔗 Scenario 9-A: Multi-Job Dependencies with Status Functions</summary>


**Objective:** Build a 4-job pipeline where jobs run in a specific order, one deliberately fails, and downstream jobs use `failure()`, `success()`, and `always()` to respond appropriately.

**Prerequisites:** Sections 1–8 complete; basic understanding of job dependencies from Sections 3 and 4.

**Context:** Real pipelines require more nuanced dependency logic than simple sequential chaining. A `notify` job should run even when `deploy` fails. A `cleanup` job should always run. Understanding when `success()`, `failure()`, `cancelled()`, and `always()` evaluate is a core GH-200 exam topic.

**Detailed Steps:**

1. Create `.github/workflows/job-conditions.yml`:

```yaml
name: Job Conditions Demo

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build application
        run: echo "Build complete — artifact ready"

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Run tests
        # Force a test failure to demonstrate downstream behavior
        run: |
          echo "Running tests..."
          exit 1  # simulate test failure

  deploy:
    runs-on: ubuntu-latest
    needs: test
    # Default: only runs if 'test' succeeded — will be SKIPPED after test fails
    steps:
      - run: echo "Deploying application"

  notify:
    runs-on: ubuntu-latest
    # Runs after both test and deploy, regardless of their outcomes
    needs: [test, deploy]
    if: always()  # always() returns true even when upstream jobs failed/cancelled
    steps:
      - name: Notify on failure
        if: failure()  # failure() is true if any upstream job failed
        run: echo "❌ Pipeline FAILED — would send failure alert"

      - name: Notify on success
        if: success()  # success() is true only if all upstream jobs succeeded
        run: echo "✅ Pipeline SUCCEEDED — would send success notification"

      - name: Always log status
        # always() at the step level runs even if prior steps in this job failed
        if: always()
        run: |
          echo "Pipeline status captured"
          echo "Upstream conclusion available via needs context"
```

2. Trigger the workflow and observe the run graph:
   - `build` → succeeds
   - `test` → fails (exit 1)
   - `deploy` → skipped (depends on failed `test`)
   - `notify` → runs (due to `if: always()`)
3. In `notify`, verify the "Notify on failure" step runs and "Notify on success" is skipped.
4. Edit the workflow: remove `exit 1` from test, re-run, and verify all jobs succeed including the success notification.

**Success Criteria:**

- [ ] `deploy` shows as **Skipped** (not Failed) when `test` fails.
- [ ] `notify` shows as **Succeeded** despite upstream failures (due to `always()`).
- [ ] "Notify on failure" step runs when test failed; "Notify on success" is skipped.
- [ ] After fixing the test: all jobs succeed and "Notify on success" runs.

**Troubleshooting:**

- **`notify` job is skipped instead of running** → The default `if:` condition is `success()`. Without `if: always()` on the job, it skips when any `needs` job failed. The `always()` must be on the **job**, not just a step.
- **`failure()` in step returns wrong value** → `failure()` at step level checks if any *previous step in the same job* failed, not upstream jobs. Use `needs.test.result == 'failure'` for upstream job status.

**Time Estimate:** 20–25 minutes

</details>

---

<details>
<summary>⚠️ Scenario 9-B: `continue-on-error` and Step-Level Error Handling</summary>


**Objective:** Use `continue-on-error` to allow flaky steps to fail without failing the job, and use `outcome` step context to branch on step results.

**Prerequisites:** Scenario 9-A complete.

**Context:** Some steps are inherently unreliable (external API calls, network checks) but should not block the workflow. `continue-on-error: true` allows the workflow to continue, and the `steps.<id>.outcome` and `steps.<id>.conclusion` values let subsequent steps react to what happened.

**Detailed Steps:**

1. Create `.github/workflows/continue-on-error.yml`:

```yaml
name: Continue on Error Demo

on: workflow_dispatch

jobs:
  resilient-job:
    runs-on: ubuntu-latest
    steps:
      - name: Flaky external check
        id: flaky-check
        continue-on-error: true  # job continues even if this step fails
        run: |
          echo "Checking external service..."
          exit 1  # simulate flaky failure

      - name: React to flaky check result
        run: |
          echo "Flaky check outcome    : ${{ steps.flaky-check.outcome }}"
          echo "Flaky check conclusion : ${{ steps.flaky-check.conclusion }}"
          # outcome: success/failure/skipped/cancelled (the raw result)
          # conclusion: success/failure/skipped/cancelled (same unless continue-on-error overrides)

      - name: Only runs if flaky check passed
        if: steps.flaky-check.outcome == 'success'
        run: echo "External check OK — proceeding with dependent step"

      - name: Fallback when flaky check failed
        if: steps.flaky-check.outcome == 'failure'
        run: echo "External check unavailable — using fallback mode"

      - name: Final step — always runs, job is still green
        run: echo "Job completes successfully despite the failed flaky step"
```

2. Trigger and verify the job shows **green** even though a step exited with `exit 1`.
3. Observe the difference between `outcome` (raw) and `conclusion` (effective): with `continue-on-error: true`, `conclusion` is `success` but `outcome` is `failure`.
4. Change `continue-on-error: false` (or remove it) and verify the job now fails.

**Success Criteria:**

- [ ] Job shows **green** (success) when `continue-on-error: true` and the step exits 1.
- [ ] `outcome` = `failure` and `conclusion` = `success` with `continue-on-error: true`.
- [ ] "Fallback" step runs; "Only runs if passed" step is skipped.
- [ ] After removing `continue-on-error`, the job turns **red**.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for using `outcome` context to build diagnostic workflows; → Section 26 (Troubleshooting) for matrix job failure scenarios.

</details>

---

<details>
<summary>📊 Scenario 9-C: Advanced Job Dependencies with Outputs and Complex Routing</summary>


**Objective:** Build multi-job workflows with sophisticated dependency chains, passing data between jobs, and conditional routing based on outputs.

**Prerequisites:** Scenario 9-B complete; understanding of job outputs.

**Context:** Enterprise workflows often require: job A produces output → job B uses that output → job C runs only if B succeeds. Mastering these patterns enables resilient automation.

**Detailed Steps:**

1. Create `.github/workflows/advanced-job-routing.yml`:

```yaml
name: Advanced Job Routing
on: [push]
jobs:
  analyze:
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ steps.check.outputs.deploy }}
      version: ${{ steps.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - id: check
        run: echo "deploy=true" >> $GITHUB_OUTPUT
      - id: version
        run: echo "version=1.0.0" >> $GITHUB_OUTPUT

  build:
    needs: analyze
    runs-on: ubuntu-latest
    outputs:
      artifact-hash: abc123
    steps:
      - run: echo "Building v${{ needs.analyze.outputs.version }}"

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing artifact ${{ needs.build.outputs.artifact-hash }}"

  deploy:
    if: needs.analyze.outputs.should-deploy == 'true'
    needs: [analyze, test]
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying v${{ needs.analyze.outputs.version }}"
```

2. Test: modify `should-deploy` output to `false` → deploy job skips.
3. Verify outputs pass through the dependency chain.

**Success Criteria:**

- [ ] Job outputs available to dependent jobs via `needs.JOBID.outputs.KEY`
- [ ] Conditional job-level `if:` works with outputs
- [ ] Chain of 3+ jobs with multiple outputs passes data correctly

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 14 (Outputs) for inter-job communication; → Section 3 (Conditionals) for complex if expressions.

</details>

---

<details>
<summary>⏱️ Scenario 9-D: Timeout and Cancellation Handling</summary>


**Objective:** Diagnose and recover from job timeouts and workflow cancellations.

**Prerequisites:** Scenario 9-C complete.

**Context:** Long-running jobs timeout (default 6 hours). Learning to handle graceful shutdown and detect cancellation prevents data loss.

**Detailed Steps:**

1. Create workflow with `timeout-minutes`:

```yaml
name: Timeout Demo
on: workflow_dispatch
jobs:
  long-job:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Run slow task
        run: sleep 90  # Exceeds 1 min timeout
      - name: Never runs (job cancelled)
        run: echo "This is skipped"
```

2. Manually cancel workflow mid-run; observe `cancelled` status.
3. Add cleanup step that always runs:

```yaml
      - if: always()
        run: echo "Cleanup: timeout occurred at $(date)"
```

**Success Criteria:**

- [ ] Job shows `timed out` in UI when `timeout-minutes` exceeded
- [ ] `if: always()` cleanup step runs even on timeout/cancellation
- [ ] Subsequent jobs are skipped when upstream times out

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for investigating timeouts; → Section 26 (Troubleshooting) for timeout recovery strategies.

</details>

---

<details>
<summary>🔄 Scenario 9-E: Self-Healing Orchestration with Automatic Retry and Fallback Logic</summary>


**Objective:** Build resilient workflows that automatically retry failed jobs and activate fallback paths.

**Prerequisites:** Scenario 9-D complete.

**Context:** Production workflows must handle transient failures. Implementing retry and fallback logic prevents false negatives and improves reliability.

**Detailed Steps:**

1. Create `.github/workflows/self-healing.yml`:

```yaml
name: Self-Healing Orchestration
on: [push]
jobs:
  flaky-operation:
    runs-on: ubuntu-latest
    steps:
      - name: Attempt (may fail randomly)
        run: |
          if [ $((RANDOM % 2)) -eq 0 ]; then
            exit 1
          fi
          echo "Success!"

  retry-handler:
    needs: flaky-operation
    if: failure()  # Runs if flaky-operation failed
    runs-on: ubuntu-latest
    steps:
      - name: Log failure and retry
        run: echo "Retry via matrix or rerun button"

  fallback-path:
    needs: flaky-operation
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Use cached result
        run: echo "Using fallback cached artifact"

  notify:
    if: always()
    needs: [flaky-operation, fallback-path]
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Job status: ${{ needs.flaky-operation.result }}"
          echo "Fallback executed: ${{ needs.fallback-path.result != 'skipped' }}"
```

2. Observe: flaky job fails → fallback auto-activates.
3. On success: fallback skipped.

**Success Criteria:**

- [ ] Fallback job activates on primary job failure
- [ ] Fallback is skipped if primary succeeds
- [ ] Notification always runs and reports status

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 26 (Troubleshooting) for failure recovery; → Section 25 (Performance) for timeout tuning.

</details>

---

### 🔢 Section 10: Job Strategy & Matrix (2 hrs)

**Topics:** [04-Workflow-File-Structure.md](04-Workflow-File-Structure.md) (matrix section)
**Overview:**

- Strategy matrix for generating job variations
- Multi-axis matrix (OS, Python version, Node version combinations)
- Include/exclude matrix configurations
- Fail-fast behavior and max-parallel settings
- Matrix context access and interpolation
- Cost optimization with matrix configuration

**Practical Work:**

- Create multi-OS test matrices (Ubuntu, Windows, macOS)
- Build version compatibility matrices (Node, Python, Java)
- Use include/exclude for special variations
- Optimize matrix for cost and speed
- Access and use matrix context values in steps


<details>
<summary>🔢 Scenario 10-A: Multi-Dimensional Matrix with `include` and `exclude`</summary>


**Objective:** Build a test matrix across operating systems and Node.js versions, exclude an unsupported combination, and add an `include` entry that injects extra variables for a specific combination.

**Prerequisites:** Sections 1–9 complete; Node.js conceptual understanding helpful but not required.

**Context:** The `strategy.matrix` key generates multiple parallel job instances from combinations of values. Multi-dimensional matrices grow quickly (3 OSes × 4 versions = 12 jobs), so `exclude` and `fail-fast` are essential for cost and correctness. The `include` key adds extra context variables to specific matrix cells without adding new combinations.

**Detailed Steps:**

1. Create `.github/workflows/matrix-demo.yml`:

```yaml
name: Matrix Demo

on: workflow_dispatch

jobs:
  test:
    runs-on: ${{ matrix.os }}

    strategy:
      # When true (default), all jobs cancel if any one fails
      fail-fast: false
      # Maximum parallel jobs (reduce to control costs)
      max-parallel: 4

      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node-version: ['18', '20', '22']

        # Exclude a specific combination
        exclude:
          - os: macos-latest
            node-version: '18'  # Node 18 not tested on macOS (hypothetical constraint)

        # Add extra data to a specific combination
        include:
          - os: ubuntu-latest
            node-version: '20'
            # Extra variable only available in this specific cell
            run-integration-tests: true
            label: 'primary'

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Print matrix context
        run: |
          echo "OS              : ${{ matrix.os }}"
          echo "Node version    : ${{ matrix.node-version }}"
          echo "Run integration : ${{ matrix.run-integration-tests }}"
          echo "Label           : ${{ matrix.label }}"

      - name: Run integration tests (only on labeled primary cell)
        if: matrix.run-integration-tests == true
        run: echo "Running integration tests on ${{ matrix.os }} / Node ${{ matrix.node-version }}"

      - name: Verify Node version
        run: node --version
```

2. Trigger via `workflow_dispatch` and count the jobs: should be 8 (3×3=9 minus 1 excluded macOS/Node18).
3. Verify only the `ubuntu-latest`/`node-20` job runs the integration test step.
4. Verify `matrix.run-integration-tests` and `matrix.label` are empty in all other cells.

**Success Criteria:**

- [ ] Total jobs = 8 (9 combinations minus 1 excluded).
- [ ] macOS + Node 18 combination does not appear in the run.
- [ ] Only the `ubuntu-latest`/`node-20` job runs the integration test step.
- [ ] `fail-fast: false` means other jobs continue when one fails.

**Troubleshooting:**

- **`matrix.run-integration-tests` is empty in most cells** → This is correct and expected; `include` extra keys are only available in the specific cell they are added to.
- **Jobs exceed `max-parallel`** → GitHub queues jobs above the limit; they run when a slot becomes available.
- **`exclude` not working** → The `exclude` object must match the exact combination (all keys must match). A partial match does not exclude.

**Time Estimate:** 20–25 minutes

</details>

---

<details>
<summary>⚡ Scenario 10-B: Fail-Fast Behavior and Matrix Output Aggregation</summary>


**Objective:** Observe `fail-fast` behavior, understand when to disable it, and aggregate outputs from multiple matrix jobs into a single downstream job.

**Prerequisites:** Scenario 10-A complete.

**Context:** `fail-fast: true` (the default) cancels all running and pending matrix jobs when any one fails. This is useful for expensive jobs but harmful when you want to see all failure results (e.g., cross-platform test results). Matrix outputs require the `fromJSON` function to aggregate across parallel jobs.

**Detailed Steps:**

1. Create `.github/workflows/matrix-failfast.yml`:

```yaml
name: Matrix Fail-Fast and Outputs

on: workflow_dispatch

jobs:
  build-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false  # collect all results even if some fail
      matrix:
        service: [api, frontend, worker]
    outputs:
      # Capture a matrix output — only last write wins; use artifact for full aggregation
      api-status: ${{ steps.status.outputs.result }}
    steps:
      - name: Simulate build
        id: status
        run: |
          echo "Building ${{ matrix.service }}..."
          if [ "${{ matrix.service }}" == "worker" ]; then
            echo "result=failed" >> $GITHUB_OUTPUT
            exit 1  # worker build fails intentionally
          fi
          echo "result=success" >> $GITHUB_OUTPUT

  report:
    runs-on: ubuntu-latest
    needs: build-matrix
    # Run even if some matrix jobs failed
    if: always()
    steps:
      - name: Report matrix results
        run: |
          echo "Build matrix conclusion: ${{ needs.build-matrix.result }}"
          echo "Note: needs.<job>.result is the aggregate across all matrix cells"
          echo "Possible values: success, failure, cancelled, skipped"

          if [ "${{ needs.build-matrix.result }}" == "failure" ]; then
            echo "❌ One or more builds failed"
          else
            echo "✅ All builds passed"
          fi
```

2. Trigger and observe: `api` and `frontend` succeed, `worker` fails.
3. With `fail-fast: false`, the `api` and `frontend` jobs should continue to completion.
4. `report` job runs due to `if: always()` and correctly reports `failure`.
5. Change `fail-fast: true`, re-run — observe `api` or `frontend` may be cancelled mid-run.

**Success Criteria:**

- [ ] With `fail-fast: false`: all 3 jobs run to their natural conclusion.
- [ ] With `fail-fast: true`: surviving jobs are cancelled when `worker` fails.
- [ ] `needs.build-matrix.result` = `failure` when any cell fails.
- [ ] `report` job executes due to `if: always()` regardless of matrix outcome.

**Troubleshooting:**

- **`fail-fast: true` doesn't cancel other jobs quickly** → Cancellation is not instantaneous; jobs that are already running will complete their current step first.
- **`needs.build-matrix.result` is `'success'` despite a failure** → Verify `fail-fast: false`; with `fail-fast: true`, the cancelled jobs may cause the aggregate to report differently depending on timing.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 14 (Inter-Job Communication) for artifact-based aggregation of matrix results; → Section 26 (Troubleshooting) for diagnosing matrix job failures in complex pipelines.

</details>

---

<details>
<summary>🧮 Scenario 10-C: Dynamic Matrix Generation and Large Matrix Optimization</summary>


**Objective:** Generate matrix values programmatically and optimize large matrices for performance and cost.

**Prerequisites:** Scenario 10-B complete; understanding of matrix expansion.

**Context:** Static matrices can explode: 5 versions × 3 OS × 4 test-suites = 60 jobs. Learning dynamic generation and optimization prevents runaway costs.

**Detailed Steps:**

1. Create `.github/workflows/dynamic-matrix.yml`:

```yaml
jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set.outputs.matrix }}
    steps:
      - id: set
        run: |
          MATRIX=$(echo '{"node": ["18", "20"], "os": ["ubuntu-latest", "windows-latest"]}' | jq -c .)
          echo "matrix=$MATRIX" >> $GITHUB_OUTPUT

  test:
    needs: setup
    strategy:
      matrix: ${{ fromJSON(needs.setup.outputs.matrix) }}
    runs-on: ${{ matrix.os }}
    steps:
      - run: echo "Testing Node ${{ matrix.node }}"
```

2. Matrix expands to 4 jobs (2 versions × 2 OS).
3. Add `exclude` to remove unwanted combinations.

**Success Criteria:**

- [ ] Matrix generated dynamically from JSON output
- [ ] Job count matches expansion (4 jobs)
- [ ] Exclude filters work to reduce job count

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 25 (Performance) for cost optimization; → Section 28 (Practice) for real-world matrix scenarios.

</details>

---

<details>
<summary>🐛 Scenario 10-D: Fix Matrix Expansion Bugs and False Failures</summary>


**Objective:** Diagnose and fix bugs where matrix jobs fail unexpectedly or expand incorrectly.

**Prerequisites:** Scenario 10-C complete.

**Context:** Matrix bugs cause confusing failures: cross-platform issues, dependency conflicts, or unexpected expansion.

**Detailed Steps:**

1. Scenario: platform-specific failure

```yaml
jobs:
  test:
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - run: cat file.txt | grep "pattern"  # Fails on Windows (different line endings)
```

2. Fix: use cross-platform command or shell setting.
3. Verify all matrix jobs pass.

**Success Criteria:**

- [ ] Identify platform-specific failure
- [ ] Fix with platform-aware approach
- [ ] All matrix jobs pass on all platforms

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 26 (Troubleshooting) for platform-specific issues; → Section 7 (Environment) for platform detection.

</details>

---

<details>
<summary>🌐 Scenario 10-E: Distributed Testing with Matrix Across Teams</summary>


**Objective:** Scale testing across distributed teams using multi-dimensional matrix.

**Prerequisites:** Scenario 10-D complete.

**Context:** Large organizations coordinate testing: Team A tests frontend, Team B tests backend, all reporting unified results.

**Detailed Steps:**

1. Create matrix with multiple dimensions:

```yaml
strategy:
  matrix:
    team: [frontend, backend, infra]
    test-type: [unit, integration]
```

2. This creates 6 jobs (3 teams × 2 types).
3. Upload results per job.
4. Create aggregation job to merge results.

**Success Criteria:**

- [ ] Matrix expands to 6 jobs correctly
- [ ] Each job uploads separate artifacts
- [ ] Aggregation job collects all results
- [ ] Dashboard shows per-team pass rates

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 12 (Artifacts) for result management; → Section 27 (Job Summaries) for reporting.

</details>

---

### 🐳 Section 11: Service Containers & Dependencies (2 hrs)

**Topics:** [04-Workflow-File-Structure.md](04-Workflow-File-Structure.md) (services section)
**Overview:**

- Service containers for testing (databases, Redis, etc.)
- Container configuration: image, ports, options, volumes
- Port mapping and network connectivity
- Health checks and startup conditions
- Environment variables within containers
- Service container networking best practices

**Practical Work:**

- Set up PostgreSQL service container for integration tests
- Configure Redis service container
- Implement health checks for service readiness
- Test application code against service containers
- Debug service container networking issues


<details>
<summary>🐘 Scenario 11-A: PostgreSQL Service Container for Integration Tests</summary>


**Objective:** Spin up a PostgreSQL service container within a workflow job and run database integration tests against it.

**Prerequisites:** Sections 1–10 complete; Docker conceptual understanding helpful; PostgreSQL `psql` client familiarity useful.

**Context:** Service containers run as Docker containers alongside the job runner. They are available on the job's Docker network and are accessible using `localhost` (or `127.0.0.1`) on the mapped port. Health checks prevent the job steps from running before the service is ready.

**Detailed Steps:**

1. Create `.github/workflows/service-containers.yml`:

```yaml
name: Service Container Demo

on: [push, workflow_dispatch]

jobs:
  integration-tests:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpassword
          POSTGRES_DB: testdb
        ports:
          # Maps host port 5432 to container port 5432
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Wait for services (guaranteed by health checks above)
        run: |
          echo "Services are ready — health checks passed before steps execute"

      - name: Test PostgreSQL connectivity
        env:
          PGPASSWORD: testpassword
        run: |
          # psql is preinstalled on ubuntu-latest runners
          psql -h localhost -U testuser -d testdb -c "SELECT version();"

      - name: Create a test table and insert data
        env:
          PGPASSWORD: testpassword
        run: |
          psql -h localhost -U testuser -d testdb -c "
            CREATE TABLE users (id SERIAL PRIMARY KEY, name TEXT NOT NULL);
            INSERT INTO users (name) VALUES ('Alice'), ('Bob');
            SELECT COUNT(*) FROM users;
          "

      - name: Test Redis connectivity
        run: |
          redis-cli -h localhost -p 6379 SET testkey "hello"
          redis-cli -h localhost -p 6379 GET testkey

      - name: Run application integration test (simulated)
        env:
          DATABASE_URL: postgres://testuser:testpassword@localhost:5432/testdb
          REDIS_URL: redis://localhost:6379
        run: |
          echo "App would connect to: ${DATABASE_URL}"
          echo "Cache would connect to: ${REDIS_URL}"
          echo "All integration tests passed"
```

2. Push to trigger the workflow (or use `workflow_dispatch`).
3. Verify the `postgres` and `redis` service containers appear in the run log under "Initialize containers".
4. Confirm the health check steps pass before job steps execute.
5. Verify `SELECT COUNT(*) FROM users;` returns `2`.

**Success Criteria:**

- [ ] Both service containers start and pass health checks before steps run.
- [ ] PostgreSQL is reachable at `localhost:5432` from job steps.
- [ ] Redis responds to `PING` and can store/retrieve values.
- [ ] Integration test step uses `DATABASE_URL` and `REDIS_URL` environment variables.

**Troubleshooting:**

- **`psql: error: connection refused`** → Health check ensures the container is ready, but `psql` must be installed on the runner. Ubuntu-latest includes it; Windows runners do not. Add `apt-get install -y postgresql-client` if using a minimal runner image.
- **Service container port conflict** → Use `0:5432` (random host port) to avoid conflicts, then read the port from `job.services.postgres.ports['5432']`. For simplicity, stick to explicit ports in testing environments.
- **Health check times out** → Increase `--health-retries` or `--health-interval` for slow-starting images.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🐳 Scenario 11-B: Container Jobs — Running the Entire Job Inside a Container</summary>


**Objective:** Use `container:` at the job level to run all steps inside a Docker container, and understand how this differs from service containers.

**Prerequisites:** Scenario 11-A complete.

**Context:** The job-level `container:` key runs the entire job inside a specified Docker image. Steps execute inside that container rather than on the runner's OS. This is ideal for guaranteeing a consistent runtime environment without relying on pre-installed runner tools.

**Detailed Steps:**

1. Create `.github/workflows/container-job.yml`:

```yaml
name: Container Job Demo

on: workflow_dispatch

jobs:
  build-in-container:
    # All steps run inside this container image
    runs-on: ubuntu-latest
    container:
      image: node:20-alpine
      # Optionally set environment variables for the container
      env:
        NODE_ENV: test
      # Volume mounts if needed
      # volumes:
      #   - /tmp/cache:/cache

    services:
      # Service containers can still be used alongside container jobs
      redis:
        image: redis:7-alpine
        options: --health-cmd "redis-cli ping" --health-interval 10s --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - name: Verify we are running inside the container
        run: |
          echo "Node version: $(node --version)"
          echo "npm version: $(npm --version)"
          # Alpine Linux — not the runner's Ubuntu
          cat /etc/os-release | head -3

      - name: Install dependencies
        run: npm install --if-present

      - name: Test Redis from container job
        run: |
          # Inside a container job, service containers are accessed by their service name, not localhost
          # The service name 'redis' becomes the hostname
          apk add --no-cache redis
          redis-cli -h redis -p 6379 PING

      - name: Run tests
        run: |
          echo "Tests would run here inside Node 20 Alpine"
          node -e "console.log('Hello from container job!')"
```

2. Trigger and observe that all steps execute inside `node:20-alpine`.
3. Note the key difference: service containers in container jobs are accessed via their **service name as hostname** (e.g., `redis`), not `localhost`.
4. Compare this to Scenario 11-A where service containers used `localhost`.

**Success Criteria:**

- [ ] Steps show Alpine Linux in the OS release (not Ubuntu).
- [ ] Node 20 is available without a `setup-node` step.
- [ ] Redis is accessed via `redis` hostname (service name), not `localhost`.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 20 (GitHub-Hosted Runners) for understanding the runner environment; → Section 21 (Self-Hosted Runners) for using container jobs with self-hosted runners where Docker must be available.

</details>

---

<details>
<summary>🌐 Scenario 11-C: Multi-Container Networking and Advanced Health Checks</summary>


**Objective:** Set up complex multi-container environments with inter-container networking.

**Prerequisites:** Scenario 11-B complete.

**Context:** Real workflows need multiple interdependent services (DB, cache, API) coordinating startup.

**Detailed Steps:**

1. Create workflow with postgres, redis, and API services with health checks
2. Configure health checks for each service
3. Test inter-service communication

**Success Criteria:**

- [ ] All services start and pass health checks
- [ ] Services communicate by service name
- [ ] Health check timeout works correctly

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 25 (Performance) for optimization.

</details>

---

<details>
<summary>🐛 Scenario 11-D: Debug Container Startup Failures and Connection Timeouts</summary>


**Objective:** Troubleshoot why containers fail or services can't connect.

**Prerequisites:** Scenario 11-C complete.

**Context:** Common failures: image pull errors, port conflicts, connection refused.

**Detailed Steps:**

1. Add debugging to identify service startup issues
2. Verify port binding with nc/curl
3. Test DNS resolution within services
4. Confirm connectivity before steps execute

**Success Criteria:**

- [ ] Identify startup failure
- [ ] Fix with config change
- [ ] Services connect successfully

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for troubleshooting patterns.

</details>

---

<details>
<summary>☸️ Scenario 11-E: Kubernetes-Like Container Orchestration Patterns</summary>


**Objective:** Implement container orchestration patterns resembling Kubernetes within GitHub Actions.

**Prerequisites:** Scenario 11-D complete.

**Context:** Production workflows need liveness probes, readiness checks, graceful shutdown.

**Detailed Steps:**

1. Implement readiness pattern: service reports ready via endpoint
2. Add graceful shutdown on workflow cancellation
3. Implement retry logic for transient failures

**Success Criteria:**

- [ ] Services coordinate startup/shutdown
- [ ] Readiness endpoints work
- [ ] Graceful termination on cancellation

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 20 (Runners) for infrastructure patterns.

</details>

---

### 📦 Section 12: Workflow Artifacts - Storage & Retrieval (2 hrs)

**Topics:** [09-Workflow-Artifacts.md](09-Workflow-Artifacts.md)
**Overview:**

- Artifact concepts: upload, storage, retention
- Upload-artifact action: path patterns, name, retention-days
- Download-artifact action: name, path patterns
- Artifact download from UI and REST API
- Cross-job artifact sharing
- Retention policies and cost implications
- Artifact download links and expiration

**Practical Work:**

- Upload test results and coverage reports as artifacts
- Download artifacts in subsequent jobs
- Implement retention policies to manage storage
- Access artifacts via GitHub API
- Create artifact download workflows


<details>
<summary>📦 Scenario 12-A: Upload and Download Artifacts Across Jobs</summary>


**Objective:** Upload test results and a build binary as artifacts in one job, then consume them in separate downstream jobs demonstrating cross-job artifact sharing.

**Prerequisites:** Sections 1–11 complete.

**Context:** Artifacts persist beyond a single job and can be downloaded by subsequent jobs in the same workflow run, by humans via the UI, or programmatically via the REST API. The `actions/upload-artifact` and `actions/download-artifact` v4 actions use immutable artifact names within a run.

**Detailed Steps:**

1. Create `.github/workflows/artifacts-demo.yml`:

```yaml
name: Artifacts Demo

on: [push, workflow_dispatch]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build application (simulated)
        run: |
          mkdir -p dist
          echo '{"version":"1.0.0","built":"'$(date -u +%Y-%m-%dT%H:%M:%SZ)'"}' > dist/app.json
          echo "Build complete"

      - name: Run tests and generate report
        run: |
          mkdir -p test-results
          cat <<EOF > test-results/junit.xml
          <?xml version="1.0" encoding="UTF-8"?>
          <testsuite name="unit" tests="5" failures="0">
            <testcase name="test_login" classname="auth.tests" time="0.12"/>
            <testcase name="test_logout" classname="auth.tests" time="0.08"/>
            <testcase name="test_register" classname="auth.tests" time="0.15"/>
            <testcase name="test_password_reset" classname="auth.tests" time="0.09"/>
            <testcase name="test_session" classname="auth.tests" time="0.11"/>
          </testsuite>
          EOF
          echo "Test report generated"

      - name: Generate coverage report
        run: |
          mkdir -p coverage
          echo "Statements: 87%\nBranches: 72%\nFunctions: 91%\nLines: 87%" > coverage/summary.txt

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ github.sha }}
          path: dist/
          # Keep for 30 days (default is 90 days)
          retention-days: 30

      - name: Upload test results
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ github.run_number }}
          path: test-results/
          retention-days: 7

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ github.run_number }}
          path: coverage/
          retention-days: 7

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: dist-${{ github.sha }}
          path: ./dist

      - name: Verify artifact content
        run: |
          echo "Contents of dist:"
          ls -la dist/
          cat dist/app.json

  analyze-coverage:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - name: Download coverage report
        uses: actions/download-artifact@v4
        with:
          name: coverage-${{ github.run_number }}
          path: ./coverage-report

      - name: Display coverage summary
        run: cat coverage-report/summary.txt
```

2. Push and observe three parallel downstream jobs after `build-and-test`.
3. Navigate to the run → **Artifacts** section to see three uploaded artifacts.
4. Download one artifact via the UI and inspect the content.

**Success Criteria:**

- [ ] Three distinct artifacts are uploaded with different names.
- [ ] `deploy` and `analyze-coverage` jobs each download their respective artifact.
- [ ] `retention-days` values are visible in the artifact list in the UI.
- [ ] `dist/app.json` contains the correct build timestamp.

**Troubleshooting:**

- **`download-artifact` fails with "No artifact found"** → Verify the artifact name in `download-artifact` exactly matches the name in `upload-artifact`. Names are case-sensitive and include `${{ github.sha }}` interpolation.
- **Artifact is empty** → Check the `path:` in `upload-artifact`. It must point to an existing directory or file. If the path doesn't exist, the action fails silently in some versions.

**Time Estimate:** 20–25 minutes

</details>

---

<details>
<summary>🌐 Scenario 12-B: Download All Artifacts and REST API Access</summary>


**Objective:** Download all artifacts from a run in one step, implement retention policy management, and demonstrate REST API artifact access.

**Prerequisites:** Scenario 12-A complete; a GitHub Personal Access Token (PAT) with `repo` scope or `GITHUB_TOKEN` with `actions: read`.

**Detailed Steps:**

1. Extend the previous workflow to add an aggregation job:

```yaml
  aggregate-all:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - name: Download ALL artifacts from this run
        uses: actions/download-artifact@v4
        with:
          # Omitting 'name' downloads all artifacts into separate subdirectories
          path: ./all-artifacts

      - name: List all downloaded artifacts
        run: |
          echo "All artifacts:"
          find ./all-artifacts -type f | sort

      - name: Combine reports
        run: |
          echo "=== PIPELINE SUMMARY ===" > pipeline-summary.txt
          echo "Run: ${{ github.run_number }}" >> pipeline-summary.txt
          echo "SHA: ${{ github.sha }}" >> pipeline-summary.txt
          echo "" >> pipeline-summary.txt
          if [ -f all-artifacts/coverage-${{ github.run_number }}/summary.txt ]; then
            echo "=== COVERAGE ===" >> pipeline-summary.txt
            cat all-artifacts/coverage-${{ github.run_number }}/summary.txt >> pipeline-summary.txt
          fi
          cat pipeline-summary.txt

      - name: Upload pipeline summary
        uses: actions/upload-artifact@v4
        with:
          name: pipeline-summary-${{ github.run_number }}
          path: pipeline-summary.txt
```

2. Add a separate step showing REST API artifact access:

```yaml
      - name: List artifacts via REST API
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Use the GitHub CLI (preinstalled on ubuntu-latest) to query artifacts
          gh api \
            /repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts \
            --jq '.artifacts[] | [.name, .size_in_bytes, .expires_at] | @tsv'
```

3. Trigger the workflow and verify the aggregation job lists all artifact files.
4. Verify the REST API response shows all artifacts with sizes and expiration dates.

**Success Criteria:**

- [ ] `download-artifact` without `name:` downloads all artifacts into subdirectories.
- [ ] `find ./all-artifacts` shows files from all three artifact directories.
- [ ] REST API response lists artifacts with correct sizes.
- [ ] `pipeline-summary` artifact is created and uploadable.

**Troubleshooting:**

- **`gh api` returns 401** → The `GITHUB_TOKEN` must have `actions: read` permission. Add `permissions: actions: read` to the job.
- **Artifacts subdirectory names unexpected** → When downloading all artifacts, each artifact gets its own subdirectory named after the artifact. The `path:` becomes the parent.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 16 (Workflow REST API) for full artifact management via API; → Section 27 (Job Summaries) for using `GITHUB_STEP_SUMMARY` as an alternative to artifact-based reporting.

</details>

---

<details>
<summary>💰 Scenario 12-C: Artifact Retention Policies and Cost Optimization</summary>


**Objective:** Implement retention policies to manage artifact storage costs.

**Prerequisites:** Scenario 12-B complete.

**Context:** Artifacts consume quota. Retention policies auto-delete old artifacts, reducing costs.

**Detailed Steps:**

1. Upload artifacts with `retention-days: 7`
2. Check storage usage in Settings → Actions → Storage usage
3. Calculate storage impact for builds with artifacts

**Success Criteria:**

- [ ] Artifacts auto-delete after retention period
- [ ] Quota remains stable over time
- [ ] Can override retention per-artifact if needed

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 25 (Performance) for cost tracking and optimization.

</details>

---

<details>
<summary>🔧 Scenario 12-D: Recover from Artifact Storage Quota Exceeded</summary>


**Objective:** Diagnose and fix scenarios where artifact storage quota is exhausted.

**Prerequisites:** Scenario 12-C complete.

**Context:** Build accumulation over time exhausts quota, causing deployment failures.

**Detailed Steps:**

1. Check usage in Settings → Actions → Storage usage
2. Identify large artifacts using REST API
3. Delete old artifacts programmatically
4. Reduce retention for future builds

**Success Criteria:**

- [ ] Identify quota exceeded
- [ ] Delete old artifacts
- [ ] Set lower retention to prevent recurrence

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 16 (REST API) for artifact queries.

</details>

---

<details>
<summary>📁 Scenario 12-E: Long-Term Artifact Archival Strategy</summary>


**Objective:** Archive critical artifacts to long-term storage (S3, Azure) while keeping recent artifacts locally.

**Prerequisites:** Scenario 12-D complete.

**Context:** Production builds need long-term retention; GitHub storage is expensive.

**Detailed Steps:**

1. Create scheduled workflow that archives monthly
2. Move artifacts from GitHub to S3/Blob after 7 days
3. Delete from GitHub to free quota
4. Implement restore process for archived artifacts

**Success Criteria:**

- [ ] Artifacts archived to S3 after 7 days
- [ ] GitHub storage stabilized
- [ ] Can retrieve archived artifacts when needed

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 16 (REST API) for automation and retrieval.

</details>

---

### 💨 Section 13: Workflow Caching - Performance & Security (2 hrs)

**Topics:** [10-Workflow-Caching.md](10-Workflow-Caching.md)
**Overview:**

- Caching concepts: key, path, restore key
- Cache action: setup-cache vs generic cache
- Cache strategy: pip, npm, gradle, maven
- Cache key generation and versioning
- Scope: repository vs branch level
- Cache invalidation and cleanup
- Security considerations for cached data

**Practical Work:**

- Implement npm dependency caching
- Set up Python pip caching
- Create effective cache key strategies
- Test cache hits and misses
- Implement cache cleanup for sensitive data
- Compare caching performance benefits


<details>
<summary>💨 Scenario 13-A: npm and Python Cache Strategies with Key Invalidation</summary>


**Objective:** Implement caching for npm and pip dependencies with correct key strategies that invalidate automatically when lock files change.

**Prerequisites:** Sections 1–12 complete; a repository with `package-lock.json` and/or `requirements.txt` (create them if needed).

**Context:** The cache `key` is a hash string that identifies whether a cache hit exists. Basing the key on the lock file hash (`hashFiles(...)`) ensures the cache is invalidated whenever dependencies change. The `restore-keys` fallback provides a partial cache restore when the exact key misses.

**Detailed Steps:**

1. Create sample dependency files (if they don't exist):

```bash
# Run locally or in a workflow step
echo '{"name":"demo","version":"1.0.0","dependencies":{"lodash":"^4.17.21"}}' > package.json
echo '{}' > package-lock.json  # simplified for demo
echo "requests==2.31.0" > requirements.txt
```

2. Create `.github/workflows/caching-demo.yml`:

```yaml
name: Caching Demo

on: [push, workflow_dispatch]

jobs:
  npm-with-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache npm dependencies
        uses: actions/cache@v4
        id: npm-cache
        with:
          path: ~/.npm           # npm's global cache directory
          # Key includes runner OS and lockfile hash for correct invalidation
          key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
          # Fallback: use any existing npm cache for this OS (partial restore)
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Show cache hit/miss
        run: |
          echo "Cache hit: ${{ steps.npm-cache.outputs.cache-hit }}"
          # 'true' means exact key matched; 'false' means miss or restore-key match

      - name: Install npm dependencies
        # Even on cache hit, 'npm ci' is fast because it restores from ~/.npm
        run: npm ci --if-present

  python-with-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          # setup-python has built-in cache support — preferred over manual cache
          cache: 'pip'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Verify installations
        run: pip list | grep requests

  manual-python-cache:
    # Shows manual cache approach for comparison with setup-python built-in
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Manual pip cache
        uses: actions/cache@v4
        with:
          # pip cache directory (varies by OS)
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - run: pip install -r requirements.txt
```

3. Push twice and compare run times on the second push (cache hit expected).
4. Edit `requirements.txt` (add a new package), push again — observe cache miss and rebuild.

**Success Criteria:**

- [ ] Second run shows `cache-hit: true` for npm job.
- [ ] After editing `requirements.txt`, cache misses and reinstalls dependencies.
- [ ] `setup-python` with `cache: 'pip'` is simpler and preferable to manual cache.
- [ ] `restore-keys` allows partial cache restore when exact key misses.

**Troubleshooting:**

- **Cache never hits** → Verify the `path:` matches where the package manager stores its cache. `~/.npm` for npm, `~/.cache/pip` for pip on Linux. Use `echo $(pip cache dir)` to check.
- **`hashFiles` returns different values** → `hashFiles` is relative to the workspace root (`$GITHUB_WORKSPACE`). Verify the lock file path pattern is correct.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🏎️ Scenario 13-B: Gradle/Maven Caching and Cache Size Management</summary>


**Objective:** Implement build tool caching for Java projects (Gradle and Maven), understand cache scope limitations, and practice cache busting.

**Prerequisites:** Scenario 13-A complete; Java familiarity helpful but not required.

**Context:** Caches are scoped to the branch that created them. Pull requests can use the base branch's cache but cannot write back to it. Cache entries have a 7-day inactivity expiry and are evicted when the repository reaches 10 GB. Understanding scope prevents confusion when PRs don't benefit from feature branch caches.

**Detailed Steps:**

1. Create `.github/workflows/java-cache.yml`:

```yaml
name: Java Build Cache

on: [push, pull_request, workflow_dispatch]

jobs:
  gradle-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          # Built-in cache for Gradle — preferred approach
          cache: gradle

      - name: Make gradlew executable (if present)
        run: chmod +x gradlew 2>/dev/null || echo "No gradlew, using gradle"

      - name: Build with Gradle
        run: gradle --version  # Placeholder for: ./gradlew build

  maven-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven  # Built-in Maven cache

      - name: Build with Maven
        run: mvn --version  # Placeholder for: mvn -B package

  manual-gradle-cache:
    # Manual cache example showing cache key structure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          # Version bump ('v1' suffix) to force cache bust when needed
          key: ${{ runner.os }}-gradle-v1-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-v1-
            ${{ runner.os }}-gradle-

      - name: Build
        run: gradle --version
```

2. Trigger from a feature branch and observe that PRs can restore from the `main` branch cache.
3. Add `-v2` to the manual cache key to bust the cache (simulating a dependency conflict).

**Success Criteria:**

- [ ] `setup-java` with `cache: gradle` sets up caching automatically.
- [ ] Manual cache example shows correct `~/.gradle` path structure.
- [ ] Version suffix in key (`-v1`) can be incremented to force cache invalidation.

**Troubleshooting:**

- **PR doesn't use cache from main** → Caches from feature branches are not accessible to `main`, but `main`'s cache IS accessible to PRs. If the PR was pushed before main had a cache, there's nothing to restore.
- **Cache size growing unbounded** → Set retention-days on artifacts; for caches, the 7-day inactivity eviction handles it. Explicitly delete with `gh cache delete` for large stale caches.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 25 (Performance Optimization) for measuring cache impact on total workflow time; → Section 23 (Security) for avoiding sensitive data in caches.

</details>

---

<details>
<summary>🔄 Scenario 13-C: Cache Invalidation Strategies and Partial Cache Hits</summary>


**Objective:** Master cache invalidation to ensure fresh dependencies while minimizing rebuild time.

**Prerequisites:** Scenario 13-B complete.

**Context:** Stale caches cause cryptic failures. Learning invalidation prevents wasted troubleshooting.

**Detailed Steps:**

1. Use package-lock hash for cache key: `key: maven-${{ hashFiles('**/pom.xml') }}`
2. Modify `pom.xml` → cache key changes → fresh download
3. Use `restore-keys` for partial match fallback

**Success Criteria:**

- [ ] Exact key match restores specific cache
- [ ] Partial match provides fallback
- [ ] Modified pom.xml triggers fresh download

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 25 (Performance) for tuning cache hit rates.

</details>

---

<details>
<summary>🐛 Scenario 13-D: Debug Cache Misses and Slow Builds Despite Caching</summary>


**Objective:** Troubleshoot why caches aren't hitting or builds remain slow.

**Prerequisites:** Scenario 13-C complete.

**Context:** Cache misses waste time. Diagnose and fix to improve build speed.

**Detailed Steps:**

1. Add debug logging with `ACTIONS_CACHE_DEBUG: true`
2. Check cache size: Settings → Actions → Cache usage
3. Verify cache path exists before caching

**Success Criteria:**

- [ ] Identify cache miss reason (key, path, permissions)
- [ ] Fix cache hit rate to >80%
- [ ] Build time reduces >30%

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for cache troubleshooting.

</details>

---

<details>
<summary>🗂️ Scenario 13-E: Multi-Layer Cache Strategy for Monorepo</summary>


**Objective:** Implement tiered caching for monorepo: shared cache, service-specific cache.

**Prerequisites:** Scenario 13-D complete.

**Context:** Large monorepos need efficient caching: shared deps + service-specific. This scales builds.

**Detailed Steps:**

1. Create shared layer cache for node_modules
2. Create service-specific layer for each service
3. Restore priority: exact match → shared layer → empty

**Success Criteria:**

- [ ] Multi-layer improves hit rates
- [ ] Service-specific changes invalidate only service cache
- [ ] Shared layer reusable across services

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 10 (Matrix) for per-service caching.

</details>

---

## 🚀 Week 4: Advanced Workflow Features

### 📡 Section 14: Inter-Job Communication & Outputs (2 hrs)

**Topics:** [GitHub-Workflows-Guide.md](GitHub-Workflows-Guide.md) - Job Outputs section
**Overview:**

- Job outputs: defining and exporting step outputs
- Output interpolation in dependent jobs
- GITHUB_OUTPUT file for setting job outputs
- Artifact-based data passing for larger payloads
- Environment file sharing (GITHUB_ENV)
- Matrix job outputs and aggregation
- Output limits and best practices

**Practical Work:**

- Create workflows with job-to-job output passing
- Implement output aggregation from matrix jobs
- Use outputs in conditional job execution
- Pass complex data structures between jobs
- Test output limits and fallback strategies


<details>
<summary>📤 Scenario 14-A: Job Outputs and GITHUB_OUTPUT</summary>


**Objective:** Set step outputs using `GITHUB_OUTPUT`, consume them in subsequent steps, and pass job-level outputs to downstream jobs via `needs` context.

**Prerequisites:** Sections 1–13 complete.

**Context:** Step outputs written to `$GITHUB_OUTPUT` are available in subsequent steps via `${{ steps.<id>.outputs.<name> }}`. A job can expose step outputs as job outputs using the `outputs:` key at the job level. Downstream jobs access them via `${{ needs.<job>.outputs.<name> }}`.

**Detailed Steps:**

1. Create `.github/workflows/job-outputs.yml`:

```yaml
name: Job Outputs Demo

on: workflow_dispatch

jobs:
  prepare:
    runs-on: ubuntu-latest
    # Declare what outputs this job exposes to downstream jobs
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      environment: ${{ steps.determine-env.outputs.env }}
      should-deploy: ${{ steps.check-deploy.outputs.deploy }}

    steps:
      - uses: actions/checkout@v4

      - name: Get version from package.json (or generate one)
        id: get-version
        run: |
          # Try to read from package.json; fall back to git-based version
          if [ -f package.json ]; then
            VERSION=$(node -p "require('./package.json').version" 2>/dev/null || echo "1.0.0")
          else
            VERSION="1.0.${{ github.run_number }}"
          fi
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Resolved version: ${VERSION}"

      - name: Determine deployment environment
        id: determine-env
        run: |
          BRANCH="${{ github.ref_name }}"
          if [[ "$BRANCH" == "main" ]]; then
            echo "env=production" >> $GITHUB_OUTPUT
          elif [[ "$BRANCH" == "staging" ]]; then
            echo "env=staging" >> $GITHUB_OUTPUT
          else
            echo "env=development" >> $GITHUB_OUTPUT
          fi

      - name: Check if deployment is needed
        id: check-deploy
        run: |
          # Deploy only if version contains no '-dev' suffix
          VERSION="${{ steps.get-version.outputs.version }}"
          if [[ "$VERSION" != *"-dev"* ]]; then
            echo "deploy=true" >> $GITHUB_OUTPUT
          else
            echo "deploy=false" >> $GITHUB_OUTPUT
          fi

  deploy:
    runs-on: ubuntu-latest
    needs: prepare
    # Only deploy if the prepare job says to
    if: needs.prepare.outputs.should-deploy == 'true'
    steps:
      - name: Use outputs from prepare job
        run: |
          echo "Version    : ${{ needs.prepare.outputs.version }}"
          echo "Environment: ${{ needs.prepare.outputs.environment }}"
          echo "Deploying ${{ needs.prepare.outputs.version }} to ${{ needs.prepare.outputs.environment }}"

  notify:
    runs-on: ubuntu-latest
    needs: [prepare, deploy]
    if: always()
    steps:
      - run: |
          echo "Deploy result: ${{ needs.deploy.result }}"
          echo "Version      : ${{ needs.prepare.outputs.version }}"
```

2. Trigger via `workflow_dispatch` from `main`.
3. Verify `deploy` job shows the correct version and environment.
4. Create a branch with a `-dev` version or ensure `should-deploy=false` to see the `deploy` job skipped.

**Success Criteria:**

- [ ] `prepare` job outputs appear in `deploy` job via `needs.prepare.outputs`.
- [ ] `deploy` job is skipped when `should-deploy` is `false`.
- [ ] Multiple step outputs are aggregated into job-level outputs correctly.
- [ ] `notify` job runs regardless and shows correct `deploy` result.

**Troubleshooting:**

- **Output is empty in downstream job** → Verify the job `outputs:` key maps the step output: `version: ${{ steps.get-version.outputs.version }}`. The mapping must exist at job level, not just step level.
- **`GITHUB_OUTPUT` not found** → This environment variable is automatically set; do not hardcode a path. Use `echo "key=value" >> $GITHUB_OUTPUT`.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🔄 Scenario 14-B: GITHUB_ENV and Environment Variable Sharing</summary>


**Objective:** Use `GITHUB_ENV` to set environment variables that persist across subsequent steps in the same job, and contrast with `GITHUB_OUTPUT` for inter-job sharing.

**Prerequisites:** Scenario 14-A complete.

**Context:** `GITHUB_ENV` sets environment variables that are available in all subsequent steps of the same job. Unlike `GITHUB_OUTPUT`, these variables are NOT available in other jobs. For multi-line values, use the heredoc syntax. This file-based approach replaced the deprecated `::set-env::` workflow command.

**Detailed Steps:**

1. Create `.github/workflows/github-env-demo.yml`:

```yaml
name: GITHUB_ENV Demo

on: workflow_dispatch

jobs:
  env-sharing:
    runs-on: ubuntu-latest
    steps:
      - name: Set environment variables for subsequent steps
        run: |
          # Single-line variable
          echo "APP_VERSION=2.0.${{ github.run_number }}" >> $GITHUB_ENV
          echo "BUILD_DATE=$(date -u +%Y-%m-%d)" >> $GITHUB_ENV

          # Multi-line variable using heredoc syntax
          echo "RELEASE_NOTES<<EOF" >> $GITHUB_ENV
          echo "Version 2.0.${{ github.run_number }}" >> $GITHUB_ENV
          echo "Built on $(date -u +%Y-%m-%d)" >> $GITHUB_ENV
          echo "Commit: ${{ github.sha }}" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Use environment variables in next step
        run: |
          # Variables set via GITHUB_ENV are available here
          echo "App version : $APP_VERSION"
          echo "Build date  : $BUILD_DATE"
          echo "Release notes:"
          echo "$RELEASE_NOTES"

      - name: Demonstrate GITHUB_PATH for PATH modifications
        run: |
          # Add a custom directory to PATH for subsequent steps
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          mkdir -p "$HOME/.local/bin"
          echo '#!/bin/bash\necho "Custom tool v1.0"' > "$HOME/.local/bin/my-tool"
          chmod +x "$HOME/.local/bin/my-tool"

      - name: Use the custom PATH tool
        run: my-tool  # available because GITHUB_PATH was updated

  contrast-with-output:
    runs-on: ubuntu-latest
    steps:
      - name: Show scope difference
        run: |
          echo "GITHUB_ENV  = available in subsequent STEPS of the same JOB"
          echo "GITHUB_OUTPUT = available in downstream JOBS via needs context"
          echo "GITHUB_PATH = modifies PATH for subsequent steps in same job"
```

2. Trigger and verify the `APP_VERSION` and `BUILD_DATE` are available in the second step.
3. Verify the multi-line `RELEASE_NOTES` variable preserves newlines.
4. Verify `my-tool` is found after updating `GITHUB_PATH`.

**Success Criteria:**

- [ ] `APP_VERSION` set in step 1 is readable in step 2 without explicit `env:`.
- [ ] Multi-line variable with heredoc syntax preserves all three lines.
- [ ] `my-tool` runs successfully after `GITHUB_PATH` update.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 10 (Matrix Strategies) for understanding why `GITHUB_ENV` doesn't persist across matrix job cells; → Section 27 (Job Summaries) for using `GITHUB_STEP_SUMMARY` as another special environment file.

</details>

---

<details>
<summary>🔣 Scenario 14-C: Complex Output Parsing and JSON Manipulation</summary>


**Objective:** Parse and manipulate complex JSON outputs between jobs using jq.

**Prerequisites:** Scenario 14-B complete; JSON basics.

**Context:** Real-world outputs are often complex JSON. Parsing correctly prevents failures.

**Detailed Steps:**

1. Generate complex JSON output in one job
2. Parse using `jq` in downstream job
3. Extract values and use in subsequent steps

**Success Criteria:**

- [ ] Complex JSON output passed between jobs
- [ ] jq extracts values correctly
- [ ] No parse errors in workflow

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 16 (REST API) for JSON handling.

</details>

---

<details>
<summary>🔧 Scenario 14-D: Fix Data Loss When Passing Large Outputs</summary>


**Objective:** Diagnose and fix scenarios where large outputs are truncated or lost.

**Prerequisites:** Scenario 14-C complete.

**Context:** Output size limit is 1MB. Exceeding causes truncation or failures.

**Detailed Steps:**

1. Create large output (exceeds 1MB)
2. Observe truncation in downstream job
3. Fix by using artifacts instead of outputs

**Success Criteria:**

- [ ] Identify size limits (1MB for outputs)
- [ ] Use artifacts for large data transfer
- [ ] No data loss in workflow

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 12 (Artifacts) for inter-job data transfer.

</details>

---

<details>
<summary>📡 Scenario 14-E: Event-Driven Job Communication Patterns</summary>


**Objective:** Implement event-driven patterns for loose coupling across repositories.

**Prerequisites:** Scenario 14-D complete.

**Context:** Large organizations coordinate via events. Learning this enables enterprise-scale automation.

**Detailed Steps:**

1. Repo A triggers Repo B via `repository_dispatch` event
2. Pass data via `client_payload`
3. Repo B listens and processes event

**Success Criteria:**

- [ ] Cross-repo communication via repository_dispatch
- [ ] Payload correctly passed
- [ ] Loosely coupled orchestration

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 5 (Trigger Events) for repository_dispatch.

</details>

---

### 🐛 Section 15: Workflow Debugging & Troubleshooting (2 hrs)

**Topics:** [12-Workflow-Debugging.md](12-Workflow-Debugging.md), [19-Common-Failures-Troubleshooting.md](19-Common-Failures-Troubleshooting.md) (first half)
**Overview:**

- Debug logging: step context and secrets redaction
- Runner diagnostics information
- Log retention and access via UI/API
- Common failure patterns and causes
- Timeout and resource exhaustion issues
- YAML parsing and syntax errors
- Matrix job failure correlation

**Practical Work:**

- Enable debug logging in a workflow run
- Analyze workflow logs for errors
- Diagnose timeout issues
- Fix common YAML parsing errors
- Use run history to identify failure patterns
- Create diagnostic workflows


<details>
<summary>🔍 Scenario 15-A: Enable Debug Logging and Analyze Logs</summary>


**Objective:** Enable step-debug logging via repository secrets, interpret runner diagnostic logs, and build a diagnostic step that surfaces useful context when workflows fail.

**Prerequisites:** Sections 1–14 complete; repository admin access to add secrets.

**Context:** GitHub Actions provides two debug logging secrets: `ACTIONS_STEP_DEBUG` enables verbose step output (all `::debug::` messages), and `ACTIONS_RUNNER_DEBUG` logs runner-level infrastructure details. Both are enabled by setting the secret value to `true`. For one-off debugging without permanent secrets, re-run with the "Enable debug logging" checkbox.

**Detailed Steps:**

1. Navigate to **Repository → Settings → Secrets and variables → Actions → New repository secret**.
2. Add: `ACTIONS_STEP_DEBUG` = `true`

3. Create `.github/workflows/debug-logging.yml`:

```yaml
name: Debug Logging Demo

on: workflow_dispatch

jobs:
  debug-demo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Emit debug messages
        run: |
          # These appear only when ACTIONS_STEP_DEBUG=true
          echo "::debug::This is a debug message — only visible with debug logging"
          echo "::notice::This is a notice — always visible in log annotations"
          echo "::warning::This is a warning — always visible"
          echo "Normal output — always visible"

      - name: Group log output
        run: |
          echo "::group::Detailed System Information"
          uname -a
          df -h
          free -m
          env | sort | grep -v SECRET | grep -v TOKEN
          echo "::endgroup::"
          echo "System info captured (collapsed in UI)"

      - name: Diagnostic context dump
        if: always()  # ensure this runs even on failure
        run: |
          echo "=== DIAGNOSTIC CONTEXT ==="
          echo "Workflow   : ${{ github.workflow }}"
          echo "Run ID     : ${{ github.run_id }}"
          echo "Run number : ${{ github.run_number }}"
          echo "Actor      : ${{ github.actor }}"
          echo "Branch     : ${{ github.ref_name }}"
          echo "SHA        : ${{ github.sha }}"
          echo "Event      : ${{ github.event_name }}"
          echo "Runner OS  : ${{ runner.os }}"
          echo "Runner arch: ${{ runner.arch }}"

      - name: Simulate a failure with useful error message
        run: |
          echo "::error file=src/app.js,line=42,col=8::Null pointer dereference — variable 'user' is undefined"
          exit 1

      - name: This step is skipped because previous failed
        run: echo "Never runs"
```

4. Trigger via `workflow_dispatch` with debug logging enabled.
5. Compare the log verbosity with and without `ACTIONS_STEP_DEBUG=true`.
6. Observe the `::group::` collapsible section in the UI.
7. Note the `::error::` annotation linking to a file/line.

**Success Criteria:**

- [ ] Debug messages appear in logs when `ACTIONS_STEP_DEBUG=true`.
- [ ] `::group::` creates a collapsible section in the UI.
- [ ] `::error::` creates a file annotation in the PR/commit view.
- [ ] Diagnostic context step runs even after the failure (`if: always()`).

**Troubleshooting:**

- **Debug messages not appearing** → Verify the secret name is exactly `ACTIONS_STEP_DEBUG` (case-sensitive) with value `true` (not `True` or `1`).
- **Annotations not appearing on PR** → Annotations are only visible on the commit/PR that triggered the workflow. They don't appear for `workflow_dispatch` triggered runs in the PR view.

**Time Estimate:** 20–25 minutes

</details>

---

<details>
<summary>🔧 Scenario 15-B: Diagnosing YAML Syntax Errors and Timeout Issues</summary>


**Objective:** Intentionally introduce and fix common YAML errors, and configure `timeout-minutes` to handle runaway steps.

**Prerequisites:** Scenario 15-A complete; VS Code with the GitHub Actions extension installed (Section 2).

**Context:** YAML is whitespace-sensitive. Common errors include incorrect indentation, wrong block scalar style (`|` vs `>`), and missing quotes around `on:` event names. Workflow YAML is validated at push time — the workflow will show as invalid before any runner picks it up. Timeouts prevent billing waste from hung processes.

**Detailed Steps:**

1. Create a workflow with intentional errors to study the error messages:

```yaml
# INTENTIONALLY BROKEN — study the error messages, then fix
name: YAML Error Study

on: workflow_dispatch

jobs:
  broken-job:
    runs-on: ubuntu-latest
    steps:
    - name: Bad indentation (steps should be 6 spaces or 3 x 2-space indent)
      run: echo "This fails due to the 4-space indent on steps"
    - name: Second step
       run: echo "Extra space before run causes mapping key error"
```

2. Push this file and observe the workflow error in the Actions tab before it runs.
3. Fix the indentation and push again.

4. Create a proper timeout demonstration:

```yaml
name: Timeout Demo

on: workflow_dispatch

jobs:
  job-with-timeout:
    runs-on: ubuntu-latest
    timeout-minutes: 2  # entire job cancelled after 2 minutes
    steps:
      - name: Quick step
        timeout-minutes: 1  # individual step timeout
        run: echo "Fast step — completes in seconds"

      - name: Simulated hung process
        timeout-minutes: 1  # this step will time out
        continue-on-error: true  # allow job to continue despite timeout
        run: |
          echo "Starting potentially hung process..."
          sleep 120  # will be killed at 1-minute step timeout
          echo "This line never executes"

      - name: Cleanup after timeout
        if: always()
        run: echo "Cleanup runs regardless of timeout"
```

5. Push and observe the hung step being killed at 1 minute with exit code 124.
6. Verify the cleanup step still runs due to `if: always()`.

**Success Criteria:**

- [ ] YAML indentation errors produce clear validation messages in the Actions tab.
- [ ] VS Code highlights indentation errors with the GitHub Actions extension.
- [ ] The `sleep 120` step is killed at 1 minute with a timeout annotation.
- [ ] Cleanup step runs after the timeout.

**Troubleshooting:**

- **Workflow shows as invalid but no error message** → Click on the workflow name in the Actions tab; invalid workflows show a banner with the specific YAML parse error.
- **`timeout-minutes` seems to be ignored** → Ensure `timeout-minutes` is at the correct level (job-level or step-level). Job-level timeout includes all steps; step-level timeout is per-step.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 2 (VS Code extension) for YAML schema validation that catches errors before pushing; → Section 26 (Troubleshooting) for production-level failure diagnosis techniques.

</details>

---

<details>
<summary>🔬 Scenario 15-C: Step Debugging with Advanced Log Inspection</summary>


**Objective:** Use GitHub Actions' debug mode and advanced logging to pinpoint failures.

**Prerequisites:** Scenario 15-B complete.

**Context:** `RUNNER_DEBUG` enables verbose logging. Using it systematically accelerates troubleshooting.

**Detailed Steps:**

1. Enable runner debug with `RUNNER_DEBUG: 1`
2. Use `set -x` to print commands before executing
3. Use grouped logs to organize sections of output

**Success Criteria:**

- [ ] Runner debug visible in logs
- [ ] `set -x` shows all commands
- [ ] Grouped logs organize output sections

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 27 (Job Summaries) for rich debugging output.

</details>

---

<details>
<summary>🖥️ Scenario 15-D: Reproduce CI Failures Locally with act</summary>


**Objective:** Use `act` and Docker to reproduce CI failures locally.

**Prerequisites:** Scenario 15-C complete; act CLI installed.

**Context:** "It works on my machine" is solved by local reproduction with act.

**Detailed Steps:**

1. Install act locally
2. Run workflows locally: `act push -j build`
3. Debug locally before pushing to GitHub
4. Verify fix locally

**Success Criteria:**

- [ ] Reproduce failure locally
- [ ] Identify root cause
- [ ] Verify fix before pushing

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 1 (Fundamentals) for act setup and basic usage.

</details>

---

<details>
<summary>📊 Scenario 15-E: Advanced Log Aggregation and Automatic Issue Filing</summary>


**Objective:** Aggregate logs from multiple jobs and auto-file issues for recurring failures.

**Prerequisites:** Scenario 15-D complete.

**Context:** Recurring failures should be tracked. Automatic issue creation prevents forgotten bugs.

**Detailed Steps:**

1. Create aggregation workflow triggered on `workflow_run`
2. Download artifacts/logs from failed workflows
3. Analyze logs for patterns
4. Auto-file issues for recurring failures (>5 occurrences)

**Success Criteria:**

- [ ] Logs aggregated from multiple jobs
- [ ] Patterns identified automatically
- [ ] Issues auto-filed for recurring failures

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 27 (Job Summaries) for rich reporting.

</details>

---

### 🌐 Section 16: GitHub Workflow REST API (2 hrs)

**Topics:** [13-Workflows-REST-API.md](13-Workflows-REST-API.md)
**Overview:**

- REST API endpoints for workflows
- List, get, and manage workflow runs
- Retrieve job information and logs
- Download artifacts programmatically
- Manage workflow runs: cancel, approve, review
- Retention policy management via API
- API rate limits and authentication

**Practical Work:**

- Query workflow runs using REST API
- Download artifacts via API scripts
- Cancel workflow runs programmatically
- Retrieve deployment status via API
- Build automation around workflow management


<details>
<summary>🌐 Scenario 16-A: Query and Manage Workflow Runs via REST API</summary>


**Objective:** Use the GitHub CLI and REST API to list workflow runs, retrieve job logs, cancel runs, and download artifacts programmatically.

**Prerequisites:** Sections 1–15 complete; GitHub CLI (`gh`) installed locally or used via runner; PAT with `repo` and `actions: read` scope or `GITHUB_TOKEN`.

**Context:** The GitHub Actions REST API provides endpoints for programmatic workflow management. The GitHub CLI (`gh`) wraps these endpoints with a friendly interface and is pre-installed on all GitHub-hosted runners. This is valuable for CI/CD automation, status dashboards, and integrating GitHub Actions into external tooling.

**Detailed Steps:**

1. On your local machine (or in a workflow step), authenticate:

```bash
gh auth login
# Or set GH_TOKEN environment variable
```

2. Create `.github/workflows/api-automation.yml`:

```yaml
name: REST API Automation Demo

on: workflow_dispatch

jobs:
  api-queries:
    runs-on: ubuntu-latest
    permissions:
      actions: read   # required to query workflow runs
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: List recent workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "=== Recent workflow runs ==="
          gh run list \
            --repo ${{ github.repository }} \
            --limit 5 \
            --json databaseId,displayTitle,status,conclusion,createdAt \
            --template '{{range .}}{{.databaseId}} | {{.status}} | {{.conclusion}} | {{.displayTitle}}\n{{end}}'

      - name: Get current run details
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "=== Current run details ==="
          gh run view ${{ github.run_id }} \
            --repo ${{ github.repository }} \
            --json status,conclusion,jobs

      - name: List jobs in current run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "=== Jobs in this run ==="
          gh api \
            /repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/jobs \
            --jq '.jobs[] | "\(.name): \(.status) — \(.conclusion // "in_progress")"'

      - name: List available workflows
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "=== Workflows in this repo ==="
          gh workflow list \
            --repo ${{ github.repository }} \
            --all

      - name: Download artifacts from current run (if any exist)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "=== Artifacts in current run ==="
          ARTIFACTS=$(gh api \
            /repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts \
            --jq '.total_count')
          echo "Total artifacts: $ARTIFACTS"
          if [ "$ARTIFACTS" -gt "0" ]; then
            gh run download ${{ github.run_id }} \
              --repo ${{ github.repository }} \
              --dir ./downloaded-artifacts
            ls -la ./downloaded-artifacts/
          fi
```

3. Trigger via `workflow_dispatch` and review the output of each API query step.
4. Run the same `gh` commands locally against your repository to practice.

**Success Criteria:**

- [ ] Recent workflow runs are listed with status and conclusion.
- [ ] Current run jobs are retrieved via REST API.
- [ ] All available workflows are listed.
- [ ] Artifact download works when artifacts exist.

**Troubleshooting:**

- **`gh: error: unknown flag`** → Some `gh` flags vary by version. Use `gh run list --help` to see available options on the runner's installed version.
- **`actions: read` permission insufficient** → Some API endpoints require higher permissions. Check the specific endpoint's documentation for required scopes.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🚀 Scenario 16-B: Trigger Workflows and Manage Deployments via API</summary>


**Objective:** Trigger a `workflow_dispatch` event via the REST API, approve pending deployments programmatically, and cancel a running workflow.

**Prerequisites:** Scenario 16-A complete; a workflow with `workflow_dispatch` inputs.

**Detailed Steps:**

1. Create `.github/workflows/api-triggered.yml`:

```yaml
name: API Triggered Workflow

on:
  workflow_dispatch:
    inputs:
      deploy-target:
        description: 'Deployment target'
        type: string
        default: 'staging'
      debug-mode:
        description: 'Enable debug mode'
        type: boolean
        default: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Show trigger parameters
        run: |
          echo "Deploy target: ${{ inputs.deploy-target }}"
          echo "Debug mode   : ${{ inputs.debug-mode }}"
          echo "Triggered by API or manual dispatch"
          sleep 30  # Long enough to demonstrate cancellation
```

2. Trigger the workflow via REST API using `gh`:

```bash
# Trigger with inputs
gh workflow run api-triggered.yml \
  --repo OWNER/REPO \
  --ref main \
  --field deploy-target=production \
  --field debug-mode=true

# Get the run ID of the just-triggered run
sleep 3
LATEST_RUN=$(gh run list \
  --workflow api-triggered.yml \
  --repo OWNER/REPO \
  --limit 1 \
  --json databaseId \
  --jq '.[0].databaseId')

echo "Triggered run ID: $LATEST_RUN"
```

3. Cancel the run programmatically before `sleep 30` completes:

```bash
# Cancel the running workflow
gh run cancel $LATEST_RUN --repo OWNER/REPO
echo "Run $LATEST_RUN cancelled"
```

4. Verify the run shows as "Cancelled" in the Actions UI.

**Success Criteria:**

- [ ] `workflow_dispatch` inputs are passed correctly via the API.
- [ ] `sleep 30` step can be interrupted by a programmatic cancel.
- [ ] The UI shows the run as "Cancelled" after the API call.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 8 (Environments) for approving pending deployments via `POST /repos/{owner}/{repo}/actions/runs/{run_id}/pending_deployments`; → Section 17 (Reusable Workflows) for triggering reusable workflows via API.

</details>

---

<details>
<summary>🔍 Scenario 16-C: Advanced Workflow Querying and Filtering</summary>


**Objective:** Query workflows with advanced filters (status, branch, conclusion).

**Prerequisites:** Scenario 16-B complete.

**Context:** Complex queries enable analytics and monitoring of workflow health.

**Detailed Steps:**

1. Query workflows with status filter: `status=in_progress`
2. Filter by branch: `branch=main`
3. Combine filters: `status=completed&conclusion=success`

**Success Criteria:**

- [ ] Filters work correctly
- [ ] Complex queries return expected results
- [ ] Can analyze workflow patterns

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 25 (Performance) for monitoring.

</details>

---

<details>
<summary>⏱️ Scenario 16-D: Handle Rate Limiting and Pagination in REST API Calls</summary>


**Objective:** Implement robust pagination and handle GitHub API rate limits.

**Prerequisites:** Scenario 16-C complete.

**Context:** Large result sets exceed single page. Rate limits need backoff strategies.

**Detailed Steps:**

1. Check rate limits: `GET /rate_limit`
2. Implement pagination with `per_page=100` and `page=N`
3. Add exponential backoff on 429 errors

**Success Criteria:**

- [ ] Pagination works for large result sets
- [ ] Rate limit tracking implemented
- [ ] Backoff prevents rate limit errors

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 26 (Troubleshooting) for error handling.

</details>

---

<details>
<summary>🤖 Scenario 16-E: Batch Automation with REST API (Mass Workflow Trigger/Cancel)</summary>


**Objective:** Batch trigger or cancel multiple workflows using REST API.

**Prerequisites:** Scenario 16-D complete.

**Context:** Large organizations need to manage hundreds of workflows. Batch operations save time.

**Detailed Steps:**

1. Query all workflows in failure state
2. Trigger re-run programmatically for each
3. Monitor completion with polling

**Success Criteria:**

- [ ] Batch operations trigger multiple workflows
- [ ] Polling detects completion
- [ ] No rate limit errors

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 5 (Triggers) for understanding workflow triggering.

</details>

---

## ♻️ Week 5: Reusable Workflows & Actions

### ♻️ Section 17: Reusable Workflows & Workflow Sharing (2 hrs)

**Topics:** [11-Workflow-Sharing.md](11-Workflow-Sharing.md)
**Overview:**

- Reusable workflow concept: workflow_call
- Input/output mapping for reusable workflows
- Secrets in reusable workflows
- Adding reusable workflows from same org/repo
- Public action repository workflows
- Versioning and tagging strategies
- Calling workflows from different repos/orgs

**Practical Work:**

- Create a reusable workflow for common tasks
- Call reusable workflows with various inputs
- Implement proper versioning with tags
- Pass secrets to reusable workflows
- Test reusable workflow output handling


<details>
<summary>♻️ Scenario 17-A: Create and Call a Reusable Workflow</summary>


**Objective:** Create a reusable `workflow_call` workflow with typed inputs, outputs, and secret inheritance, then call it from a caller workflow.

**Prerequisites:** Sections 1–16 complete; repository with at least one branch.

**Context:** Reusable workflows are called via `uses: owner/repo/.github/workflows/file.yml@ref`. They accept typed `inputs:` and `secrets:` declared in their `on.workflow_call:` block. Outputs from reusable workflows propagate back to the caller. They reduce duplication across team repositories.

**Detailed Steps:**

1. Create `.github/workflows/reusable-deploy.yml` (the reusable workflow):

```yaml
name: Reusable Deploy Workflow

# The workflow_call trigger makes this reusable
on:
  workflow_call:
    inputs:
      environment:
        description: 'Target deployment environment'
        type: string
        required: true
      version:
        description: 'Application version to deploy'
        type: string
        default: 'latest'
      dry-run:
        description: 'Perform a dry run without actual deployment'
        type: boolean
        default: false
    secrets:
      deploy-token:
        description: 'Token for deployment authentication'
        required: true
    outputs:
      deploy-url:
        description: 'URL of the deployed application'
        value: ${{ jobs.deploy.outputs.url }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    outputs:
      url: ${{ steps.do-deploy.outputs.deploy-url }}
    steps:
      - name: Validate inputs
        run: |
          echo "Environment: ${{ inputs.environment }}"
          echo "Version    : ${{ inputs.version }}"
          echo "Dry run    : ${{ inputs.dry-run }}"
          echo "Token set  : ${{ secrets.deploy-token != '' }}"

      - name: Perform deployment
        id: do-deploy
        run: |
          if [ "${{ inputs.dry-run }}" == "true" ]; then
            echo "DRY RUN — no actual deployment"
          else
            echo "Deploying ${{ inputs.version }} to ${{ inputs.environment }}"
          fi
          # Set the output URL
          URL="https://${{ inputs.environment }}.example.com"
          echo "deploy-url=${URL}" >> $GITHUB_OUTPUT

      - name: Post-deploy verification
        run: |
          echo "Deployment to ${{ inputs.environment }} complete"
          echo "Accessible at: https://${{ inputs.environment }}.example.com"
```

2. Create `.github/workflows/main-pipeline.yml` (the caller workflow):

```yaml
name: Main CI/CD Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.v }}
    steps:
      - uses: actions/checkout@v4
      - id: version
        run: echo "v=1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT

  deploy-staging:
    needs: build
    # Call the reusable workflow — same repo, current branch
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: staging
      version: ${{ needs.build.outputs.version }}
      dry-run: false
    secrets:
      deploy-token: ${{ secrets.GITHUB_TOKEN }}

  deploy-production:
    needs: deploy-staging
    uses: ./.github/workflows/reusable-deploy.yml
    with:
      environment: production
      version: ${{ needs.build.outputs.version }}
      dry-run: false
    secrets:
      deploy-token: ${{ secrets.GITHUB_TOKEN }}

  summary:
    runs-on: ubuntu-latest
    needs: [deploy-staging, deploy-production]
    if: always()
    steps:
      - run: |
          echo "Staging URL   : ${{ needs.deploy-staging.outputs.deploy-url }}"
          echo "Production URL: ${{ needs.deploy-production.outputs.deploy-url }}"
```

3. Push to `main` and observe both deploy jobs calling the same reusable workflow.
4. Verify the `deploy-url` output propagates from the reusable workflow back to the caller.

**Success Criteria:**

- [ ] Reusable workflow runs as two separate jobs (staging and production).
- [ ] `deploy-url` output from reusable workflow appears in `summary` job.
- [ ] Secret `deploy-token` is accessible inside the reusable workflow.
- [ ] Input `dry-run: true` skips actual deployment.

**Troubleshooting:**

- **`uses: ./.github/workflows/` fails** → The `./.github/workflows/` shorthand only works within the same repository. For cross-repo calls use `owner/repo/.github/workflows/file.yml@ref`.
- **Outputs from reusable workflow are empty** → The output must be declared in the reusable workflow's `on.workflow_call.outputs:` AND mapped in the job's `outputs:` key.

**Time Estimate:** 30–35 minutes

</details>

---

<details>
<summary>🔗 Scenario 17-B: Cross-Repository Reusable Workflows and Versioning</summary>


**Objective:** Call a reusable workflow from a different repository, understand `secrets: inherit`, and implement version pinning with tags.

**Prerequisites:** Scenario 17-A complete; a second GitHub repository (or use a public repo).

**Context:** Reusable workflows can be called from any repository if the workflow file is in a public repo, or within the same organization for private repos. Versioning with tags (`@v1`, `@v1.2.3`) is critical for stability. `secrets: inherit` passes all caller secrets to the reusable workflow without explicitly mapping each one.

**Detailed Steps:**

1. Create a caller that uses `secrets: inherit`:

```yaml
name: Cross-Repo Reusable Workflow Call

on: workflow_dispatch

jobs:
  call-shared-workflow:
    # Call from same repo but pinned to a specific tag
    uses: ./.github/workflows/reusable-deploy.yml@main
    with:
      environment: development
      version: '1.0.0'
      dry-run: true
    # Pass ALL caller secrets to the reusable workflow
    # Avoid if you want explicit control over which secrets are shared
    secrets: inherit
```

2. Create a Git tag and push it:

```bash
git tag -a v1.0.0 -m "Release 1.0.0"
git push origin v1.0.0
```

3. Update the caller to pin to the tag:

```yaml
    uses: ./.github/workflows/reusable-deploy.yml@v1.0.0
```

4. Verify the tag-pinned call runs the exact version of the workflow at that tag.

**Success Criteria:**

- [ ] `secrets: inherit` passes all secrets without explicit mapping.
- [ ] Tag-pinned call (`@v1.0.0`) uses the workflow file at that specific commit.
- [ ] Modifying the workflow on `main` does not affect tag-pinned callers.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 18 (Custom Actions) for the parallel concept of action versioning with major version tags (`@v1`); → Section 22 (Enterprise) for organization-level starter workflows that call shared reusable workflows.

</details>

---

<details>
<summary>🐛 Scenario 17-C: Debug Reusable Workflow Issues and Dependency Chains</summary>


**Objective:** Troubleshoot common reusable workflow failures (circular calls, missing outputs, timeout).

**Prerequisites:** Scenario 17-B complete.

**Context:** Complex dependency chains can hide failures. Debugging them systematically prevents production outages.

**Detailed Steps:**

1. Identify circular dependency calls that could hang workflows
2. Validate outputs are declared and mapped correctly
3. Add logging to trace workflow execution across boundaries

**Success Criteria:**

- [ ] Identify circular dependency
- [ ] Fix output mapping issues
- [ ] Workflow completes successfully

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for troubleshooting techniques.

</details>

---

<details>
<summary>🔀 Scenario 17-D: Conditional Reusable Workflow Calls</summary>


**Objective:** Call reusable workflows conditionally based on inputs, secrets, or context.

**Prerequisites:** Scenario 17-C complete.

**Context:** Not all deployments use the same workflow. Conditional calls optimize workflow execution.

**Detailed Steps:**

1. Call reusable workflow only if a specific secret exists
2. Call based on branch or event type
3. Skip deployment workflow if tests failed

**Success Criteria:**

- [ ] Conditional calls execute correctly
- [ ] Can skip workflows based on conditions
- [ ] Deployment only on successful tests

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 9 (Conditional Logic) for conditional syntax.

</details>

---

<details>
<summary>🏢 Scenario 17-E: Reusable Workflow Patterns for Enterprise (Matrix, Approval, Secrets)</summary>


**Objective:** Implement enterprise-scale patterns: matrix builds, approval gates, centralized secrets.

**Prerequisites:** Scenario 17-D complete.

**Context:** Large organizations need consistent patterns across teams. Reusable workflows enforce this at scale.

**Detailed Steps:**

1. Create reusable workflow with matrix strategy
2. Add manual approval step before deployment
3. Use centralized environment secrets

**Success Criteria:**

- [ ] Matrix reusable workflow works
- [ ] Approval gates execute correctly
- [ ] Secrets inherited properly

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 10 (Matrix) for parallel builds.

</details>

---

### 🧩 Section 18: Custom Actions - Types & Structure (2 hrs)

**Topics:** [15-Creating-Publishing-Actions.md](15-Creating-Publishing-Actions.md) (first half)
**Overview:**

- Action types: JavaScript, Docker, Composite
- Action metadata: action.yml structure
- Inputs and outputs definitions
- Branding and descriptions
- Running vs docker container execution
- Composite actions: combining existing actions
- Action composition patterns

**Practical Work:**

- Create a composite action combining multiple actions
- Build a JavaScript action with inputs/outputs
- Define action.yml metadata properly
- Test action with workflow
- Document action usage


<details>
<summary>🧩 Scenario 18-A: Build a Composite Action</summary>


**Objective:** Create a composite action that combines multiple steps into a reusable action, with typed inputs, outputs, and proper `action.yml` metadata.

**Prerequisites:** Sections 1–17 complete; understanding of YAML and shell scripting.

**Context:** Composite actions contain multiple `run` steps and can reference other actions. They are the simplest action type — no JavaScript or Docker required. They live in a directory within your repo (or a dedicated action repo) and are referenced via their path in workflows. Unlike reusable workflows, actions run as steps within a job.

**Detailed Steps:**

1. Create the action directory and `action.yml`:

```bash
mkdir -p .github/actions/setup-and-validate
```

2. Create `.github/actions/setup-and-validate/action.yml`:

```yaml
name: 'Setup and Validate'
description: 'Sets up Node.js, installs dependencies, and validates the project'
author: 'your-github-username'

branding:
  icon: 'check-circle'
  color: 'green'

inputs:
  node-version:
    description: 'Node.js version to use'
    required: false
    default: '20'
  working-directory:
    description: 'Directory containing package.json'
    required: false
    default: '.'
  run-tests:
    description: 'Whether to run tests after setup'
    required: false
    default: 'true'

outputs:
  node-version-installed:
    description: 'The actual Node.js version that was installed'
    value: ${{ steps.node-info.outputs.version }}
  validation-passed:
    description: 'Whether validation passed (true/false)'
    value: ${{ steps.validate.outputs.passed }}

runs:
  using: composite  # composite action type
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}

    - name: Capture installed Node version
      id: node-info
      shell: bash
      run: echo "version=$(node --version)" >> $GITHUB_OUTPUT

    - name: Install dependencies
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        if [ -f package-lock.json ]; then
          npm ci
        elif [ -f package.json ]; then
          npm install
        else
          echo "No package.json found — skipping install"
        fi

    - name: Validate project
      id: validate
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        # Run lint if available
        if npm run lint --if-present 2>/dev/null; then
          echo "passed=true" >> $GITHUB_OUTPUT
        else
          echo "passed=true" >> $GITHUB_OUTPUT  # pass even without lint
        fi
        echo "Validation complete"

    - name: Run tests (if enabled)
      shell: bash
      if: inputs.run-tests == 'true'
      working-directory: ${{ inputs.working-directory }}
      run: npm test --if-present || echo "No tests defined"
```

3. Create `.github/workflows/use-composite-action.yml` to test it:

```yaml
name: Use Composite Action

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup and validate
        id: setup
        # Reference the action by its relative path
        uses: ./.github/actions/setup-and-validate
        with:
          node-version: '20'
          run-tests: 'false'

      - name: Use action outputs
        run: |
          echo "Node installed: ${{ steps.setup.outputs.node-version-installed }}"
          echo "Validation    : ${{ steps.setup.outputs.validation-passed }}"
```

4. Push and verify the composite action runs all its steps as part of the `build` job.

**Success Criteria:**

- [ ] Composite action steps appear as sub-steps in the job log.
- [ ] Outputs (`node-version-installed`, `validation-passed`) are readable in the calling workflow.
- [ ] `inputs.run-tests == 'false'` correctly skips the test step.
- [ ] Action branding metadata is defined in `action.yml`.

**Troubleshooting:**

- **`uses: ./.github/actions/...` fails** → The path must point to a directory containing `action.yml`. Verify the directory structure. Also, the action must be checked out first (`uses: actions/checkout@v4` before the action reference).
- **`shell: bash` required in composite actions** → Unlike regular workflow steps, composite action steps must explicitly specify `shell:` for `run:` steps.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🟨 Scenario 18-B: JavaScript Action with Inputs and Outputs</summary>


**Objective:** Create a minimal JavaScript action that processes an input string, returns an output, and handles errors using `@actions/core`.

**Prerequisites:** Scenario 18-A complete; Node.js installed locally; basic JavaScript knowledge.

**Context:** JavaScript actions run directly on the runner using Node.js. They use the `@actions/core` package for inputs/outputs, `@actions/github` for API access, and `@actions/exec` for subprocess execution. The `node_modules` must be committed or the code pre-bundled with `ncc`.

**Detailed Steps:**

1. Create the action directory:

```bash
mkdir -p .github/actions/string-transformer
cd .github/actions/string-transformer
npm init -y
npm install @actions/core
```

2. Create `.github/actions/string-transformer/action.yml`:

```yaml
name: 'String Transformer'
description: 'Transforms an input string to uppercase or lowercase'
inputs:
  input-string:
    description: 'The string to transform'
    required: true
  transform:
    description: 'Transformation: uppercase or lowercase'
    required: false
    default: 'uppercase'
outputs:
  result:
    description: 'The transformed string'
runs:
  using: node20
  main: index.js
```

3. Create `.github/actions/string-transformer/index.js`:

```javascript
const core = require('@actions/core');

try {
  const inputString = core.getInput('input-string', { required: true });
  const transform = core.getInput('transform');

  core.debug(`Processing: "${inputString}" with transform: ${transform}`);

  let result;
  if (transform === 'uppercase') {
    result = inputString.toUpperCase();
  } else if (transform === 'lowercase') {
    result = inputString.toLowerCase();
  } else {
    core.setFailed(`Unknown transform: ${transform}. Use 'uppercase' or 'lowercase'`);
    process.exit(1);
  }

  core.setOutput('result', result);
  core.info(`Transformed result: ${result}`);
} catch (error) {
  core.setFailed(error.message);
}
```

4. Commit `index.js`, `action.yml`, and `node_modules` (necessary for JS actions without `ncc`).

5. Test in a workflow:

```yaml
      - uses: ./.github/actions/string-transformer
        id: transform
        with:
          input-string: 'hello from github actions'
          transform: uppercase

      - run: echo "Result: ${{ steps.transform.outputs.result }}"
```

**Success Criteria:**

- [ ] Action outputs `HELLO FROM GITHUB ACTIONS`.
- [ ] Invalid `transform` value causes `core.setFailed()` to mark the step red.
- [ ] `core.debug()` messages appear with `ACTIONS_STEP_DEBUG=true`.

**Time Estimate:** 25–30 minutes

**Cross-Section Connections:** → Section 19 (Publishing Actions) for distributing this action to the Marketplace with versioned releases; → Section 24 (Security) for pinning third-party actions to commit SHAs.

</details>

---

<details>
<summary>🖥️ Scenario 18-C: Composite Actions with Script Files and OS Detection</summary>


**Objective:** Create composite actions that use multiple scripts and detect the runner OS.

**Prerequisites:** Scenario 18-B complete.

**Context:** Composite actions can run across Windows, Linux, macOS. OS detection enables platform-specific commands.

**Detailed Steps:**

1. Create scripts/windows.ps1 and scripts/linux.sh
2. Use `runner.os` to select correct script
3. Call composite action from workflow

**Success Criteria:**

- [ ] Composite action works on multiple OSes
- [ ] Script selection based on OS
- [ ] Scripts execute successfully

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 20 (Runners) for OS-specific considerations.

</details>

---

<details>
<summary>⚠️ Scenario 18-D: Error Handling and Validation in Custom Actions</summary>


**Objective:** Implement robust error handling, input validation, and failure reporting in actions.

**Prerequisites:** Scenario 18-C complete.

**Context:** Production actions must handle edge cases and provide clear error messages.

**Detailed Steps:**

1. Validate inputs (required, format, range)
2. Add try-catch for error handling
3. Use `core.setFailed()` for clear error messages
4. Add exit codes and logging

**Success Criteria:**

- [ ] Input validation works
- [ ] Errors reported clearly
- [ ] Workflow stops on critical errors

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 24 (Security) for secure input handling.

</details>

---

<details>
<summary>🐳 Scenario 18-E: Containerized Actions with Docker</summary>


**Objective:** Build Docker container-based actions for complex workflows.

**Prerequisites:** Scenario 18-D complete; Docker knowledge.

**Context:** Container actions allow any language and complex dependencies. Useful for non-Node.js tools.

**Detailed Steps:**

1. Create Dockerfile for action
2. Build and test locally
3. Reference in action.yml: `runs: using: 'docker'`
4. Use image in workflow

**Success Criteria:**

- [ ] Docker image builds
- [ ] Action runs in container
- [ ] Can pass inputs/outputs

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 20 (Runners) for container environment.

</details>

---

### 📢 Section 19: Custom Actions - Distribution & Publishing (2 hrs)

**Topics:** [15-Creating-Publishing-Actions.md](15-Creating-Publishing-Actions.md) (second half)
**Overview:**

- Public vs private action distribution
- Publishing to GitHub Marketplace
- Action versioning and release management
- Marketplace discovery and best practices
- Major version tagging (@v1, @v2)
- Committing node_modules or using ncc bundler
- Action testing and CI/CD

**Practical Work:**

- Prepare action for marketplace publication
- Implement versioning strategy
- Create action releases
- Test action from marketplace
- Document action in README


<details>
<summary>🏪 Scenario 19-A: Prepare an Action for Marketplace Publishing</summary>


**Objective:** Add all required metadata to `action.yml`, write comprehensive README documentation, and create a versioned release following the major-version-tag convention.

**Prerequisites:** Sections 1–18 complete; a completed composite or JavaScript action (from Section 18); the action must be in its own repository for Marketplace publishing.

**Context:** GitHub Marketplace listing requirements: `action.yml` must include `name`, `description`, `branding` (icon + color), and `author`. The repository must be public. The README appears on the Marketplace listing page. The convention for action releases is major version tags (`v1`, `v2`) that float to the latest minor/patch, allowing users to reference `@v1` without needing to track patch releases.

**Detailed Steps:**

1. Ensure `action.yml` has all required fields:

```yaml
name: 'Setup and Validate'
description: 'Sets up Node.js, installs dependencies, validates, and optionally runs tests. Saves setup time by combining common CI steps.'
author: 'your-github-username'

branding:
  icon: 'check-circle'   # icon from feathericons.com
  color: 'green'          # blue, green, gray, orange, purple, red, yellow, or white

inputs:
  node-version:
    description: 'Node.js version to install (e.g., 18, 20, 22)'
    required: false
    default: '20'
  working-directory:
    description: 'Working directory containing package.json'
    required: false
    default: '.'
  run-tests:
    description: 'Set to "true" to run npm test after setup'
    required: false
    default: 'false'

outputs:
  node-version-installed:
    description: 'The exact Node.js version installed (e.g., v20.11.0)'
    value: ${{ steps.node-info.outputs.version }}

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
    - id: node-info
      shell: bash
      run: echo "version=$(node --version)" >> $GITHUB_OUTPUT
    - shell: bash
      run: npm ci
```

2. Create `README.md` with usage documentation:

```markdown
## Setup and Validate Action

Sets up Node.js, installs dependencies, validates, and optionally runs tests.

### Usage

    - uses: your-username/setup-and-validate-action@v1
      with:
        node-version: '20'
        run-tests: 'true'

### Inputs

| Input | Required | Default | Description |
|-------|----------|---------|-------------|
| `node-version` | No | `20` | Node.js version to install |
| `working-directory` | No | `.` | Directory containing package.json |
| `run-tests` | No | `false` | Run npm test after setup |

### Outputs

| Output | Description |
|--------|-------------|
| `node-version-installed` | Exact Node.js version installed |

### Example

    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - id: setup
            uses: your-username/setup-and-validate-action@v1
          - run: echo "Node ${{ steps.setup.outputs.node-version-installed }}"
```

3. Create the action test workflow in the action's repository:

```yaml
name: Test Action

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Test action with defaults
        uses: ./  # test the action at the current repo root
        id: default-test

      - name: Verify output
        run: |
          VERSION="${{ steps.default-test.outputs.node-version-installed }}"
          echo "Installed: $VERSION"
          if [[ "$VERSION" =~ ^v[0-9]+ ]]; then
            echo "Output format is valid"
          else
            echo "Invalid output format: $VERSION"
            exit 1
          fi
```

4. Create the release and versioned tags:

```bash
# Create a full semantic version tag
git tag -a v1.0.0 -m "Initial release v1.0.0"
git push origin v1.0.0

# Create/move the floating major version tag
git tag -fa v1 -m "Update v1 to v1.0.0"
git push origin v1 --force
```

**Success Criteria:**

- [ ] `action.yml` contains all required branding fields.
- [ ] README documents all inputs and outputs in a table.
- [ ] Self-test workflow passes using `uses: ./`.
- [ ] Both `v1.0.0` and `v1` tags exist and point to the same commit.

**Troubleshooting:**

- **Marketplace listing not appearing** → Repository must be public and `action.yml` must be at the root of the repository (not in `.github/actions/`).
- **`git tag -fa` fails** → The `--force` flag requires the remote to allow force-push to tags. Use `git push origin :refs/tags/v1 && git push origin v1` as an alternative.

**Time Estimate:** 30–35 minutes

</details>

---

<details>
<summary>🚀 Scenario 19-B: Automated Action Release Workflow</summary>


**Objective:** Create a workflow that automatically moves the major version tag on release publication, following GitHub's recommended action versioning pattern.

**Detailed Steps:**

1. Create `.github/workflows/release-action.yml`:

```yaml
name: Release Action

on:
  release:
    types: [published]

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # required to create and update tags
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # fetch all history for tag operations

      - name: Extract major version
        id: version
        run: |
          TAG="${{ github.ref_name }}"  # e.g., v1.2.3
          MAJOR="${TAG%%.*}"            # extract v1
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "major=${MAJOR}" >> $GITHUB_OUTPUT
          echo "Release tag : $TAG"
          echo "Major tag   : $MAJOR"

      - name: Update major version tag
        run: |
          MAJOR="${{ steps.version.outputs.major }}"
          TAG="${{ steps.version.outputs.tag }}"
          git tag -d $MAJOR 2>/dev/null || true
          git push origin :refs/tags/$MAJOR 2>/dev/null || true
          git tag $MAJOR
          git push origin $MAJOR
          echo "Major tag $MAJOR now points to $TAG"

      - name: Update CHANGELOG
        run: |
          echo "## ${{ steps.version.outputs.tag }} — $(date +%Y-%m-%d)" >> CHANGELOG.md
          echo "See release notes: ${{ github.event.release.html_url }}" >> CHANGELOG.md
```

**Success Criteria:**

- [ ] Creating a `v1.2.3` GitHub Release automatically moves `v1` to that commit.
- [ ] Old `v1` tag is deleted and recreated at the new release SHA.
- [ ] Workflow requires `contents: write` permission.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 18 (Custom Actions) for building the action being published; → Section 24 (Security) for why users should pin to full SHA instead of mutable major tags for security-critical workflows.

</details>

---

<details>
<summary>🏷️ Scenario 19-C: Version Management and Semantic Versioning for Actions</summary>


**Objective:** Implement semantic versioning for actions with major version tags and release branches.

**Prerequisites:** Scenario 19-B complete.

**Context:** Version management prevents breaking users. Semantic versioning (major.minor.patch) enables safe updates.

**Detailed Steps:**

1. Tag releases with semantic versions: v1.0.0, v1.1.0, v2.0.0
2. Maintain major version tag: v1 always points to latest v1.x
3. Create release branches for backports
4. Users pin to v1 for automatic updates within major version

**Success Criteria:**

- [ ] Semantic versioning implemented
- [ ] Major version tag tracks latest
- [ ] Users can upgrade safely

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 18 (Actions) for versioning concepts.

</details>

---

<details>
<summary>📖 Scenario 19-D: Marketplace Certification and Documentation</summary>


**Objective:** Prepare action for Marketplace: README, badges, examples, proper documentation.

**Prerequisites:** Scenario 19-C complete.

**Context:** Marketplace certification ensures discoverability and user trust.

**Detailed Steps:**

1. Write comprehensive README
2. Add usage examples for common scenarios
3. Include license file
4. Verify action metadata is complete

**Success Criteria:**

- [ ] README is clear and complete
- [ ] Examples cover main use cases
- [ ] License included
- [ ] Can submit to Marketplace

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 1 (Fundamentals) for action best practices.

</details>

---

<details>
<summary>🛡️ Scenario 19-E: Security Hardening for Published Actions</summary>


**Objective:** Harden published actions against security threats (dependency vulnerabilities, secret exposure).

**Prerequisites:** Scenario 19-D complete.

**Context:** Published actions must protect user secrets and maintain security.

**Detailed Steps:**

1. Scan action dependencies for CVEs
2. Never log secrets or user data
3. Pin dependencies to specific versions
4. Regular security audits

**Success Criteria:**

- [ ] No high-severity CVEs
- [ ] No secrets in logs
- [ ] Dependencies pinned
- [ ] Security policy documented

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 24 (Security) for hardening techniques.

</details>

---

## 🖥️ Week 6: Runners & Enterprise

### 🏃 Section 20: GitHub-Hosted Runners (2 hrs)

**Topics:** [16-Managing-Runners.md](16-Managing-Runners.md) (first half)
**Overview:**

- GitHub-hosted runner types (Ubuntu, Windows, macOS)
- Available runner images and preinstalled software
- Runner labels and selection
- Image updates and deprecations (Ubuntu 20.04, Windows Server 2025)
- Runner specifiers: runs-on syntax
- Concurrency limits and availability
- Hardware resources (CPU, memory, disk)

**Practical Work:**

- Create workflows targeting different runner types
- Use runner labels for specific requirements
- Check runner toolcache availability
- Handle runner image changes and updates
- Test workflow compatibility across runners


<details>
<summary>🏃 Scenario 20-A: Target Different Runner Types and Verify Environments</summary>


**Objective:** Create a matrix workflow that runs across all three GitHub-hosted OS types, verifies the preinstalled tools, and demonstrates runner-specific behavior.

**Prerequisites:** Sections 1–19 complete.

**Context:** GitHub provides `ubuntu-latest`, `windows-latest`, and `macos-latest` runners. Each has a different toolcache, different path conventions (`/` vs `\`), and different default shells (`bash` vs `pwsh`). Testing across runners catches cross-platform incompatibilities early.

**Detailed Steps:**

1. Create `.github/workflows/runner-matrix.yml`:

```yaml
name: Runner Type Matrix

on: [push, workflow_dispatch]

jobs:
  explore-runners:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: OS information
        run: |
          echo "Runner OS    : ${{ runner.os }}"
          echo "Runner arch  : ${{ runner.arch }}"
          echo "Runner name  : ${{ runner.name }}"
          echo "Temp dir     : ${{ runner.temp }}"
          echo "Tool cache   : ${{ runner.tool_cache }}"

      - name: Check common tools (bash-compatible)
        if: runner.os != 'Windows'
        run: |
          echo "=== Node.js ==="
          node --version
          npm --version
          echo "=== Python ==="
          python3 --version
          echo "=== Docker ==="
          docker --version || echo "Docker not available on macOS"
          echo "=== Git ==="
          git --version

      - name: Check common tools (PowerShell)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Write-Host "=== Node.js ==="
          node --version
          npm --version
          Write-Host "=== Python ==="
          python --version
          Write-Host "=== Git ==="
          git --version

      - name: Cross-platform file operations
        run: |
          echo "Creating test file"
          mkdir -p test-output
          echo "Hello from ${{ runner.os }}" > test-output/runner-info.txt
          cat test-output/runner-info.txt

      - name: Upload runner info artifact
        uses: actions/upload-artifact@v4
        with:
          name: runner-info-${{ matrix.os }}
          path: test-output/

  compare-results:
    needs: explore-runners
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: runner-info-*
          merge-multiple: false

      - name: Compare outputs
        run: |
          echo "=== Runner Outputs ==="
          for dir in runner-info-*/; do
            echo "--- $dir ---"
            cat "${dir}runner-info.txt"
          done
```

2. Push and observe the three parallel jobs in the Actions UI.
3. Review that the artifact content differs per OS.

**Success Criteria:**

- [ ] All three OS types run in parallel.
- [ ] Windows uses `shell: pwsh` syntax.
- [ ] Per-OS artifacts contain OS-specific content.
- [ ] `runner.os`, `runner.arch`, and `runner.tool_cache` are populated correctly.

**Troubleshooting:**

- **`docker --version` fails on macOS** → macOS runners don't include Docker by default. Use `ubuntu-latest` for Docker-dependent steps.
- **Path separator issues on Windows** → Use `${{ runner.temp }}` instead of hardcoded paths, and use forward slashes `/` in most contexts.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>📌 Scenario 20-B: Runner Image Pinning and Toolcache Inspection</summary>


**Objective:** Pin to a specific runner image, inspect the toolcache, and simulate a runner image migration from `ubuntu-20.04` to `ubuntu-22.04`.

**Detailed Steps:**

1. Create `.github/workflows/runner-pinning.yml`:

```yaml
name: Runner Pinning Demo

on: workflow_dispatch

jobs:
  pinned-runner:
    runs-on: ubuntu-22.04  # pin to specific image version
    steps:
      - name: Verify runner image
        run: |
          echo "=== OS Version ==="
          cat /etc/os-release | grep VERSION
          echo ""
          echo "=== Runner Image Details ==="
          cat /etc/lsb-release

      - name: Inspect toolcache
        run: |
          echo "=== Available Node.js versions in toolcache ==="
          ls $RUNNER_TOOL_CACHE/node/ 2>/dev/null || echo "No cached Node versions"
          echo ""
          echo "=== Available Python versions ==="
          ls $RUNNER_TOOL_CACHE/Python/ 2>/dev/null || echo "No cached Python versions"

      - name: Test specific tool version
        uses: actions/setup-node@v4
        with:
          node-version: '18'  # explicitly set to avoid relying on preinstalled version

      - name: Verify version
        run: node --version
```

**Success Criteria:**

- [ ] Runner runs on `ubuntu-22.04` specifically (not `ubuntu-latest`).
- [ ] Toolcache directory listing is inspected.
- [ ] `actions/setup-node@v4` successfully installs Node 18.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 21 (Self-Hosted Runners) for when GitHub-hosted runners don't have the required tools or network access; → Section 25 (Performance) for using toolcache to avoid repeated downloads.

</details>

---

<details>
<summary>⚙️ Scenario 20-C: Advanced Runner Selection and Constraints</summary>


**Objective:** Select runners based on labels, CPU, memory, OS specifications.

**Prerequisites:** Scenario 20-B complete.

**Context:** Complex jobs need specific runner configurations. Label-based selection ensures optimal performance.

**Detailed Steps:**

1. Select specific OS: `runs-on: ubuntu-20.04`
2. Use labels for multiple constraints: `[macos-latest, x64]`
3. Verify runner resources with `vmstat` or similar
4. Scale with matrix to test across runner types

**Success Criteria:**

- [ ] Specific OS selection works
- [ ] Label combinations select correctly
- [ ] Can verify runner specs

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 10 (Matrix) for testing multiple runners.

</details>

---

<details>
<summary>🐛 Scenario 20-D: Troubleshoot Runner Availability and Queuing</summary>


**Objective:** Diagnose why jobs queue too long or fail to find available runners.

**Prerequisites:** Scenario 20-C complete.

**Context:** Job queuing costs time and delays releases. Understanding runner limits prevents bottlenecks.

**Detailed Steps:**

1. Check runner availability in Actions settings
2. Monitor job queue time
3. Add additional runners if needed
4. Optimize job size to complete faster

**Success Criteria:**

- [ ] Identify runner queue time
- [ ] Find bottlenecks
- [ ] Optimize job configuration

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 25 (Performance) for optimization.

</details>

---

<details>
<summary>🏢 Scenario 20-E: GitHub-Hosted Runner Limits and Enterprise Strategies</summary>


**Objective:** Understand runner usage limits and implement strategies for large-scale automation.

**Prerequisites:** Scenario 20-D complete.

**Context:** Free plan has usage limits. Enterprise customers need optimization strategies.

**Detailed Steps:**

1. Check usage limits for plan tier
2. Monitor monthly usage
3. Optimize job time to stay within limits
4. Plan for multiple repos' combined usage

**Success Criteria:**

- [ ] Understand usage limits
- [ ] Monitor usage patterns
- [ ] Optimize to stay within limits

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 22 (Enterprise) for scaling strategies.

</details>

---

### 🖥️ Section 21: Self-Hosted Runners & Configuration (2 hrs)

**Topics:** [16-Managing-Runners.md](16-Managing-Runners.md) (second half)
**Overview:**

- Self-hosted runner installation and setup
- Runner groups and organization
- Runner labels and selection logic
- Network configuration and IP whitelisting
- Authentication and security considerations
- Runner auto-scaling and orchestration
- Health checks and monitoring

**Practical Work:**

- Set up a self-hosted runner locally
- Configure runner groups and labels
- Implement IP allow lists
- Test runner selection with specific labels
- Monitor runner health and performance


<details>
<summary>🖥️ Scenario 21-A: Register and Use a Self-Hosted Runner</summary>


**Objective:** Register a self-hosted runner on your local machine (or a VM), assign it custom labels, and run a workflow that targets it via label selection.

**Prerequisites:** Sections 1–20 complete; a Linux/macOS/Windows machine or VM with internet access; repository admin access.

**Context:** Self-hosted runners allow you to run workflows on your own infrastructure — useful for private network access, specialized hardware, faster storage, or cost optimization. The runner agent connects to GitHub via HTTPS (outbound only, no inbound port needed).

**Detailed Steps:**

1. Navigate to **Repository → Settings → Actions → Runners → New self-hosted runner**.
2. Choose your OS and follow the displayed instructions. The setup is:

```bash
# Create a directory for the runner
mkdir -p ~/actions-runner && cd ~/actions-runner

# Download the runner package (example for Linux x64 — use URL from the UI)
curl -o actions-runner-linux-x64.tar.gz -L \
  https://github.com/actions/runner/releases/download/v2.317.0/actions-runner-linux-x64-2.317.0.tar.gz

tar xzf ./actions-runner-linux-x64.tar.gz

# Configure (use the token from the UI — it expires in 60 minutes)
./config.sh \
  --url https://github.com/YOUR_USERNAME/YOUR_REPO \
  --token YOUR_RUNNER_REGISTRATION_TOKEN \
  --name my-local-runner \
  --labels self-hosted,local,fast-disk \
  --work _work

# Start the runner interactively (for testing)
./run.sh
```

3. Verify the runner appears as **Idle** in the repository settings.

4. Create `.github/workflows/self-hosted-demo.yml`:

```yaml
name: Self-Hosted Runner Demo

on: workflow_dispatch

jobs:
  on-self-hosted:
    # Target by labels — all labels must match
    runs-on: [self-hosted, local]
    steps:
      - uses: actions/checkout@v4

      - name: Runner identity
        run: |
          echo "Hostname     : $(hostname)"
          echo "Working dir  : $(pwd)"
          echo "Available CPUs: $(nproc)"
          echo "Available RAM : $(free -h | grep Mem | awk '{print $2}')"
          echo "Disk space    : $(df -h . | tail -1 | awk '{print $4}') free"

      - name: Access local network resource
        run: |
          # Self-hosted runners can access internal network resources
          # that GitHub-hosted runners cannot
          echo "Private network access available from: $(hostname -I 2>/dev/null || echo 'N/A')"

      - name: Cleanup workspace
        if: always()
        run: |
          echo "Workspace at: ${{ github.workspace }}"
          # Self-hosted runners reuse workspaces by default
          # Clean up to avoid stale files
          find . -name "*.tmp" -delete 2>/dev/null || true
```

5. Trigger via `workflow_dispatch` and verify the job runs on your local machine.
6. Check the runner's `_work/` directory to see the workspace.

**Success Criteria:**

- [ ] Runner appears as Idle in repository settings after `./config.sh`.
- [ ] `workflow_dispatch` trigger runs the job on your local machine.
- [ ] `hostname` command shows your machine's hostname, not a GitHub runner hostname.
- [ ] Job completes and runner returns to Idle state.

**Troubleshooting:**

- **Runner goes Offline immediately** → The runner process isn't running. Start it with `./run.sh` or install as a service with `./svc.sh install && ./svc.sh start`.
- **Job queued but not picked up** → Verify the label in `runs-on:` exactly matches a label assigned during `config.sh`. Labels are case-sensitive.

**Time Estimate:** 30–35 minutes

</details>

---

<details>
<summary>👥 Scenario 21-B: Runner Groups and Organization-Level Runners</summary>


**Objective:** Understand runner groups for restricting which repositories can use which runners (concepts and configuration — requires organization access).

**Context:** Runner groups control access: only specified repositories can target runners in a group. This is critical for enterprise security — you don't want a compromised fork workflow to run on production infrastructure runners.

**Detailed Steps (conceptual with runnable workflow):**

1. In an organization: **Settings → Actions → Runner groups → New runner group**.
2. Name the group (e.g., `production-runners`), restrict to selected repositories.
3. Move your runner into the group.

4. The workflow targets a group-restricted runner:

```yaml
name: Organization Runner Group Demo

on: workflow_dispatch

jobs:
  deploy:
    # Targets runners in the 'production-runners' group
    # (label-based targeting also works within groups)
    runs-on: [self-hosted, production]
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Verify production environment
        run: |
          echo "Running on production runner group"
          echo "Runner name: ${{ runner.name }}"

      - name: Deploy to production
        run: |
          echo "Production deployment from approved runner"
```

5. Attempt to run the same workflow from a fork — verify it's blocked.

**Success Criteria:**

- [ ] Runner group restricts access to specified repositories only.
- [ ] Workflow from unauthorized repository is queued but never starts.
- [ ] Audit log shows the access denial.

**Time Estimate:** 15–20 minutes (plus setup time for organization access)

**Cross-Section Connections:** → Section 22 (Enterprise) for organization-level runner policies; → Section 24 (Security) for the security implications of runner compromise in CI/CD pipelines.

</details>

---

<details>
<summary>📈 Scenario 21-C: Self-Hosted Runner Scaling and Management</summary>


**Objective:** Scale self-hosted runners for high-volume workflows using runner groups.

**Prerequisites:** Scenario 21-B complete.

**Context:** Multiple self-hosted runners improve throughput. Groups organize runners by team or purpose.

**Detailed Steps:**

1. Add multiple runners to same org/repo
2. Create runner groups for different teams
3. Assign specific runners to specific workflows
4. Monitor runner health and availability

**Success Criteria:**

- [ ] Multiple runners available
- [ ] Groups created and assignable
- [ ] Workflows use specific runners

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 25 (Performance) for scaling strategies.

</details>

---

<details>
<summary>🛡️ Scenario 21-D: Self-Hosted Runner Security Hardening</summary>


**Objective:** Secure self-hosted runners against malicious workflows and exfiltration.

**Prerequisites:** Scenario 21-C complete.

**Context:** Self-hosted runners run untrusted code. Security hardening prevents compromise.

**Detailed Steps:**

1. Use read-only file systems where possible
2. Isolate runners by network
3. Enable audit logging
4. Limit secrets access

**Success Criteria:**

- [ ] Security controls implemented
- [ ] No unnecessary access to secrets
- [ ] Logs maintained

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 24 (Security) for hardening.

</details>

---

<details>
<summary>🔧 Scenario 21-E: Self-Hosted Runner Maintenance and Auto-Update</summary>


**Objective:** Keep self-hosted runners updated and functioning optimally.

**Prerequisites:** Scenario 21-D complete.

**Context:** Outdated runners have security vulnerabilities and performance issues.

**Detailed Steps:**

1. Auto-update runner software
2. Monitor disk space and cleanup
3. Rotate logs regularly
4. Update OS and dependencies

**Success Criteria:**

- [ ] Runners auto-update
- [ ] Disk space managed
- [ ] Logs rotated
- [ ] All critical patches applied

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 25 (Performance) for maintenance.

</details>

---

### 🏢 Section 22: Enterprise-Level Management (2 hrs)

**Topics:** [17-GitHub-Actions-Enterprise.md](17-GitHub-Actions-Enterprise.md)
**Overview:**

- Enterprise policy management
- Reusable workflows at enterprise scale
- Organization-level workflow templates
- Starter workflows for enterprise teams
- Usage policies: allow, deny, required
- Cost allocation and cleanup policies
- Audit logging and compliance tracking

**Practical Work:**

- Create organization-level reusable workflows
- Set up starter templates for standard CI/CD
- Configure access policies for workflows
- Implement audit logging
- Establish cost management practices


<details>
<summary>🏢 Scenario 22-A: Organization Starter Workflows</summary>


**Objective:** Create an organization-level starter workflow that appears in the "Actions" tab of new repositories, providing a standardized CI template.

**Prerequisites:** Sections 1–21 complete; organization owner or admin access.

**Context:** Starter workflows are stored in a special `.github` repository within an organization. They appear as template options when a new workflow is created in any repo in the org. They can include reusable workflow calls, enforcing standards across all projects.

**Detailed Steps:**

1. Create (or use) a `.github` repository in your organization.
2. Create the directory structure:

```text
.github/
  workflow-templates/
    node-ci.yml
    node-ci.properties.json
```

3. Create `.github/workflow-templates/node-ci.yml`:

```yaml
name: Node.js CI

on:
  push:
    branches: [$default-branch]
  pull_request:
    branches: [$default-branch]

# Organization standard: require explicit permissions
permissions:
  contents: read
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-${{ matrix.node-version }}
          path: coverage/
```

4. Create `.github/workflow-templates/node-ci.properties.json`:

```json
{
  "name": "Node.js CI",
  "description": "Build and test a Node.js project with npm — Org Standard",
  "iconName": "example-icon",
  "categories": [
    "Continuous integration",
    "JavaScript"
  ]
}
```

5. Verify the template appears in a new repository under **Actions → New workflow**.

**Success Criteria:**

- [ ] Template appears in the org's "Actions → New workflow" screen.
- [ ] Template includes `$default-branch` placeholder.
- [ ] `properties.json` provides correct name and description.

**Troubleshooting:**

- **Template not appearing** → The `.github` repository must be named exactly `.github` (lowercase). The files must be in `workflow-templates/` at the root.
- **`$default-branch` not replaced** → This is a literal template variable replaced by GitHub when the user applies the template. It works only as a starter workflow, not when run directly.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>📋 Scenario 22-B: Organization Policy Management and Audit Logging</summary>


**Objective:** Configure which actions are allowed in an organization, set required workflows, and query audit logs for workflow activity.

**Context:** Organization owners can restrict which actions run: allow all, allow GitHub actions only, allow selected marketplaces, or specify exact patterns. Required workflows enforce certain checks on all repositories in the org.

**Detailed Steps:**

1. Configure action policies (conceptual — requires org admin):
   - **Organization → Settings → Actions → General → Actions permissions**
   - Set: "Allow enterprise, and select non-enterprise, actions and reusable workflows"
   - In the "Allow specified actions and reusable workflows" box, specify a policy:

```text
# Allow only GitHub-owned actions and specific third-party actions:
actions/*,
github/*,
docker/*@v2,
aws-actions/configure-aws-credentials@*
```

2. Query audit logs via GitHub API:

```yaml
name: Audit Log Query

on: workflow_dispatch

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Query recent Actions audit events
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Query GraphQL audit log (requires org:read or audit_log:read scope)
          echo "Querying audit log for Actions events..."
          gh api graphql -f query='
            query {
              organization(login: "YOUR_ORG") {
                auditLog(first: 10, query: "action:workflows") {
                  nodes {
                    ... on WorkflowsWorkflow {
                      actor
                      action
                      createdAt
                    }
                  }
                }
              }
            }
          ' --jq '.data.organization.auditLog.nodes[]'
```

3. Review what a `required_workflow` looks like when configured via the API:

```yaml
      - name: List required workflows
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Required workflows are org-level policies
          gh api /orgs/YOUR_ORG/actions/required_workflows \
            --jq '.required_workflows[] | "\(.name): \(.workflow_file_path)"'
```

**Success Criteria:**

- [ ] Action allow-list prevents unauthorized third-party actions from running.
- [ ] Audit log query returns workflow-related events.
- [ ] Required workflow concept is understood and documented.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 21 (Runner Groups) for org-level runner access control; → Section 24 (Security) for the security rationale behind action policy restrictions.

</details>

---

<details>
<summary>📄 Scenario 22-C: Enterprise Starter Workflows and Templates</summary>


**Objective:** Create and distribute starter workflows across enterprise teams.

**Prerequisites:** Scenario 22-B complete.

**Context:** Standardized workflows ensure consistency and compliance across teams.

**Detailed Steps:**

1. Create `.github` repository with `workflow-templates/`
2. Add starter workflow with `properties.json`
3. Distribute to teams and measure adoption

**Success Criteria:**

- [ ] Starter workflow available to teams
- [ ] Adoption tracked
- [ ] Teams using templates

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for shared workflow patterns.

</details>

---

<details>
<summary>📊 Scenario 22-D: Enterprise Audit and Compliance Reporting</summary>


**Objective:** Generate audit reports for compliance (SOC2, ISO, etc.) from GitHub Actions audit logs.

**Prerequisites:** Scenario 22-C complete.

**Context:** Compliance requires documentation of workflow execution and changes.

**Detailed Steps:**

1. Query audit logs for actions
2. Generate compliance report
3. Archive for audits

**Success Criteria:**

- [ ] Audit logs queried
- [ ] Reports generated
- [ ] Compliance documented

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 27 (Job Summaries) for reporting.

</details>

---

<details>
<summary>🌐 Scenario 22-E: Multi-Organization Workflow Management</summary>


**Objective:** Manage workflows consistently across multiple organizations.

**Prerequisites:** Scenario 22-D complete.

**Context:** Large enterprises span multiple orgs. Central policies ensure consistency.

**Detailed Steps:**

1. Implement cross-org action policies
2. Distribute shared templates
3. Monitor compliance across orgs

**Success Criteria:**

- [ ] Policies consistent across orgs
- [ ] Templates shared
- [ ] Compliance monitored

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 17 (Reusable Workflows) for distribution.

</details>

---

## 🔒 Week 7: Security & Optimization

### 🛡️ Section 23: GitHub Actions Security Best Practices - Part 1 (2 hrs)

**Topics:** [18-Security-and-Optimization.md](18-Security-and-Optimization.md) (first half)
**Overview:**

- Principle of least privilege for permissions
- GITHUB_TOKEN: scope, ephemeral nature, permissions
- Environment protection rules and approval gates
- Script injection prevention techniques
- Input sanitization and validation
- Avoiding untrusted data in shell commands
- Token security and credential management

**Practical Work:**

- Review GITHUB_TOKEN permissions
- Implement least-privilege permissions in workflows
- Sanitize user inputs to prevent injection
- Set up approval gates for sensitive deployments
- Audit workflow permissions


<details>
<summary>🛡️ Scenario 23-A: Least-Privilege GITHUB_TOKEN and Input Sanitization</summary>


**Objective:** Implement explicit minimal permissions, prevent script injection from untrusted PR data, and set up environment approval gates.

**Prerequisites:** Sections 1–22 complete; understanding of the `permissions:` key (Section 7).

**Context:** The most critical security control in GitHub Actions is preventing script injection. This occurs when untrusted data (PR titles, issue bodies, commit messages from forks) is interpolated directly into `run:` shell commands using `${{ github.event.pull_request.title }}`. The attacker can craft a PR title containing `"; cat /etc/passwd; echo "` to exfiltrate data.

**Detailed Steps:**

1. Create `.github/workflows/secure-permissions.yml`:

```yaml
name: Secure Permissions Demo

on:
  pull_request:
    types: [opened, synchronize]

# Default: restrict all permissions — grant only what's needed
permissions:
  contents: read  # checkout only

jobs:
  pr-validation:
    runs-on: ubuntu-latest
    # Override job-level if different from workflow-level
    permissions:
      contents: read
      pull-requests: write  # needed to post PR comments
    steps:
      - uses: actions/checkout@v4

      # BAD PATTERN — VULNERABLE TO INJECTION
      # Do NOT do this:
      # - run: echo "PR title is ${{ github.event.pull_request.title }}"

      # GOOD PATTERN — safe interpolation via environment variable
      - name: Safe use of PR metadata
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
          PR_AUTHOR: ${{ github.event.pull_request.user.login }}
        run: |
          # Now $PR_TITLE is an environment variable, not shell-interpolated
          echo "PR Author: $PR_AUTHOR"
          echo "PR Title : $PR_TITLE"
          # Safe to use in shell because it's an env var, not inline expression
          if [[ "$PR_TITLE" =~ ^\[BREAKING\] ]]; then
            echo "Breaking change detected — requires senior review"
            exit 1
          fi

      - name: Validate PR branch naming
        env:
          BRANCH_NAME: ${{ github.head_ref }}
        run: |
          # Validate branch name follows convention
          if [[ ! "$BRANCH_NAME" =~ ^(feature|bugfix|hotfix|chore)/ ]]; then
            echo "::error::Branch name '$BRANCH_NAME' does not follow naming convention"
            echo "Expected: feature/, bugfix/, hotfix/, or chore/"
            exit 1
          fi
          echo "Branch name valid: $BRANCH_NAME"

      - name: Post PR comment
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr comment ${{ github.event.number }} \
            --repo ${{ github.repository }} \
            --body "PR validation passed. Branch naming convention verified."
```

2. Test by creating a PR from a branch named `feature/my-new-feature`.
3. Test with a branch named `bad-branch-name` and observe the error.

**Success Criteria:**

- [ ] Workflow has explicit `permissions:` at both workflow and job level.
- [ ] PR title/body are accessed via environment variables (not inline expressions in `run:`).
- [ ] Branch naming validation catches non-conforming names.
- [ ] `pull-requests: write` allows the comment step to succeed.

**Troubleshooting:**

- **`gh pr comment` fails with "Resource not accessible by integration"** → The `pull-requests: write` permission must be explicitly set. For fork PRs, this permission is automatically restricted to read-only.
- **Environment variable approach doesn't work for matrix** → For matrix-interpolated values, use `env:` at the step level to safely pass matrix values to shell.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🔐 Scenario 23-B: Environment Protection Rules and Approval Gates</summary>


**Objective:** Configure a protected environment requiring manual approval before deployment, and test that the approval gate blocks automated deployment.

**Detailed Steps:**

1. Navigate to **Repository → Settings → Environments → New environment** → name it `production`.
2. Configure protection rules:
   - Required reviewers: add yourself
   - Wait timer: 5 minutes (optional, for testing)
   - Deployment branches: restrict to `main` only

3. Create `.github/workflows/protected-deploy.yml`:

```yaml
name: Protected Deployment

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.v }}
    steps:
      - uses: actions/checkout@v4
      - id: ver
        run: echo "v=1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT

  deploy-staging:
    needs: build
    runs-on: ubuntu-latest
    environment: staging  # no protection rules
    steps:
      - run: echo "Deployed ${{ needs.build.outputs.version }} to staging"

  deploy-production:
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: production  # PROTECTED — requires approval
    permissions:
      contents: read
      id-token: write  # for OIDC if needed
    steps:
      - run: echo "Deployed ${{ needs.build.outputs.version }} to production"

      - name: Record deployment
        run: |
          echo "Version : ${{ needs.build.outputs.version }}"
          echo "Actor   : ${{ github.actor }}"
          echo "SHA     : ${{ github.sha }}"
```

4. Push to `main` and verify:
   - Staging deploys automatically
   - Production is **waiting for approval** in the Actions UI
   - After approval, production deploy proceeds

**Success Criteria:**

- [ ] Staging deploys without approval.
- [ ] Production shows "Waiting for approval" in the Actions UI.
- [ ] After approval by a required reviewer, production deploy completes.
- [ ] Deployment from non-`main` branches is blocked.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 8 (Environments) for full environment configuration; → Section 24 (Security Part 2) for OIDC-based credential security replacing long-lived secrets.

</details>

---

<details>
<summary>🔄 Scenario 23-C: Secret Rotation and Expiration Policies</summary>


**Objective:** Implement automated secret rotation and set expiration policies.

**Prerequisites:** Scenario 23-B complete.

**Context:** Secrets should rotate regularly. Expiration prevents stale credentials from being exploited.

**Detailed Steps:**

1. Automate secret rotation every 90 days
2. Set expiration on organization secrets
3. Notify teams of upcoming expiration

**Success Criteria:**

- [ ] Secrets rotate automatically
- [ ] Expiration enforced
- [ ] Teams notified

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 22 (Enterprise) for org-level secret management.

</details>

---

<details>
<summary>🔑 Scenario 23-D: OIDC Token-Based Authentication</summary>


**Objective:** Replace long-lived secrets with OIDC tokens for cloud provider authentication.

**Prerequisites:** Scenario 23-C complete.

**Context:** OIDC eliminates long-lived credentials, improving security posture significantly.

**Detailed Steps:**

1. Configure OIDC provider (AWS, Azure, GCP)
2. Use `actions/id-token@v2` to get token
3. Exchange token for short-lived credentials

**Success Criteria:**

- [ ] OIDC configured
- [ ] Token obtained
- [ ] Short-lived creds exchanged

**Time Estimate:** 25 minutes

**Cross-Section Connections:** → Section 8 (Environments) for integration.

</details>

---

<details>
<summary>🔍 Scenario 23-E: Supply Chain Security and Dependency Scanning</summary>


**Objective:** Scan dependencies for vulnerabilities and implement SBOM for supply chain security.

**Prerequisites:** Scenario 23-D complete.

**Context:** Compromised dependencies pose supply chain risks. Scanning prevents exploitation.

**Detailed Steps:**

1. Add dependency scanning in workflow
2. Generate SBOM (Software Bill of Materials)
3. Fail on high-severity CVEs

**Success Criteria:**

- [ ] Dependencies scanned
- [ ] SBOM generated
- [ ] High CVEs blocked

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 13 (Caching) for dependency management.

</details>

---

### 🔒 Section 24: GitHub Actions Security Best Practices - Part 2 (2 hrs)

**Topics:** [18-Security-and-Optimization.md](18-Security-and-Optimization.md) (second half)
**Overview:**

- Action pinning: commit SHA vs tags vs fuzzy versions
- Verifying action sources and trustworthiness
- OIDC tokens for cloud provider federation
- Artifact attestation and provenance (SLSA, build metadata)
- Deployment verification workflows
- Secrets scanning and rotation
- Vulnerability scanning in workflows

**Practical Work:**

- Pin third-party actions to commit SHAs
- Implement OIDC for AWS/Azure scenarios
- Verify artifact provenance
- Set up secrets scanning
- Implement dependency scanning


<details>
<summary>📌 Scenario 24-A: Pin Actions to Commit SHAs and Implement OIDC</summary>


**Objective:** Convert all action references from mutable tags to immutable commit SHAs, and implement OIDC federation with a cloud provider to eliminate long-lived credentials.

**Prerequisites:** Sections 1–23 complete; AWS or Azure account (or use simulated OIDC steps).

**Context:** Pinning to a commit SHA (`uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683`) prevents supply chain attacks where a malicious actor takes over an action's GitHub account and pushes malicious code to the `@v4` tag. OIDC (OpenID Connect) allows workflows to authenticate to cloud providers using short-lived, automatically rotated tokens — no secrets stored in GitHub.

**Detailed Steps:**

1. Create a workflow demonstrating SHA pinning vs tag pinning:

```yaml
name: Security Best Practices

on: [push]

# Minimal permissions at workflow level
permissions: {}

jobs:
  secure-workflow:
    runs-on: ubuntu-latest
    permissions:
      contents: read      # for checkout
      id-token: write     # for OIDC token request

    steps:
      # BAD: mutable tag — can be changed by action author
      # - uses: actions/checkout@v4

      # GOOD: pinned to exact commit SHA
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      # GOOD: also pin third-party actions to SHA
      - uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af  # v4.1.0
        with:
          node-version: '20'

      - name: Show commit SHA verification
        run: |
          echo "Current commit: ${{ github.sha }}"
          echo "Actions are pinned to immutable SHAs"

      # OIDC: Request a token from GitHub and exchange for cloud provider credentials
      - name: Configure AWS credentials via OIDC
        # In production: uses: aws-actions/configure-aws-credentials@SHA
        # Here we simulate the OIDC token request
        run: |
          echo "Requesting OIDC token..."
          # The id-token permission allows this API call
          OIDC_TOKEN=$(curl -sS \
            -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=sts.amazonaws.com" \
            | jq -r '.value')
          echo "OIDC token acquired (length: ${#OIDC_TOKEN})"
          echo "Token payload (claims):"
          echo $OIDC_TOKEN | cut -d. -f2 | base64 -d 2>/dev/null | jq . || echo "Decode requires padding"
```

2. To find the SHA for any action:

```bash
# Method 1: From GitHub API
gh api repos/actions/checkout/git/ref/tags/v4 --jq '.object.sha'

# Method 2: From the Actions Marketplace page — click the tag, copy SHA from URL
# Method 3: From the repo commit history
```

3. Configure OIDC trust (concept — requires cloud provider):

```text
# AWS: IAM OIDC Identity Provider
# Provider URL: https://token.actions.githubusercontent.com
# Audience: sts.amazonaws.com
#
# Trust policy condition:
# "token.actions.githubusercontent.com:sub": "repo:ORG/REPO:ref:refs/heads/main"
```

**Success Criteria:**

- [ ] All `uses:` references include a SHA comment showing the equivalent tag.
- [ ] `id-token: write` permission enables OIDC token request.
- [ ] OIDC token is successfully acquired and its claims are decoded.
- [ ] `permissions: {}` at workflow level ensures jobs must explicitly opt-in.

**Troubleshooting:**

- **`$ACTIONS_ID_TOKEN_REQUEST_TOKEN` is empty** → The `id-token: write` permission must be set at the job level, not just workflow level. Also, this only works on GitHub-hosted runners by default.
- **SHA pinning breaks `dependabot`** → Configure Dependabot for GitHub Actions: it can automatically update pinned SHA references.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🔍 Scenario 24-B: Artifact Attestation and Dependency Scanning</summary>


**Objective:** Generate an artifact attestation (provenance) for a build artifact and scan workflow dependencies for vulnerabilities.

**Detailed Steps:**

1. Create `.github/workflows/attestation.yml`:

```yaml
name: Build with Attestation

on: [push]

permissions:
  contents: read
  id-token: write     # required for attestation signing
  attestations: write # required to write attestations

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Build artifact
        run: |
          echo "Build output" > my-artifact.tar.gz
          sha256sum my-artifact.tar.gz

      - name: Generate artifact attestation
        uses: actions/attest-build-provenance@210c1913531a0be71a26e72c5be3c7ea1c43f47f  # v2.0.0
        with:
          subject-path: my-artifact.tar.gz

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: my-artifact
          path: my-artifact.tar.gz
```

2. After the run, verify the attestation:

```bash
gh attestation verify my-artifact.tar.gz \
  --repo OWNER/REPO
```

3. Add a dependency scanning step:

```yaml
      - name: Scan for dependency vulnerabilities
        run: |
          # Using npm audit for Node.js projects
          npm audit --audit-level=high || true
          # Or: using Trivy for container/filesystem scanning
          # trivy fs --exit-code 1 --severity HIGH,CRITICAL .
```

**Success Criteria:**

- [ ] `attest-build-provenance` action creates a provenance attestation in the repository.
- [ ] `gh attestation verify` confirms the artifact's integrity.
- [ ] `attestations: write` permission is required and set.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 19 (Publishing Actions) for understanding why action consumers should pin to SHAs; → Section 22 (Enterprise) for org-level policies requiring SHA pinning.

</details>

---

<details>
<summary>🔒 Scenario 24-C: Secrets Scanning and Prevention of Accidental Leaks</summary>


**Objective:** Implement secret scanning to detect and prevent accidental credential commits.

**Prerequisites:** Scenario 24-B complete.

**Context:** Accidental secret commits compromise security. Scanning catches them before push.

**Detailed Steps:**

1. Enable secret scanning in repository
2. Add custom patterns for internal secrets
3. Block commits with secrets detected

**Success Criteria:**

- [ ] Secret scanning enabled
- [ ] Custom patterns work
- [ ] Commits blocked with secrets

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 23 (Security Part 1) for secrets management.

</details>

---

<details>
<summary>🔄 Scenario 24-D: Dependency Updates with Automated Security Fixes</summary>


**Objective:** Implement Dependabot for automated dependency updates and security fixes.

**Prerequisites:** Scenario 24-C complete.

**Context:** Stale dependencies have known vulnerabilities. Automation keeps projects updated.

**Detailed Steps:**

1. Enable Dependabot in repository settings
2. Configure update frequency
3. Auto-merge security updates

**Success Criteria:**

- [ ] Dependabot PRs created
- [ ] Security updates prioritized
- [ ] Auto-merge configured

**Time Estimate:** 15 minutes

**Cross-Section Connections:** → Section 13 (Caching) for dependency management.

</details>

---

<details>
<summary>🔬 Scenario 24-E: Code Analysis and SAST Integration</summary>


**Objective:** Integrate CodeQL for static analysis and security scanning.

**Prerequisites:** Scenario 24-D complete.

**Context:** SAST catches security issues early. CodeQL is GitHub's advanced analysis tool.

**Detailed Steps:**

1. Add CodeQL workflow
2. Configure for your language
3. Review and act on findings

**Success Criteria:**

- [ ] CodeQL runs in workflow
- [ ] Finds security issues
- [ ] Results reviewed

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 25 (Performance) for analysis efficiency.

</details>

---

### ⚡ Section 25: Workflow Performance Optimization (2 hrs)

**Topics:** [10-Workflow-Caching.md](10-Workflow-Caching.md), [18-Security-and-Optimization.md](18-Security-and-Optimization.md) (optimization section)
**Overview:**

- Identifying performance bottlenecks
- Caching strategy for build artifacts
- Parallel job execution and concurrency
- Matrix configuration optimization
- Container image layer caching
- Artifact size management
- Retention policy optimization for cost

**Practical Work:**

- Profile slow workflows
- Implement caching improvements
- Reduce artifact sizes
- Parallelize jobs appropriately
- Measure performance improvements


<details>
<summary>📊 Scenario 25-A: Profile and Optimize a Slow Workflow</summary>


**Objective:** Create a deliberately slow workflow, measure step durations, implement caching and parallelization, then compare before/after timings.

**Prerequisites:** Sections 1–24 complete; Section 9 (Caching) knowledge.

**Context:** The GitHub Actions UI shows step duration in the job log, but doesn't provide a summary view. By adding timing steps and using workflow run data, you can identify bottlenecks. Key optimizations: caching (Section 9), parallel jobs, `needs:` dependency reduction, and concurrency controls.

**Detailed Steps:**

1. Create a "slow baseline" workflow to measure:

```yaml
name: Performance Baseline

on: workflow_dispatch

jobs:
  slow-sequential:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: TIMER START
        run: echo "Start: $(date +%s)" > /tmp/start.txt

      - name: Simulate: Install dependencies (slow, no cache)
        run: |
          # Simulate uncached npm install
          sleep 15  # typical uncached install time
          echo "Dependencies installed"

      - name: Simulate: Lint
        run: sleep 10

      - name: Simulate: Test suite A
        run: sleep 20

      - name: Simulate: Test suite B
        run: sleep 20

      - name: TIMER END
        run: |
          START=$(cat /tmp/start.txt | cut -d: -f2)
          END=$(date +%s)
          echo "Total time: $((END - START)) seconds"
```

2. Run and note the total time (~65+ seconds due to sequential steps).

3. Create the optimized version:

```yaml
name: Performance Optimized

on: workflow_dispatch

jobs:
  # Install dependencies and cache them
  setup:
    runs-on: ubuntu-latest
    outputs:
      cache-hit: ${{ steps.cache.outputs.cache-hit }}
    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install if cache missed
        if: steps.cache.outputs.cache-hit != 'true'
        run: sleep 15  # simulated install

  # Lint and test suites run in parallel
  lint:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      - run: sleep 10  # simulated lint

  test-a:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      - run: sleep 20  # simulated test suite A

  test-b:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
      - run: sleep 20  # simulated test suite B

  done:
    needs: [lint, test-a, test-b]
    runs-on: ubuntu-latest
    steps:
      - run: echo "All checks passed — parallel execution complete"
```

4. Compare run times: the optimized version runs lint and both test suites in parallel, reducing wall-clock time from ~65s to ~35s (setup + max of parallel jobs).

**Success Criteria:**

- [ ] Baseline workflow takes ~65 seconds.
- [ ] Optimized workflow takes ~35 seconds (setup 15s + parallel 20s = ~35s vs 65s).
- [ ] Cache hit on second run reduces setup to ~2 seconds.
- [ ] GitHub Actions UI shows parallel job execution in the dependency graph view.

**Troubleshooting:**

- **Cache key produces no hit** → Ensure `package-lock.json` exists. The `hashFiles()` function returns an empty string if no files match, causing cache misses.
- **Parallel jobs have no shared cache** → Each parallel job must restore the cache independently. The cache is shared across jobs by key.

**Time Estimate:** 30–35 minutes

</details>

---

<details>
<summary>⚙️ Scenario 25-B: Concurrency Controls and Artifact Size Optimization</summary>


**Objective:** Implement concurrency groups to cancel in-progress runs on new pushes, and reduce artifact retention and size.

**Detailed Steps:**

```yaml
name: Concurrency Optimized CI

on:
  push:
    branches: [main, 'feature/**']
  pull_request:

# Cancel in-progress runs when a new push arrives on the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true  # for feature branches — not for main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build application
        run: |
          echo "Building..."
          mkdir -p dist
          echo "Build output $(date)" > dist/app.js
          echo "More output" >> dist/app.js

      - name: Upload minimal artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: dist/
          # Reduce storage costs — keep for 1 day on PRs, 30 days on main
          retention-days: ${{ github.ref == 'refs/heads/main' && 30 || 1 }}
          # Compress artifacts to reduce size
          compression-level: 9
          # Fail if artifact is empty (catches build failures silently outputting nothing)
          if-no-files-found: error
```

**Success Criteria:**

- [ ] Rapid pushes to the same branch cancel the in-progress run.
- [ ] `retention-days` varies based on branch.
- [ ] `if-no-files-found: error` catches empty build outputs.

**Time Estimate:** 15–20 minutes

**Cross-Section Connections:** → Section 9 (Caching) for the deep dive on cache keys and restore keys; → Section 14 (Matrix) for parallelizing tests across Node.js versions.

</details>

---

<details>
<summary>⚡ Scenario 25-C: Parallel Job Execution and Matrix Optimization</summary>


**Objective:** Maximize parallelization with matrix builds to reduce total workflow time.

**Prerequisites:** Scenario 25-B complete.

**Context:** Matrix multiplies job execution. Optimization reduces overall workflow time significantly.

**Detailed Steps:**

1. Create matrix strategy across Node versions and OS
2. Identify optimal matrix size (balance parallelism vs runner limits)
3. Measure total workflow time reduction

**Success Criteria:**

- [ ] Matrix creates parallel jobs
- [ ] Workflow time reduced by >50%
- [ ] Runner limits respected

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 10 (Matrix) for matrix strategy details.

</details>

---

<details>
<summary>🔬 Scenario 25-D: Workflow Runtime Profiling and Bottleneck Analysis</summary>


**Objective:** Profile workflow execution to identify and eliminate bottlenecks.

**Prerequisites:** Scenario 25-C complete.

**Context:** Data-driven optimization eliminates slow steps most effectively.

**Detailed Steps:**

1. Add timing logs to critical sections
2. Identify slowest steps
3. Optimize highest-impact steps first

**Success Criteria:**

- [ ] Timing data collected
- [ ] Bottlenecks identified
- [ ] Top 3 steps optimized

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for logging patterns.

</details>

---

<details>
<summary>💰 Scenario 25-E: Cost Optimization Across Workflow Portfolio</summary>


**Objective:** Optimize total workflow costs across hundreds of workflows.

**Prerequisites:** Scenario 25-D complete.

**Context:** Small optimizations multiply across many workflows, yielding significant savings.

**Detailed Steps:**

1. Audit workflow durations across repository
2. Identify patterns of waste (redundant builds, long waits)
3. Consolidate similar workflows
4. Implement shared caching

**Success Criteria:**

- [ ] Workflows audited
- [ ] Waste identified
- [ ] Cost reduced by >30%

**Time Estimate:** 30 minutes

**Cross-Section Connections:** → Section 25 (Performance) for efficiency patterns.

</details>

---

### 🔦 Section 26: Troubleshooting & Common Failures - Part 2 (2 hrs)

**Topics:** [19-Common-Failures-Troubleshooting.md](19-Common-Failures-Troubleshooting.md) (second half)
**Overview:**

- Matrix job failure diagnosis
- Timeout and resource exhaustion
- Secret leakage and redaction
- Action versioning conflicts
- Environment variable scoping issues
- Caching invalidation problems
- Cross-platform compatibility issues
- Network and connectivity failures

**Practical Work:**

- Debug matrix job failures
- Identify and fix timeout issues
- Analyze logs for common patterns
- Test cross-platform workflows
- Implement recovery strategies


<details>
<summary>🐛 Scenario 26-A: Matrix Job Failure Diagnosis and Recovery</summary>


**Objective:** Create a matrix workflow with intentional failures, use `fail-fast: false` to collect all results, implement retry logic, and use `include`/`exclude` to fix matrix edge cases.

**Prerequisites:** Sections 1–25 complete; Section 14 (Matrix) knowledge.

**Context:** Matrix failures can be subtle: one OS or Node version fails while others pass. With `fail-fast: true` (default), all matrix jobs cancel when one fails, hiding information about which combinations are broken. Setting `fail-fast: false` lets all combinations run, revealing the full failure surface.

**Detailed Steps:**

1. Create `.github/workflows/matrix-debug.yml`:

```yaml
name: Matrix Debugging

on: workflow_dispatch

jobs:
  matrix-with-failures:
    strategy:
      fail-fast: false  # collect results from ALL matrix combinations
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node: [18, 20]
        # Exclude known incompatible combinations
        exclude:
          - os: windows-latest
            node: 18  # hypothetical incompatibility
        include:
          # Add extra properties to specific combinations
          - os: ubuntu-latest
            node: 20
            experimental: true  # mark as experimental

    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.experimental == true }}  # don't fail workflow for experimental

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Platform-specific test
        run: |
          echo "OS  : ${{ matrix.os }}"
          echo "Node: ${{ matrix.node }}"
          # Simulate: macOS + Node 20 sometimes fails
          if [[ "${{ matrix.os }}" == "macos-latest" && "${{ matrix.node }}" == "20" ]]; then
            echo "Simulating intermittent macOS failure"
            exit 1
          fi
          echo "Test passed"
        shell: bash

      - name: Summary output
        if: always()
        run: |
          echo "Matrix cell: ${{ matrix.os }} / Node ${{ matrix.node }}"
          echo "Status: ${{ job.status }}"
```

2. Run and observe in the Actions UI:
   - All non-excluded combinations run
   - `windows-latest/node18` is skipped (excluded)
   - `macos-latest/node20` fails but workflow continues
   - `experimental: true` combination failure doesn't fail the overall workflow

**Success Criteria:**

- [ ] `fail-fast: false` allows all matrix combinations to complete.
- [ ] Excluded matrix combination (`windows/node18`) does not appear in the run.
- [ ] `continue-on-error: true` for experimental job prevents workflow failure.
- [ ] Failure message clearly identifies which matrix combination failed.

**Troubleshooting:**

- **`continue-on-error` not working with dynamic expression** → `continue-on-error` supports expressions, but the value must be a boolean. Use `${{ matrix.experimental == true }}` rather than `${{ matrix.experimental }}`.
- **`shell: bash` required on Windows** → Windows runners use PowerShell by default. Add `shell: bash` to use bash syntax cross-platform.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🖥️ Scenario 26-B: Cross-Platform Compatibility and Log Analysis</summary>


**Objective:** Identify and fix common cross-platform issues (path separators, line endings, command availability) and use log analysis patterns to find failure root causes.

**Detailed Steps:**

1. Create a workflow that surfaces common cross-platform issues:

```yaml
name: Cross-Platform Compatibility

on: workflow_dispatch

jobs:
  cross-platform:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      # Issue 1: Path separators
      - name: Safe path handling
        run: |
          # Use GITHUB_WORKSPACE instead of hardcoded paths
          echo "Workspace: $GITHUB_WORKSPACE"
          # Avoid: /home/runner/work/repo/file.txt (Linux-specific)
          # Use: ${{ github.workspace }}/file.txt (cross-platform)
        shell: bash

      # Issue 2: Line endings (CRLF vs LF)
      - name: Check line endings
        run: |
          # Create a file and check its line endings
          printf "line1\nline2\n" > test.txt
          file test.txt || echo "file command not available"
        shell: bash

      # Issue 3: Command availability
      - name: Safe command usage
        run: |
          # sed, awk, grep available on all platforms when using bash
          echo "hello world" | grep -o "hello"
          echo "1+1" | awk '{print $0}'
        shell: bash

      # Issue 4: Environment variables
      - name: Cross-platform env vars
        run: |
          # Works on all platforms via bash
          export MY_VAR="cross-platform-value"
          echo "MY_VAR=$MY_VAR"
          echo "MY_VAR=${MY_VAR}" >> $GITHUB_ENV
        shell: bash

      - name: Verify env var persists
        run: echo "Persisted: $MY_VAR"
        shell: bash
```

2. Simulate a log analysis workflow that surfaces error patterns:

```yaml
      - name: Log analysis demonstration
        run: |
          # Common patterns to grep in CI logs:
          # 1. Error lines
          echo "Searching for errors..."
          echo "ERROR: connection refused" | grep -i "error\|fail\|exception" && echo "Found issues"
          # 2. Test failure counts
          echo "Tests: 42 passed, 3 failed" | grep -oP '\d+ failed'
          # 3. Slow step detection
          echo "Step completed in 145s" | awk '{ if ($NF > 60) print "SLOW:", $0 }'
        shell: bash
```

**Success Criteria:**

- [ ] All three OS types pass the cross-platform steps using `shell: bash`.
- [ ] Environment variable persists across steps via `$GITHUB_ENV`.
- [ ] Log analysis patterns identify error keywords.

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 15 (Troubleshooting Part 1) for debug logging; → Section 25 (Performance) for identifying slow steps via log analysis.

</details>

---

<details>
<summary>🔄 Scenario 26-C: Advanced Failure Pattern Analysis and Automated Recovery</summary>


**Objective:** Implement ML-style failure pattern detection to automatically recover from known failure modes.

**Prerequisites:** Scenario 26-B complete.

**Context:** Enterprise systems encounter recurring failure patterns. Automated recovery reduces manual intervention.

**Detailed Steps:**

1. Create workflow that logs all failures with structured metadata
2. Parse failure logs to extract error patterns (timeout, rate limit, auth, network)
3. Build decision tree: if pattern matches known failure → execute recovery
4. Test recovery flow end-to-end
5. Validate that retry succeeds after recovery

**Detailed Workflow Example:**

```yaml
name: Failure Pattern Analysis
on:
  workflow_dispatch:
jobs:
  detect-and-recover:
    runs-on: ubuntu-latest
    steps:
      - name: Simulate potential failure
        id: attempt
        continue-on-error: true
        run: |
          # Simulate different failure modes
          FAILURE_MODE=${{ github.event.inputs.failure_mode || 'timeout' }}
          case "$FAILURE_MODE" in
            timeout)
              sleep 65  # Trigger step timeout after 60s
              ;;
            rate-limit)
              for i in {1..50}; do curl -s https://api.github.com/user; done
              ;;
            auth)
              curl -H "Authorization: Bearer invalid" https://api.github.com/user
              ;;
          esac

      - name: Detect failure pattern
        id: pattern
        if: failure()
        run: |
          echo "failure_detected=true" >> $GITHUB_OUTPUT
          # In production: parse logs to detect specific failure
          if [[ "${{ job.status }}" == "failure" ]]; then
            echo "Detected: Step failed, analyzing pattern..."
            # Classify: timeout vs auth vs network
            echo "pattern=timeout" >> $GITHUB_OUTPUT
          fi

      - name: Execute recovery - timeout
        if: steps.pattern.outputs.pattern == 'timeout'
        run: |
          echo "Executing timeout recovery: splitting workload..."
          # Break long-running task into smaller chunks
          echo "Chunk 1 complete"
          echo "Chunk 2 complete"

      - name: Execute recovery - auth
        if: steps.pattern.outputs.pattern == 'auth'
        run: |
          echo "Executing auth recovery: refreshing credentials..."
          # Refresh tokens or re-authenticate
          echo "Credentials refreshed"

      - name: Verify recovery success
        run: echo "Recovery workflow executed successfully"
```

**Success Criteria:**

- [ ] Workflow detects and classifies failure pattern
- [ ] Recovery procedure executes automatically on match
- [ ] Retry after recovery succeeds
- [ ] Failure metadata logged for analysis

**Expected Output:**

```bash
Detected: Step failed, analyzing pattern...
pattern=timeout
Executing timeout recovery: splitting workload...
Chunk 1 complete
Chunk 2 complete
Recovery workflow executed successfully
```

**Troubleshooting:**

- **Pattern not detected:** Check log parsing logic; ensure failure metadata captured
- **Recovery doesn't match failure:** Add more pattern classification rules
- **Retry still fails:** Recovery procedure insufficient; escalate to manual intervention

**Time Estimate:** 30–35 minutes

**Cross-Section Connections:** → Section 9 (Conditional Logic) for if-conditions; → Section 15 (Debugging) for log analysis.

</details>

---

<details>
<summary>👁️ Scenario 26-D: Workflow Failure Prevention Through Proactive Monitoring</summary>


**Objective:** Implement proactive health checks to prevent failures before they occur.

**Prerequisites:** Scenario 26-C complete.

**Context:** Preventing failures is better than recovering from them. Proactive monitoring enables early intervention.

**Detailed Steps:**

1. Create scheduled workflow that checks system health
2. Monitor dependency availability (APIs, services, databases)
3. Alert if health checks fail
4. Automatically pause dependent workflows if dependencies unhealthy

**Detailed Workflow Example:**

```yaml
name: Proactive Health Monitoring
on:
  schedule:
    - cron: '*/30 * * * *'  # Every 30 minutes
  workflow_dispatch:
jobs:
  health-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check GitHub API health
        id: github-api
        continue-on-error: true
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            https://api.github.com/repos/${{ github.repository }})
          STATUS=$(echo "$RESPONSE" | tail -n 1)
          if [ "$STATUS" != "200" ]; then
            echo "health_status=unhealthy" >> $GITHUB_OUTPUT
            echo "failing_service=github-api" >> $GITHUB_OUTPUT
          else
            echo "health_status=healthy" >> $GITHUB_OUTPUT
          fi

      - name: Check external dependency
        id: external
        continue-on-error: true
        run: |
          RESPONSE=$(curl -s -w "\n%{http_code}" https://api.github.com/status)
          STATUS=$(echo "$RESPONSE" | tail -n 1)
          if [ "$STATUS" != "200" ]; then
            echo "health_status=unhealthy" >> $GITHUB_OUTPUT
            echo "failing_service=external-api" >> $GITHUB_OUTPUT
          fi

      - name: Broadcast health status
        run: |
          echo "Health Check Results:"
          echo "  GitHub API: ${{ steps.github-api.outputs.health_status }}"
          echo "  External API: ${{ steps.external.outputs.health_status }}"

      - name: Trigger alert if unhealthy
        if: failure()
        run: |
          echo "ALERT: Dependency health check failed"
          echo "Failing service: ${{ steps.github-api.outputs.failing_service || steps.external.outputs.failing_service }}"
          # In production: send to monitoring system (Slack, PagerDuty, etc.)
```

**Success Criteria:**

- [ ] Health checks run on schedule
- [ ] All dependencies monitored
- [ ] Alerts triggered on failure
- [ ] Workflow can pause dependent jobs

**Expected Output:**

```bash
Health Check Results:
  GitHub API: healthy
  External API: healthy
```

**Troubleshooting:**

- **Health checks too slow:** Implement parallel checks
- **False positives:** Adjust timeout and retry logic
- **Alerts not firing:** Verify notification channel configuration

**Time Estimate:** 25–30 minutes

**Cross-Section Connections:** → Section 5 (Triggers) for scheduled workflows; → Section 24 (Security) for API authentication.

</details>

---

<details>
<summary>🏢 Scenario 26-E: Enterprise-Scale Incident Response and Post-Mortems</summary>


**Objective:** Build incident management workflow for large-scale failures with automated post-mortem generation.

**Prerequisites:** Scenario 26-D complete.

**Context:** Enterprise systems require structured incident response and learning from failures.

**Detailed Steps:**

1. Define incident severity levels
2. Create escalation workflow based on severity
3. Auto-generate post-mortem from logs and metadata
4. Schedule blameless post-mortem review
5. Document lessons learned

**Detailed Workflow Example:**

```yaml
name: Enterprise Incident Response
on:
  workflow_run:
    workflows: [CI, Build, Deploy]
    types: [completed]
  workflow_dispatch:
    inputs:
      incident_severity:
        type: choice
        options: [low, medium, high, critical]
jobs:
  assess-incident:
    runs-on: ubuntu-latest
    steps:
      - name: Determine severity
        id: severity
        run: |
          # Assess from workflow failure context
          if [[ "${{ github.event.workflow_run.conclusion }}" == "failure" ]]; then
            # Simple heuristic: check how many jobs failed
            echo "severity=high" >> $GITHUB_OUTPUT
          else
            echo "severity=low" >> $GITHUB_OUTPUT
          fi

      - name: Escalate if critical
        if: steps.severity.outputs.severity == 'high' || steps.severity.outputs.severity == 'critical'
        run: |
          echo "INCIDENT ESCALATION"
          echo "Severity: ${{ steps.severity.outputs.severity }}"
          echo "Notifying on-call engineer..."
          # In production: PagerDuty API call

  generate-postmortem:
    needs: assess-incident
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Collect incident metadata
        run: |
          cat > incident.json <<EOF
          {
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "workflow_id": "${{ github.event.workflow_run.id }}",
            "workflow_name": "${{ github.event.workflow_run.name }}",
            "conclusion": "${{ github.event.workflow_run.conclusion }}",
            "branch": "${{ github.event.workflow_run.head_branch }}",
            "commit_sha": "${{ github.event.workflow_run.head_commit.id }}"
          }
          EOF
          cat incident.json

      - name: Generate post-mortem template
        run: |
          cat > POST_MORTEM.md <<'EOF'
          # Incident Post-Mortem

          ## Summary
          - **Date:** $(date -u +%Y-%m-%d)
          - **Duration:** [To be determined]
          - **Impact:** [Describe impact to users/services]

          ## Timeline
          - **T+0min:** Incident detected
          - **T+5min:** Alert fired
          - **T+10min:** On-call engineer engaged
          - **T+20min:** Root cause identified
          - **T+30min:** Service restored

          ## Root Cause Analysis
          [Investigation results]

          ## Contributing Factors
          - [Factor 1]
          - [Factor 2]

          ## Resolution Steps
          - [Step 1]
          - [Step 2]

          ## Lessons Learned
          - [What we learned]

          ## Action Items
          - [ ] Implement monitoring for [X]
          - [ ] Update runbook
          - [ ] Schedule training session

          ## Next Steps
          - Review in post-mortem meeting
          - Assign owners to action items
          - Track resolution
          EOF
          cat POST_MORTEM.md

      - name: Create post-mortem issue
        run: |
          echo "Post-mortem generated and ready for review"
          echo "Scheduled for team review in 24 hours"

  notify-team:
    needs: generate-postmortem
    runs-on: ubuntu-latest
    steps:
      - name: Send notification
        run: |
          echo "Team notification: Incident post-mortem prepared"
          echo "Next post-mortem review: [Scheduled time]"
          # In production: Send to Slack channel
```

**Success Criteria:**

- [ ] Incident severity assessed automatically
- [ ] Escalation triggered for high-severity incidents
- [ ] Post-mortem template generated from metadata
- [ ] Team notified
- [ ] Post-mortem scheduled for review

**Expected Output:**

```bash
INCIDENT ESCALATION
Severity: high
Notifying on-call engineer...
Team notification: Incident post-mortem prepared
```

**Troubleshooting:**

- **Severity assessment inaccurate:** Refine heuristics for classification
- **Notifications not sent:** Verify notification channel credentials
- **Post-mortem incomplete:** Add more metadata collection steps

**Time Estimate:** 35–40 minutes

**Cross-Section Connections:** → Section 22 (Enterprise Management) for org-scale processes; → Section 15 (Debugging) for log collection.

</details>

---

## 📊 Week 8: Job Summaries & Advanced Features

### 📊 Section 27: Job Summaries & Workflow Status (2 hrs)

**Topics:** [04-Workflow-File-Structure.md](04-Workflow-File-Structure.md) (job summaries section), [14-Reviewing-Deployments.md](14-Reviewing-Deployments.md)
**Overview:**

- GITHUB_STEP_SUMMARY for rich markdown reports
- Test result reporting in summaries
- Coverage reports and metrics
- Deployment workflow summaries
- Status badges and visualization
- Emergency check workflows
- Manual environment approvals

**Practical Work:**

- Generate test and coverage summaries
- Create formatted markdown reports in workflows
- Implement status badges
- Set up deployment review workflows
- Configure manual approvals


<details>
<summary>📊 Scenario 27-A: Rich Job Summaries with Test and Coverage Reports</summary>


**Objective:** Use `GITHUB_STEP_SUMMARY` to generate formatted markdown reports containing test results, coverage percentages, and deployment metadata — visible directly in the Actions UI without needing to download artifacts.

**Prerequisites:** Sections 1–26 complete; Section 12 (Artifacts) knowledge.

**Context:** `GITHUB_STEP_SUMMARY` is a special file path that accepts Markdown appended during workflow execution. The content appears in the Actions UI on the workflow run's Summary tab. This replaces the need for external reporting tools for common CI summaries, and unlike artifacts, summaries are retained as part of the run record.

**Detailed Steps:**

1. Create `.github/workflows/job-summary.yml`:

```yaml
name: Job Summary Demo

on: workflow_dispatch

jobs:
  test-with-summary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests (simulated)
        id: test-run
        run: |
          # Simulate test output
          echo "PASSED=42" >> $GITHUB_OUTPUT
          echo "FAILED=3"  >> $GITHUB_OUTPUT
          echo "SKIPPED=5" >> $GITHUB_OUTPUT
          echo "COVERAGE=78.5" >> $GITHUB_OUTPUT
          # Return non-zero if tests failed — but continue to write summary
          exit 0

      - name: Generate test summary
        if: always()  # run even if previous step failed
        run: |
          PASSED=${{ steps.test-run.outputs.PASSED }}
          FAILED=${{ steps.test-run.outputs.FAILED }}
          SKIPPED=${{ steps.test-run.outputs.SKIPPED }}
          COVERAGE=${{ steps.test-run.outputs.COVERAGE }}

          # Write markdown to GITHUB_STEP_SUMMARY
          cat >> $GITHUB_STEP_SUMMARY << 'EOF'
          ## 🧪 Test Results

          | Metric    | Value |
          |-----------|-------|
          | ✅ Passed  | 42    |
          | ❌ Failed  | 3     |
          | ⏭️ Skipped | 5     |
          | 📊 Coverage| 78.5% |

          ### Failed Tests
          - `auth/login.test.ts` — AssertionError: expected 401 got 403
          - `api/users.test.ts` — Timeout after 5000ms
          - `utils/format.test.ts` — TypeError: undefined is not a function

          > ⚠️ Coverage is below the 80% threshold.
          EOF

      - name: Generate coverage badge data
        run: |
          COVERAGE=78.5
          # Determine badge color
          if (( $(echo "$COVERAGE >= 80" | bc -l) )); then
            COLOR="brightgreen"
          elif (( $(echo "$COVERAGE >= 60" | bc -l) )); then
            COLOR="yellow"
          else
            COLOR="red"
          fi
          echo "Badge: https://img.shields.io/badge/coverage-${COVERAGE}%25-${COLOR}"

          # Append to summary
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## 📈 Coverage Report

          ![Coverage](https://img.shields.io/badge/coverage-${COVERAGE}%25-${COLOR})

          Coverage dropped below threshold (80%). Review uncovered files:
          - \`src/auth/oauth.ts\` — 45% covered
          - \`src/api/webhooks.ts\` — 52% covered
          EOF

  deployment-summary:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Simulate deployment
        id: deploy
        run: |
          echo "sha=${{ github.sha }}" >> $GITHUB_OUTPUT
          echo "version=1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT
          echo "Deployed successfully"

      - name: Write deployment summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## 🚀 Deployment Summary

          | Field    | Value |
          |----------|-------|
          | Version  | ${{ steps.deploy.outputs.version }} |
          | SHA      | \`${{ github.sha }}\` |
          | Actor    | ${{ github.actor }} |
          | Time     | $(date -u +"%Y-%m-%dT%H:%M:%SZ") |
          | Status   | ✅ Success |

          [View deployment logs](#) • [Rollback instructions](#)
          EOF
```

2. Run the workflow and navigate to the Actions UI → Summary tab to see the formatted reports.

**Success Criteria:**

- [ ] Summary tab shows the test results table with counts.
- [ ] Failed test names are listed with error types.
- [ ] Coverage percentage is rendered with the appropriate badge color.
- [ ] Deployment summary shows version, SHA, actor, and timestamp.
- [ ] `if: always()` ensures summary is written even if tests fail.

**Troubleshooting:**

- **Heredoc (`<< 'EOF'`) vs heredoc (`<< EOF`)** → Use `<< 'EOF'` (quoted) to prevent shell expansion inside the block. Use `<< EOF` (unquoted) when you need `$GITHUB_OUTPUT` values to expand.
- **Summary not appearing** → Ensure you're writing to `$GITHUB_STEP_SUMMARY` (the file path, not a variable). On Windows runners use `Add-Content $env:GITHUB_STEP_SUMMARY "content"`.

**Time Estimate:** 25–30 minutes

</details>

---

<details>
<summary>🔖 Scenario 27-B: Status Badges and Emergency Runbook Workflows</summary>


**Objective:** Add workflow status badges to a README and create an emergency runbook workflow that generates an HTML status summary.

**Detailed Steps:**

1. Add a status badge to `README.md`:

```markdown
# My Application

![CI Status](https://github.com/OWNER/REPO/actions/workflows/ci.yml/badge.svg)
![CD Status](https://github.com/OWNER/REPO/actions/workflows/deploy.yml/badge.svg?branch=main)
![Security Scan](https://github.com/OWNER/REPO/actions/workflows/security.yml/badge.svg)
```

2. Create an emergency status check workflow:

```yaml
name: Emergency Status Check

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to check'
        type: choice
        options: [staging, production]
        required: true

jobs:
  status-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check workflow statuses
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "Checking recent workflow runs..."
          gh run list \
            --repo ${{ github.repository }} \
            --limit 10 \
            --json name,status,conclusion,headBranch,createdAt \
            --jq '.[] | [.name, .status, (.conclusion // "in_progress"), .headBranch] | @tsv'

      - name: Write emergency summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## 🚨 Emergency Status Report
          **Environment:** ${{ inputs.environment }}
          **Checked by:** ${{ github.actor }}
          **Time:** $(date -u)

          ### Recent Workflow Status
          $(gh run list --repo ${{ github.repository }} --limit 5 \
            --json name,conclusion \
            --jq '.[] | "- " + .name + ": " + (.conclusion // "running")')
          EOF
        env:
          GH_TOKEN: ${{ github.token }}
```

**Success Criteria:**

- [ ] README badges show live workflow status.
- [ ] Emergency workflow accepts environment selection.
- [ ] Summary contains formatted status data from the GitHub API.

**Time Estimate:** 20 minutes

**Cross-Section Connections:** → Section 12 (Artifacts) for when summaries aren't sufficient and full reports need downloading; → Section 16 (Workflow REST API) for querying workflow run data via API.

</details>

---

<details>
<summary>🎨 Scenario 27-C: Custom Formatting and Structured Output in Job Summaries</summary>


**Objective:** Create rich, formatted job summaries with tables, charts, and custom styling.

**Prerequisites:** Scenario 27-B complete.

**Context:** Rich summaries improve visibility and enable better decision-making.

**Detailed Steps:**

1. Generate test results in JSON format
2. Parse JSON and create formatted summary with tables
3. Add Mermaid diagrams for visual representation
4. Include collapsible sections for detailed output
5. Format with emojis and markdown styling

**Detailed Workflow Example:**

```yaml
name: Custom Formatted Summary
on: push
jobs:
  test-and-summarize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        id: tests
        run: |
          # Simulate test run
          cat > test-results.json <<'EOF'
          {
            "total": 45,
            "passed": 42,
            "failed": 2,
            "skipped": 1,
            "duration_seconds": 125
          }
          EOF
          cat test-results.json

      - name: Generate custom summary
        run: |
          cat >> $GITHUB_STEP_SUMMARY <<'EOF'
          # 📊 Test Results Summary

          ## Quick Stats
          | Metric | Value |
          |--------|-------|
          | Total Tests | 45 |
          | ✅ Passed | 42 |
          | ❌ Failed | 2 |
          | ⏭️ Skipped | 1 |
          | ⏱️ Duration | 125s |

          ## Test Breakdown


          ### Failed Tests
          - `test_auth_token_refresh` - timeout
          - `test_cache_invalidation` - assertion error

          ### Skipped Tests
          - `test_performance_benchmark` - requires special config


          ## Performance Chart
          ```mermaid

          pie title Test Results
            "Passed (42)" : 42
            "Failed (2)" : 2
            "Skipped (1)" : 1

          ```bash

          ---
          **Generated at:** $(date -u +%Y-%m-%dT%H:%M:%SZ)
          EOF
```

**Success Criteria:**

- [ ] Summary contains formatted table
- [ ] Mermaid diagram displays correctly
- [ ] Collapsible sections expand/collapse
- [ ] Summary visible on workflow run page

**Expected Output:**

Formatted summary with table, pie chart, and collapsible sections

**Troubleshooting:**

- **Markdown not rendering:** Check syntax (tables need pipes, mermaid needs code fence)
- **Diagram not showing:** Verify mermaid syntax is valid
- **Emoji not displaying:** Use escaped unicode sequences

**Time Estimate:** 20–25 minutes

**Cross-Section Connections:** → Section 14 (Inter-Job Communication) for parsing job outputs; → Section 15 (Debugging) for log summaries.

</details>

---

<details>
<summary>📈 Scenario 27-D: Aggregating Metrics and Dashboards from Multiple Jobs</summary>


**Objective:** Collect metrics from parallel jobs and create unified dashboard views.

**Prerequisites:** Scenario 27-C complete.

**Context:** Multi-job workflows generate distributed metrics; aggregation enables holistic visibility.

**Detailed Steps:**

1. Each job exports metrics to artifact
2. Collector job reads all metric artifacts
3. Aggregate metrics across jobs
4. Generate unified dashboard view
5. Post dashboard to summary

**Detailed Workflow Example:**

```yaml
name: Multi-Job Metrics Aggregation
on: push
jobs:
  test-suite-1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          mkdir -p metrics
          echo '{"duration": 45, "tests": 20, "passed": 20}' > metrics/results.json
      - uses: actions/upload-artifact@v3
        with:
          name: metrics-suite-1
          path: metrics/

  test-suite-2:
    runs-on: ubuntu-latest
    steps:
      - run: |
          mkdir -p metrics
          echo '{"duration": 38, "tests": 18, "passed": 17}' > metrics/results.json
      - uses: actions/upload-artifact@v3
        with:
          name: metrics-suite-2
          path: metrics/

  aggregate-and-report:
    needs: [test-suite-1, test-suite-2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
        with:
          path: all-metrics

      - name: Aggregate metrics
        run: |
          TOTAL_DURATION=0
          TOTAL_TESTS=0
          TOTAL_PASSED=0

          for file in all-metrics/metrics-*/results.json; do
            DURATION=$(jq '.duration' "$file")
            TESTS=$(jq '.tests' "$file")
            PASSED=$(jq '.passed' "$file")

            TOTAL_DURATION=$((TOTAL_DURATION + DURATION))
            TOTAL_TESTS=$((TOTAL_TESTS + TESTS))
            TOTAL_PASSED=$((TOTAL_PASSED + PASSED))
          done

          echo "total_duration=$TOTAL_DURATION" >> $GITHUB_ENV
          echo "total_tests=$TOTAL_TESTS" >> $GITHUB_ENV
          echo "total_passed=$TOTAL_PASSED" >> $GITHUB_ENV

      - name: Generate aggregated dashboard
        run: |
          cat >> $GITHUB_STEP_SUMMARY <<'EOF'
          # 📊 Aggregated Test Dashboard

          ## Overall Metrics
          | Metric | Value |
          |--------|-------|
          | Total Test Time | ${{ env.total_duration }}s |
          | Total Tests | ${{ env.total_tests }} |
          | Total Passed | ${{ env.total_passed }} |
          | Pass Rate | $(( 100 * ${{ env.total_passed }} / ${{ env.total_tests }} ))% |

          EOF
```

**Success Criteria:**

- [ ] Metrics collected from multiple jobs
- [ ] Aggregation computes totals correctly
- [ ] Dashboard displays unified view
- [ ] Percentages and rates calculated

**Expected Output:**

```bash
Aggregated Test Dashboard
Total Test Time: 83s
Total Tests: 38
Total Passed: 37
Pass Rate: 97%
```

**Troubleshooting:**

- **Artifact not found:** Verify upload job names and paths
- **JSON parsing fails:** Check JSON format in uploaded files
- **Math incorrect:** Verify arithmetic in shell script

**Time Estimate:** 25–30 minutes

**Cross-Section Connections:** → Section 12 (Artifacts) for multi-artifact handling; → Section 14 (Outputs) for metrics passing.

</details>

---

<details>
<summary>📡 Scenario 27-E: Integration with External Reporting and BI Systems</summary>


**Objective:** Export workflow metrics to external BI/analytics platforms for enterprise dashboards.

**Prerequisites:** Scenario 27-D complete.

**Context:** Enterprise teams use centralized BI tools; integrating GitHub workflow data improves visibility.

**Detailed Steps:**

1. Format metrics for BI system (CSV, JSON, SQL)
2. Connect to BI platform API
3. Authenticate and send data
4. Verify data appears in dashboard
5. Create scheduled reporting workflow

**Detailed Workflow Example:**

```yaml
name: Export to BI System
on:
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours
  workflow_run:
    workflows: [CI]
    types: [completed]
jobs:
  export-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Collect workflow metrics
        id: metrics
        run: |
          cat > metrics.csv <<'EOF'
          workflow_name,run_id,conclusion,duration_seconds,timestamp
          ${{ github.workflow }},${{ github.run_id }},${{ job.status }},125,$(date -u +%Y-%m-%dT%H:%M:%SZ)
          EOF
          cat metrics.csv

      - name: Convert to JSON for API
        id: json
        run: |
          cat > payload.json <<'EOF'
          {
            "workflow": "${{ github.workflow }}",
            "run_id": ${{ github.run_id }},
            "status": "${{ job.status }}",
            "duration_seconds": 125,
            "timestamp": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "repository": "${{ github.repository }}",
            "branch": "${{ github.ref_name }}"
          }
          EOF
          cat payload.json

      - name: Send to external BI system
        env:
          BI_API_URL: ${{ secrets.BI_API_URL }}
          BI_API_KEY: ${{ secrets.BI_API_KEY }}
        run: |
          curl -X POST "$BI_API_URL/ingest" \
            -H "Authorization: Bearer $BI_API_KEY" \
            -H "Content-Type: application/json" \
            -d @payload.json \
            --fail

      - name: Verify data ingestion
        run: echo "Metrics successfully exported to BI system"

  generate-report:
    needs: export-metrics
    runs-on: ubuntu-latest
    steps:
      - name: Query BI system for report
        run: |
          echo "Generating weekly workflow report..."
          echo "Total workflows run: 127"
          echo "Average duration: 142 seconds"
          echo "Success rate: 96.8%"
```

**Success Criteria:**

- [ ] Metrics formatted correctly for BI API
- [ ] Authentication successful
- [ ] Data ingestion returns success
- [ ] Data appears in BI dashboard within 5 minutes
- [ ] Scheduled export runs on schedule

**Expected Output:**

```bash
Metrics successfully exported to BI system
Generating weekly workflow report...
Total workflows run: 127
Average duration: 142 seconds
Success rate: 96.8%
```

**Troubleshooting:**

- **API authentication fails:** Verify API key and URL in secrets
- **Data format rejected:** Check BI API documentation for payload format
- **Schedule doesn't trigger:** Verify cron syntax and repo permissions
- **Data not appearing in dashboard:** Check BI system logs for ingestion errors

**Time Estimate:** 30–35 minutes

**Cross-Section Connections:** → Section 16 (REST API) for API integration; → Section 8 (Secrets) for credential management.

</details>

---

### 📝 Section 28: Comprehensive Practice Assessment (2 hrs)

**Topics:** All topic files - comprehensive review
**Overview:**

- Scenario-based problem solving
- Integration testing across workflow concepts
- Enterprise workflow design
- Security and compliance scenarios
- Performance optimization challenges
- Real-world troubleshooting cases

**Deliverables:**

- Complete practice scenarios
- Build one complex multi-feature workflow
- Solve 10+ practice questions
- Review explanations for any incorrect answers


<details>
<summary>📝 Scenario 28-A: Comprehensive Multi-Feature Workflow Assessment</summary>


**Objective:** Build a single production-grade workflow that integrates at minimum 8 GitHub Actions features covered in the study plan, demonstrating mastery of their interaction.

**Prerequisites:** All Sections 1–27 complete.

**Context:** The GH-200 exam frequently tests scenario-based questions where multiple features interact. This capstone scenario requires you to design a workflow integrating: matrix strategy, reusable workflows, environment protection, caching, job summaries, secrets, artifact publishing, and conditional execution — mirroring real exam scenario complexity.

**Detailed Steps:**

1. Design the workflow architecture before writing YAML:
   - `strategy.matrix` to test on multiple Node.js versions
   - `needs:` to sequence build → test → deploy
   - `cache` to share node_modules across jobs
   - `environment: production` with approval gate
   - `GITHUB_STEP_SUMMARY` for consolidated test + deploy reporting
   - `permissions:` scoped to minimum required per job

2. Implement `.github/workflows/capstone.yml`:

```yaml
name: Capstone Assessment Workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

# Minimal default permissions — jobs opt in as needed
permissions: {}

jobs:
  # ── Job 1: Matrix build + test ──────────────────────────────
  test:
    strategy:
      fail-fast: false
      matrix:
        node: [18, 20]
        os: [ubuntu-latest, windows-latest]
        exclude:
          - os: windows-latest
            node: 18
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}

      - name: Cache node_modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node${{ matrix.node }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: ${{ runner.os }}-node${{ matrix.node }}-

      - name: Install and test
        id: test
        run: |
          echo "Testing on ${{ matrix.os }} / Node ${{ matrix.node }}"
          echo "passed=10" >> $GITHUB_OUTPUT
          echo "failed=0" >> $GITHUB_OUTPUT
        shell: bash

      - name: Test summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ### ${{ matrix.os }} / Node ${{ matrix.node }}
          - Passed: ${{ steps.test.outputs.passed }}
          - Failed: ${{ steps.test.outputs.failed }}
          EOF
        shell: bash

  # ── Job 2: Build artifact ────────────────────────────────────
  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      artifact-name: ${{ steps.build.outputs.name }}
    steps:
      - uses: actions/checkout@v4

      - id: build
        run: |
          mkdir -p dist
          echo "Build ${{ github.run_number }}" > dist/app.js
          echo "name=app-v1.0.${{ github.run_number }}" >> $GITHUB_OUTPUT

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.build.outputs.name }}
          path: dist/
          retention-days: 7
          if-no-files-found: error

  # ── Job 3: Deploy to production (protected environment) ─────
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: deploy-production
      cancel-in-progress: false  # never cancel in-flight production deploys
    permissions:
      contents: read
      id-token: write  # for OIDC
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: ${{ needs.build.outputs.artifact-name }}
          path: dist/

      - name: Deploy
        env:
          DEPLOY_ENV: production
        run: echo "Deploying $(cat dist/app.js) to $DEPLOY_ENV"

      - name: Deployment summary
        if: always()
        run: |
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## 🚀 Deployment
          - Artifact: ${{ needs.build.outputs.artifact-name }}
          - Status: ${{ job.status }}
          - Actor: ${{ github.actor }}
          EOF
```

3. Run the workflow and verify in the Actions UI:
   - Dependency graph shows test → build → deploy
   - Matrix excludes `windows/node18`
   - Production deploy shows "Waiting for approval" (if environment configured)
   - Summary tab shows per-matrix test results + deployment status

**Success Criteria:**

- [ ] Matrix produces 3 combinations (ubuntu/18, ubuntu/20, windows/20) — 1 excluded.
- [ ] Build job only runs after all test matrix cells pass.
- [ ] Deploy job waits for approval (if environment configured with protection rules).
- [ ] Artifact is downloaded in deploy job via `needs.build.outputs.artifact-name`.
- [ ] Summary tab displays formatted reports from all jobs.
- [ ] `concurrency:` prevents parallel production deployments.

**Time Estimate:** 45–60 minutes

</details>

---

<details>
<summary>📋 Scenario 28-B: Practice Exam Question Gauntlet (10 Scenarios)</summary>


**Objective:** Work through 10 targeted scenario questions spanning key exam domains without YAML code — pure conceptual recall.

**Questions:**

1. A workflow uses `on: pull_request` targeting a forked repository. The workflow calls `secrets.MY_SECRET`. What value does the step receive? *(Answer: empty string — fork PRs don't have access to secrets by default)*

2. You need a workflow to run at 9:00 AM UTC every weekday. Write the cron expression. *(Answer: `0 9 * * 1-5`)*

3. A reusable workflow requires a `with.environment` input. The calling workflow doesn't pass it. What happens? *(Answer: If the input has `required: true`, the workflow fails with a validation error at parse time)*

4. You want `job-b` to run only if `job-a` failed. What syntax do you use? *(Answer: `needs: [job-a]` + `if: failure()`)*

5. What is the maximum retention period for GitHub Actions artifacts on a public repository? *(Answer: 90 days)*

6. A matrix job has `fail-fast: false` and `continue-on-error: true`. One cell fails. What happens to the workflow run conclusion? *(Answer: The workflow succeeds — `continue-on-error: true` means the job failure doesn't fail the workflow)*

7. You write `${{ github.event.pull_request.title }}` directly in a `run:` step's shell command. What security risk does this introduce? *(Answer: Script injection — an attacker can craft a PR title with shell metacharacters)*

8. A composite action wants to run a JavaScript file. Is this supported? *(Answer: No — composite actions support only `run:` steps and `uses:` steps calling other actions, not direct JavaScript execution. Use a JavaScript action for that.)*

9. What permission does a job need to call `gh pr comment`? *(Answer: `pull-requests: write`)*

10. An `on: schedule` workflow runs at midnight but your job takes 25 minutes. Your next scheduled run is at 12:01 AM. What happens? *(Answer: GitHub Actions queues the run — concurrent scheduled runs are allowed, they don't cancel each other)*

**Time Estimate:** 20–30 minutes

**Cross-Section Connections:** → All sections — this is a comprehensive review assessment.

</details>

---

<details>
<summary>☁️ Scenario 28-C: Advanced IaC Patterns with GitHub Actions Orchestration</summary>


**Objective:** Orchestrate complex Infrastructure as Code deployments using GitHub Actions with Terraform/CloudFormation.

**Prerequisites:** Scenario 28-B complete.

**Context:** Enterprise infrastructure requires coordinated multi-region, multi-account deployments with state management.

**Detailed Steps:**

1. Create modular Terraform configurations
2. Build workflow that plans infrastructure across multiple environments
3. Implement approval gates before apply
4. Handle state file management (locking, versioning)
5. Validate and apply infrastructure changes

**Detailed Workflow Example:**

```yaml
name: IaC Orchestration - Multi-Region Deploy
on:
  push:
    paths:
      - 'infrastructure/**'
      - '.github/workflows/iac*.yml'
  workflow_dispatch:
    inputs:
      action:
        type: choice
        options: [plan, apply, destroy]
jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [dev, staging, prod]
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.6.0

      - name: Initialize Terraform - ${{ matrix.environment }}
        run: |
          cd infrastructure/${{ matrix.environment }}
          terraform init -backend=true

      - name: Validate configuration
        run: |
          cd infrastructure/${{ matrix.environment }}
          terraform validate

      - name: Plan infrastructure changes
        id: plan
        run: |
          cd infrastructure/${{ matrix.environment }}
          terraform plan -out=tfplan -lock=true

      - name: Save plan artifact
        uses: actions/upload-artifact@v3
        with:
          name: tfplan-${{ matrix.environment }}
          path: infrastructure/${{ matrix.environment }}/tfplan

      - name: Comment PR with plan summary
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const planOutput = 'Plan summary: ${{ steps.plan.outputs.stdout }}';
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '```\n' + planOutput + '\n```'
            });

  terraform-apply:
    needs: terraform-plan
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production-approval
    strategy:
      matrix:
        environment: [dev, staging, prod]
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v2

      - name: Download plan artifact
        uses: actions/download-artifact@v3
        with:
          name: tfplan-${{ matrix.environment }}
          path: infrastructure/${{ matrix.environment }}/

      - name: Apply infrastructure - ${{ matrix.environment }}
        run: |
          cd infrastructure/${{ matrix.environment }}
          terraform apply -auto-approve tfplan

      - name: Export outputs
        id: outputs
        run: |
          cd infrastructure/${{ matrix.environment }}
          terraform output -json > outputs.json
          echo "Outputs saved"
```

**Success Criteria:**

- [ ] Terraform plans generate for all environments
- [ ] Configuration validation succeeds
- [ ] Approval gate enforces review before production
- [ ] Apply only executes after approval
- [ ] State files locked during operations

**Expected Output:**

```bash
Terraform initialized
Validation passed
Plan: 5 to add, 2 to change, 1 to destroy
Applied successfully
Outputs saved
```

**Troubleshooting:**

- **State lock conflict:** Check for concurrent runs; increase lock timeout
- **Approval not enforcing:** Verify environment protection rules
- **Plan artifact missing:** Ensure upload happens before apply job

**Time Estimate:** 35–40 minutes

**Cross-Section Connections:** → Section 8 (Secrets) for sensitive variables; → Section 22 (Enterprise) for multi-account governance.

</details>

---

<details>
<summary>🔄 Scenario 28-D: Drift Detection and Auto-Remediation Workflows</summary>


**Objective:** Detect infrastructure drift from intended state and automatically remediate divergences.

**Prerequisites:** Scenario 28-C complete.

**Context:** Infrastructure drift causes configuration inconsistencies. Automated detection and remediation maintains compliance.

**Detailed Steps:**

1. Schedule drift detection workflow
2. Compare current infrastructure against desired state
3. Identify divergences (removed resources, modified configs)
4. Log drift events with severity
5. Trigger auto-remediation for non-critical drift
6. Notify team for manual review of critical drift

**Detailed Workflow Example:**

```yaml
name: Infrastructure Drift Detection
on:
  schedule:
    - cron: '0 */12 * * *'  # Every 12 hours
  workflow_dispatch:
jobs:
  detect-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v2

      - name: Refresh state
        run: |
          cd infrastructure/prod
          terraform init
          terraform refresh

      - name: Check for drift
        id: drift
        continue-on-error: true
        run: |
          cd infrastructure/prod
          DRIFT_OUTPUT=$(terraform plan -json 2>&1)
          if echo "$DRIFT_OUTPUT" | grep -q 'to change\|to destroy'; then
            echo "drift_detected=true" >> $GITHUB_OUTPUT
            echo "Changes detected in infrastructure"
          else
            echo "drift_detected=false" >> $GITHUB_OUTPUT
          fi

      - name: Save drift report
        if: steps.drift.outputs.drift_detected == 'true'
        run: |
          cd infrastructure/prod
          terraform plan > drift-report.txt
          cat drift-report.txt

      - name: Upload drift report
        if: steps.drift.outputs.drift_detected == 'true'
        uses: actions/upload-artifact@v3
        with:
          name: drift-report
          path: infrastructure/prod/drift-report.txt

  remediate-drift:
    needs: detect-drift
    if: failure() == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v2

      - name: Classify drift severity
        id: severity
        run: |
          # Heuristic: count changes to classify severity
          echo "severity=medium" >> $GITHUB_OUTPUT
          echo "auto_remediate=true" >> $GITHUB_OUTPUT

      - name: Auto-remediate non-critical drift
        if: steps.severity.outputs.auto_remediate == 'true'
        run: |
          cd infrastructure/prod
          terraform apply -auto-approve
          echo "Drift remediated automatically"

      - name: Notify team of remediation
        run: |
          echo "Drift remediation completed"
          echo "Resources resynchronized to desired state"
```

**Success Criteria:**

- [ ] Drift detection runs on schedule
- [ ] All resource types checked
- [ ] Drift report generated and saved
- [ ] Auto-remediation executes for allowed changes
- [ ] Team notified of significant drift

**Expected Output:**

```bash
Checking for drift...
Changes detected in infrastructure
Drift remediation completed
Resources resynchronized to desired state
```

**Troubleshooting:**

- **False positives:** Adjust plan parsing to exclude cosmetic changes
- **Remediation blocked:** Check IAM permissions and approval rules
- **Schedule doesn't run:** Verify cron syntax and workflow enabled

**Time Estimate:** 30–35 minutes

**Cross-Section Connections:** → Section 5 (Triggers) for scheduled workflows; → Section 15 (Debugging) for drift analysis.

</details>

---

<details>
<summary>🌍 Scenario 28-E: Multi-Environment Orchestration with IaC</summary>


**Objective:** Coordinate infrastructure deployments across dev → staging → prod with validation gates and rollback capability.

**Prerequisites:** Scenario 28-D complete.

**Context:** Enterprise deployments require careful orchestration across environments with safety checks and rollback procedures.

**Detailed Steps:**

1. Define environment progression (dev → staging → prod)
2. Create approval gates between environments
3. Implement smoke tests to validate each environment
4. Set up automatic rollback on test failure
5. Track deployment history for audit

**Detailed Workflow Example:**

```yaml
name: Multi-Environment IaC Orchestration
on:
  push:
    branches: [main]
    paths:
      - 'infrastructure/**'
jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    environment: development
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v2

      - name: Deploy to dev
        run: |
          cd infrastructure/dev
          terraform init
          terraform apply -auto-approve

      - name: Run dev smoke tests
        run: |
          echo "Testing dev environment..."
          curl -f http://dev-api.internal/health || exit 1
          echo "Dev environment healthy"

  deploy-staging:
    needs: deploy-dev
    runs-on: ubuntu-latest
    environment: staging-approval
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v2

      - name: Deploy to staging
        run: |
          cd infrastructure/staging
          terraform init
          terraform apply -auto-approve

      - name: Run staging smoke tests
        id: tests
        continue-on-error: true
        run: |
          echo "Testing staging environment..."
          sleep 2
          echo "Staging tests passed"

      - name: Rollback if tests fail
        if: steps.tests.outcome == 'failure'
        run: |
          cd infrastructure/staging
          echo "Tests failed, initiating rollback..."
          terraform destroy -auto-approve
          exit 1

  deploy-prod:
    needs: deploy-staging
    runs-on: ubuntu-latest
    environment: production-approval
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v2

      - name: Final pre-prod validation
        run: |
          echo "Running pre-deployment checks..."
          echo "✓ Security scan passed"
          echo "✓ Configuration validated"
          echo "✓ Dependencies verified"

      - name: Deploy to production
        run: |
          cd infrastructure/prod
          terraform init
          terraform apply -auto-approve

      - name: Run prod smoke tests
        run: |
          echo "Testing production environment..."
          curl -f https://api.example.com/health || exit 1
          echo "Production environment healthy"

      - name: Update deployment tracking
        run: |
          echo "Deployment completed and verified"
          echo "Deployment timestamp: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "Deployed by: ${{ github.actor }}"
```

**Success Criteria:**

- [ ] Each environment deploys in sequence
- [ ] Approval gates enforce manual review
- [ ] Smoke tests validate each environment
- [ ] Rollback executes on test failure
- [ ] Deployment history tracked
- [ ] Prod deployment only proceeds after staging validation

**Expected Output:**

```bash
Deploying to dev...
Dev environment healthy
Deploying to staging...
Staging tests passed
Deploying to production...
Production environment healthy
Deployment completed and verified
```

**Troubleshooting:**

- **Approval not enforcing:** Check environment protection rules
- **Smoke tests failing:** Review test logic and environment configuration
- **Rollback incomplete:** Verify destroy permissions and state management
- **Deployment stuck:** Check for manual approvals pending

**Time Estimate:** 40–45 minutes

**Cross-Section Connections:** → Section 8 (Secrets) for environment-specific credentials; → Section 22 (Enterprise) for approval workflows.

</details>

---

## 🎯 Week 9: Final Review & Assessment

### 📋 Section 29: Mock Exam - Full Practice Test (2 hrs)

**Topics:** [question-prompt.md](question-prompt.md) - Generate practice questions
**Overview:**

- Full 100-question practice exam
- Covers all 5 exam domains
- Timed assessment (2 hours simulating actual exam)
- Question difficulty distribution: 20% easy, 60% medium, 20% hard
- Performance tracking and weak area identification

**Deliverables:**

- Complete mock exam in 2 hours
- Identify weak knowledge areas
- Review explanations for all incorrect answers
- Track improvement from earlier assessments


<details>
<summary>⏱️ Scenario 29-A: Timed Mock Exam Protocol (2 Hours)</summary>


**Objective:** Simulate actual exam conditions — time-boxed, no notes, and score yourself honestly to produce an actionable weakness report.

**Prerequisites:** All Sections 1–28 complete.

**Context:** The GH-200 exam is 100 questions in 2 hours (average 72 seconds per question). Questions fall into 5 domains: Author and Maintain Workflows (~50%), Consume Workflows (~15%), Author and Maintain Actions (~20%), Manage GitHub Actions (environments, runners, billing) (~15%). The pass mark is 71%.

**Exam Protocol:**

1. **Set a 2-hour countdown timer** before starting.
2. Open `question-prompt.md` and generate a 100-question practice set.
3. Answer each question by writing your answer (A/B/C/D or True/False) in a separate document — do not look anything up.
4. Mark flagged/uncertain questions with a `?` to review if time allows.
5. After 2 hours, stop regardless of progress.

**Scoring Template:**

```markdown
## Mock Exam Score Sheet — Attempt [N]

**Date:** [YYYY-MM-DD]
**Time taken:** [X hours Y minutes]
**Target score:** 71+

| Domain                              | Questions | Correct | Score |
|-------------------------------------|-----------|---------|-------|
| Author & Maintain Workflows         | ~50       |         | %     |
| Consume Workflows                   | ~15       |         | %     |
| Author & Maintain Actions           | ~20       |         | %     |
| Manage GitHub Actions               | ~15       |         | %     |
| **TOTAL**                           | **100**   |         | **%** |

## Missed Questions Analysis

| # | Topic | Why Missed | Action |
|---|-------|-----------|--------|
|   |       |           |        |

## Weak Areas (< 70% in domain)
-
-

## Next Steps
- [ ] Review sections:
- [ ] Rebuild workflow for:
- [ ] Re-quiz on:
```

5. After scoring, identify any domain scoring below 70% — those are your priority review areas before the next mock or the actual exam.

**Success Criteria:**

- [ ] Exam completed in exactly 2 hours (no overtime).
- [ ] Score sheet filled in with per-domain breakdown.
- [ ] Missed question analysis completed for all wrong answers.
- [ ] Specific sections identified for targeted review.
- [ ] Score is >71% (or specific sections identified if not).

**Troubleshooting / Common Exam Pitfalls:**

| Pitfall | Correct Understanding |
|---------|----------------------|
| Confusing `on: workflow_call` with `on: workflow_dispatch` | `workflow_call` = called by another workflow; `workflow_dispatch` = manual trigger |
| `GITHUB_TOKEN` scope for fork PRs | Fork PRs get `pull-requests: read` only, not write |
| `needs:` context vs `steps:` context | `needs.JOB.outputs.KEY` uses job outputs; `steps.STEP.outputs.KEY` uses step outputs within same job |
| Default shell on Windows | PowerShell, not bash — add `shell: bash` for cross-platform scripts |
| Artifact retention defaults | Default: 90 days (public repos), configurable 1–90 days |

**Time Estimate:** 2 hours exam + 30 minutes scoring and analysis

</details>

---

<details>
<summary>🎯 Scenario 29-B: Weak Area Drill — Targeted Workflow Reconstruction</summary>


**Objective:** For each domain you scored below 70% in Scenario 29-A, write one workflow from memory without referencing notes, then verify it against documentation.

**Drill Template (repeat per weak area):**

1. **Pick the lowest-scoring domain topic** (e.g., "reusable workflow secrets passing").
2. **Set a 15-minute timer.**
3. **Write the workflow YAML from memory** targeting that specific feature.
4. **Verify** by checking the relevant study plan section and comparing.
5. **Note any errors** in your scoring sheet under "Why Missed."

**Example Drill — Reusable Workflow with Secrets:**

```yaml
# Caller — write this from memory, then verify
name: Caller
on: push
jobs:
  call:
    uses: ./.github/workflows/reusable.yml
    secrets:
      API_KEY: ${{ secrets.API_KEY }}

# Called — write this from memory, then verify
name: Reusable
on:
  workflow_call:
    secrets:
      API_KEY:
        required: true
jobs:
  use-secret:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Key length ${#API_KEY}"
        env:
          API_KEY: ${{ secrets.API_KEY }}
```

**Time Estimate:** 15 minutes per weak area × number of weak areas

**Cross-Section Connections:** → Section 28 (Comprehensive Assessment) for the initial practice scenarios that informed weak areas; → Section 30 (Final Prep) for the final targeted review before exam day.

</details>

---

<details>
<summary>🚀 Scenario 29-C: Blue-Green and Canary Deployments</summary>


**Objective:** Implement blue-green and canary deployment strategies to minimize downtime and risk.

**Prerequisites:** Scenario 29-B complete.

**Context:** Advanced deployment patterns reduce risk by allowing quick rollback and gradual traffic shifting.

**Detailed Steps:**

1. Set up blue (current) and green (new) environments
2. Deploy new version to green environment
3. Run smoke tests on green
4. Implement traffic shifting strategy (immediate for blue-green, gradual for canary)
5. Monitor error rates and metrics during deployment
6. Rollback if issues detected

**Detailed Workflow Example:**

```yaml
name: Blue-Green Deployment
on:
  workflow_dispatch:
    inputs:
      deployment_strategy:
        type: choice
        options: [blue-green, canary]
jobs:
  deploy-green:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build application
        run: |
          echo "Building application v${{ github.run_number }}..."
          mkdir -p build
          echo "app-v${{ github.run_number }}" > build/version.txt

      - name: Deploy to green environment
        run: |
          echo "Deploying to green environment..."
          echo "Green URL: https://green.example.com"
          echo "Version: $(cat build/version.txt)"

      - name: Run green smoke tests
        id: green-tests
        continue-on-error: true
        run: |
          echo "Running smoke tests on green..."
          sleep 2
          echo "✓ Health check passed"
          echo "✓ Critical paths tested"
          echo "✓ Database connectivity verified"
          echo "green_healthy=true" >> $GITHUB_OUTPUT

      - name: Shift traffic strategy
        if: steps.green-tests.outputs.green_healthy == 'true'
        run: |
          STRATEGY=${{ github.event.inputs.deployment_strategy }}
          if [ "$STRATEGY" = "blue-green" ]; then
            echo "Executing immediate blue-green cutover..."
            echo "100% traffic → green environment"
            echo "Blue environment available for rollback"
          elif [ "$STRATEGY" = "canary" ]; then
            echo "Executing canary deployment..."
            echo "5% traffic → green environment"
            echo "95% traffic → blue environment (current)"
          fi

      - name: Monitor deployment
        if: steps.green-tests.outputs.green_healthy == 'true'
        run: |
          echo "Monitoring deployment metrics..."
          for i in {1..30}; do
            echo "[Check $i/30] Error rate: 0.02%, P95 latency: 145ms"
            sleep 1
          done
          echo "Deployment stable"

  rollback:
    needs: deploy-green
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Initiate rollback
        run: |
          echo "Deployment failed, initiating rollback..."
          echo "Reverting 100% traffic to blue environment"
          echo "Green environment stopped"
          echo "Rollback completed at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
```

**Success Criteria:**

- [ ] Green environment deploys successfully
- [ ] Smoke tests pass on green
- [ ] Traffic shifts according to strategy
- [ ] Metrics monitored during deployment
- [ ] Rollback executes immediately if issues detected

**Expected Output:**

```bash
Deploying to green environment...
Running smoke tests on green...
Executing immediate blue-green cutover...
100% traffic → green environment
Monitoring deployment metrics...
Deployment stable
```

**Troubleshooting:**

- **Green deployment fails:** Review build logs and environment configuration
- **Smoke tests fail:** Debug application health checks
- **Traffic shift incomplete:** Check load balancer configuration
- **Rollback too slow:** Optimize rollback procedure

**Time Estimate:** 35–40 minutes

**Cross-Section Connections:** → Section 20 (Runners) for deployment infrastructure; → Section 25 (Performance) for monitoring during deployment.

</details>

---

<details>
<summary>🔄 Scenario 29-D: Rollback Automation and Failure Recovery</summary>


**Objective:** Build automated rollback procedures that execute when deployment health checks fail.

**Prerequisites:** Scenario 29-C complete.

**Context:** Automatic rollback minimizes MTTR (Mean Time To Recovery) when deployments cause issues.

**Detailed Steps:**

1. Deploy new version
2. Run comprehensive health checks
3. Monitor error rates and performance metrics
4. If metrics exceed thresholds, trigger automatic rollback
5. Verify rollback success
6. Notify team

**Detailed Workflow Example:**

```yaml
name: Deployment with Automatic Rollback
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy new version
        id: deploy
        run: |
          echo "Deploying version ${{ github.sha }}..."
          echo "Deployment URL: https://app.example.com"
          echo "deployed_version=${{ github.sha }}" >> $GITHUB_OUTPUT

  health-check:
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Run comprehensive health checks
        id: health
        continue-on-error: true
        run: |
          echo "Running post-deployment health checks..."
          ERROR_RATE=$(( RANDOM % 5 ))
          P95_LATENCY=$(( 100 + RANDOM % 100 ))

          echo "Error rate: ${ERROR_RATE}%"
          echo "P95 latency: ${P95_LATENCY}ms"

          ERROR_THRESHOLD=2
          LATENCY_THRESHOLD=150

          if [ $ERROR_RATE -gt $ERROR_THRESHOLD ]; then
            echo "health_status=unhealthy" >> $GITHUB_OUTPUT
            exit 1
          elif [ $P95_LATENCY -gt $LATENCY_THRESHOLD ]; then
            echo "health_status=unhealthy" >> $GITHUB_OUTPUT
            exit 1
          else
            echo "health_status=healthy" >> $GITHUB_OUTPUT
          fi

  rollback:
    needs: [deploy, health-check]
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Detect failure reason
        run: |
          echo "Deployment health check failed"
          echo "Initiating automatic rollback..."

      - name: Rollback to previous version
        run: |
          echo "Rolling back to previous stable version..."
          echo "Previous version: ${{ github.event.before }}"

      - name: Verify rollback
        run: |
          echo "Verifying rollback..."
          sleep 3
          echo "✓ Rollback successful"
          echo "✓ Health checks passing"
          echo "✓ Service stable"

      - name: Notify team
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '🚨 Automatic rollback executed due to health check failure'
            });
```

**Success Criteria:**

- [ ] Health checks execute after deployment
- [ ] Metrics compared against thresholds
- [ ] Rollback triggers automatically on threshold breach
- [ ] Rollback completes within 5 minutes
- [ ] Team notified of rollback
- [ ] Post-rollback health checks verify stability

**Expected Output:**

```bash
Running post-deployment health checks...
Error rate: 3%
Health check failed
Initiating automatic rollback...
Rolling back to previous stable version...
Rollback successful
```

**Troubleshooting:**

- **Thresholds too sensitive:** Adjust error rate and latency limits
- **Rollback hangs:** Check deployment tool configuration
- **Notifications not sent:** Verify notification channel setup
- **Previous version unavailable:** Maintain versioned deployment artifacts

**Time Estimate:** 30–35 minutes

**Cross-Section Connections:** → Section 15 (Debugging) for metrics collection; → Section 26 (Troubleshooting) for failure analysis.

</details>

---

<details>
<summary>🚩 Scenario 29-E: Progressive Delivery and Feature Flags</summary>


**Objective:** Deploy new features behind feature flags to enable progressive rollout and easy rollback.

**Prerequisites:** Scenario 29-D complete.

**Context:** Feature flags decouple deployment from release, allowing safe progressive rollout and instant rollback.

**Detailed Steps:**

1. Create feature flag configuration
2. Deploy code with feature behind flag (disabled by default)
3. Gradually enable flag for increasing user percentages
4. Monitor metrics at each rollout stage
5. Complete rollout or rollback based on metrics

**Detailed Workflow Example:**

```yaml
name: Progressive Feature Rollout
on:
  workflow_dispatch:
    inputs:
      feature_name:
        description: 'Feature flag to rollout'
        default: 'new_checkout_flow'
jobs:
  deploy-with-flag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create feature flag configuration
        run: |
          cat > feature-flags.json <<'EOF'
          {
            "new_checkout_flow": {
              "enabled": false,
              "rollout_percentage": 0,
              "created_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            }
          }
          EOF
          cat feature-flags.json

      - name: Deploy code with feature (disabled)
        run: |
          echo "Deploying new feature code..."
          echo "Feature: ${{ github.event.inputs.feature_name }}"
          echo "Rollout: 0% (disabled)"
          echo "Deployment complete"

  rollout-stage-1:
    needs: deploy-with-flag
    runs-on: ubuntu-latest
    steps:
      - name: Enable feature for 5% users
        run: |
          echo "Enabling ${{ github.event.inputs.feature_name }} for 5% of users..."
          echo "Rollout percentage: 5%"
          echo "Estimated users affected: ~500"

      - name: Monitor metrics (5 minutes)
        run: |
          echo "Monitoring metrics for 5 minutes..."
          for i in {1..5}; do
            echo "[Minute $i] Error rate: 0.1%, Conversion: 2.1%, Load: 42%"
            sleep 5
          done
          echo "Metrics stable at 5% rollout"

  rollout-stage-2:
    needs: rollout-stage-1
    runs-on: ubuntu-latest
    steps:
      - name: Enable feature for 25% users
        run: |
          echo "Expanding to 25% of users..."
          echo "Rollout percentage: 25%"
          echo "Estimated users affected: ~2,500"

      - name: Monitor metrics (5 minutes)
        run: |
          echo "Monitoring metrics for 5 minutes..."
          for i in {1..5}; do
            echo "[Minute $i] Error rate: 0.1%, Conversion: 2.1%, Load: 58%"
            sleep 5
          done
          echo "Metrics stable at 25% rollout"

  rollout-stage-3:
    needs: rollout-stage-2
    runs-on: ubuntu-latest
    steps:
      - name: Enable feature for 100% users
        run: |
          echo "Rolling out to 100% of users..."
          echo "Rollout percentage: 100%"
          echo "Feature fully released"

      - name: Final monitoring (10 minutes)
        run: |
          echo "Final monitoring of full rollout..."
          for i in {1..10}; do
            echo "[Minute $i] Error rate: 0.1%, Conversion: 2.2%, Load: 78%"
            sleep 5
          done
          echo "Rollout complete and stable"

  emergency-rollback:
    needs: rollout-stage-3
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Disable feature immediately
        run: |
          echo "Emergency: Disabling feature flag..."
          echo "Feature: ${{ github.event.inputs.feature_name }}"
          echo "Rollout percentage: 0%"
          echo "Feature disabled"

      - name: Verify rollback
        run: |
          echo "Verifying rollback..."
          echo "✓ Feature disabled for all users"
          echo "✓ Error rate returned to normal"
          echo "✓ Service stable"
```

**Success Criteria:**

- [ ] Feature deployed with flag disabled
- [ ] Gradual rollout through stages (5% → 25% → 100%)
- [ ] Metrics monitored at each stage
- [ ] Rollout continues only if metrics healthy
- [ ] Emergency rollback available at any stage
- [ ] Full rollout completes without errors

**Expected Output:**

```bash
Enabling new_checkout_flow for 5% of users...
Metrics stable at 5% rollout
Expanding to 25% of users...
Metrics stable at 25% rollout
Rolling out to 100% of users...
Rollout complete and stable
```

**Troubleshooting:**

- **Metrics not available:** Verify monitoring system integration
- **Rollout takes too long:** Consider increasing stage percentages
- **Feature flag not updating:** Check flag management service (LaunchDarkly, etc.)
- **Rollback doesn't disable feature:** Verify flag update permissions

**Time Estimate:** 40–45 minutes

**Cross-Section Connections:** → Section 7 (Environment Configuration) for feature flag variables; → Section 25 (Performance) for metrics monitoring.

</details>

---

### 🎓 Section 30: Final Consolidation & Exam Prep (2 hrs)

**Topics:** All materials - focused review
**Overview:**

- Weak area deep-dive based on practice test results
- Quick reference guides for key concepts
- Common exam pitfalls and tricks
- Time management strategies
- Question-type strategies (scenario, troubleshooting, best practices)
- Final knowledge verification
- Exam day logistics and tips

**Deliverables:**

- Review notes on weak areas
- Create personal quick reference guide
- Verify readiness with final mini-quizzes
- Plan exam day approach


<details>
<summary>📖 Scenario 30-A: Personal Quick Reference Guide Construction</summary>


**Objective:** Build a one-page (or single-screen) cheat sheet of the most frequently-tested GitHub Actions facts, syntax patterns, and gotchas — in your own words.

**Prerequisites:** All Sections 1–29 complete; Section 29 weak area analysis in hand.

**Context:** Research consistently shows that the act of constructing a reference guide (not just reading one) consolidates memory better than passive review. This scenario is about the construction process, not the output document.

**Quick Reference Template:**

```markdown
# GitHub Actions GH-200 Quick Reference

## Trigger Syntax (on:)
| Event              | Key Options                        |
|--------------------|------------------------------------|
| push               | branches, tags, paths              |
| pull_request       | types, branches, paths             |
| schedule           | cron: '0 9 * * 1-5' (Mon–Fri 9AM) |
| workflow_dispatch  | inputs: (string/bool/choice/env)   |
| workflow_call      | inputs: + secrets: (required/optional) |
| workflow_run       | workflows: ['Name'], types: [completed] |

## Context Reference
| Context          | Example Use                               |
|------------------|-------------------------------------------|
| github.sha       | Current commit SHA                        |
| github.ref       | refs/heads/main                           |
| github.event_name| push, pull_request, workflow_dispatch     |
| github.actor     | Username who triggered the run            |
| needs.JOB.outputs.KEY | Cross-job output passing           |
| steps.STEP.outputs.KEY | Same-job step output             |
| secrets.NAME     | Secret value (empty in fork PRs)          |
| vars.NAME        | Repository/org/environment variable       |
| env.NAME         | Workflow/job/step environment variable    |

## Job Control
- needs: [job-a, job-b]          # run after both complete
- if: failure()                   # run only if a needed job failed
- if: always()                    # run regardless of status
- continue-on-error: true         # don't fail workflow if this job fails
- timeout-minutes: 30             # default: 360 (6 hours)

## Special Files (Environment Files)
| File                | Purpose                          |
|---------------------|----------------------------------|
| $GITHUB_OUTPUT      | Set step outputs (key=value)     |
| $GITHUB_ENV         | Set env vars for subsequent steps|
| $GITHUB_STEP_SUMMARY| Append markdown to job summary   |
| $GITHUB_PATH        | Prepend directories to PATH      |

## Permissions
- Default: read for most + write for contents on workflow triggers
- Set permissions: {} to lock down, then grant per-job
- Fork PRs: pull-requests is read-only regardless of settings
- id-token: write — required for OIDC

## Action Types
| Type       | Defined by        | Runs on  |
|------------|-------------------|----------|
| JavaScript | action.yml + JS   | Runner   |
| Docker     | action.yml + Dockerfile | Container |
| Composite  | action.yml (steps:) | Runner  |

## Common Gotchas
1. `env:` at workflow level ≠ available in `on:` or `defaults:`
2. `needs:` context only in jobs that list those as dependencies
3. Fork PRs: secrets empty, pull-requests write blocked
4. Cron: based on UTC, not local time
5. `workflow_run`: triggered by another workflow's run, not its content
6. Composite actions: cannot use `env:` at action level (only at step level)
7. Matrix `include`: adds a new cell, doesn't modify existing ones
8. `cancel-in-progress: false` recommended for production deployments
```

**Success Criteria:**

- [ ] Quick reference covers all 5 exam domains.
- [ ] Every entry is written from memory first, then verified.
- [ ] Gotchas section includes at least 5 items from your personal weak areas.
- [ ] Reference fits on 1–2 pages when printed.

**Time Estimate:** 45–60 minutes

</details>

---

<details>
<summary>🎯 Scenario 30-B: Exam Day Protocol and Final Readiness Verification</summary>


**Objective:** Confirm technical readiness with a final 20-question speed drill, and establish an exam day strategy.

**20-Question Speed Drill (2 minutes each — 40 minutes total):**

Answer these without referencing any material:

1. Minimum cron schedule frequency in GitHub Actions? *(Every 5 minutes)*
2. Which `on:` event triggers a workflow when called by another workflow? *(`workflow_call`)*
3. How do you pass an output from step `build` to the next step in the same job? *(`${{ steps.build.outputs.KEY }}`)*
4. What's the maximum number of jobs in a matrix? *(256)*
5. Which permission allows `gh pr comment`? *(`pull-requests: write`)*
6. What syntax runs a job regardless of other jobs' failure? *(`if: always()`)*
7. How do you prevent a cron workflow from running if the previous run is still active? *(concurrency with `cancel-in-progress: true`)*
8. What file type defines a JavaScript action? *(`action.yml` in the root of the action repo)*
9. Default artifact retention for private repos? *(90 days)*
10. How do environment variables set in `$GITHUB_ENV` differ from `env:` in the step? *(`$GITHUB_ENV` persists to all subsequent steps; `env:` in step is step-scoped only)*
11. Which action type supports running shell scripts without a `Dockerfile`? *(Composite)*
12. What happens to `${{ secrets.MY_SECRET }}` in a fork PR workflow? *(Returns empty string)*
13. How do you declare a reusable workflow's required input? *(`inputs: NAME: required: true` under `on: workflow_call:`)*
14. What's the correct way to reference a reusable workflow in the same repository? *(`uses: ./.github/workflows/name.yml`)*
15. Which context provides the SHA of the commit that triggered the workflow? *(`github.sha`)*
16. How do you limit parallel matrix jobs to 3 at a time? *(`max-parallel: 3` under `strategy:`)*
17. What does `fail-fast: false` do in a matrix strategy? *(Allows all matrix cells to complete even if one fails)*
18. Which event type should you use to trigger a workflow after another workflow completes? *(`on: workflow_run`)*
19. What permission does OIDC token generation require? *(`id-token: write`)*
20. How do you pin an action to an immutable reference? *(Use commit SHA: `uses: owner/action@SHA # v1.2.3`)*

**Exam Day Checklist:**

- [ ] Exam environment loaded and tested (browser, stable internet)
- [ ] Quick reference guide printed or on second screen
- [ ] Rested and not cramming the morning of
- [ ] Strategy: answer all questions, flag uncertain ones, review flagged at end
- [ ] Time management: 72 seconds average per question; move on if stuck

**Final Readiness Score:**

- 18–20 correct: Ready — expect 85%+
- 15–17 correct: Borderline — review missed topics, consider 1 more day
- <15 correct: Not ready — identify patterns in missed questions, spend 1–2 days drilling

**Time Estimate:** 40 minutes drill + 20 minutes exam prep review

**Cross-Section Connections:** → This section synthesizes all 29 preceding sections. If any drill answer above is uncertain, return to the corresponding section for a 20-minute targeted review.

</details>

---

<details>
<summary>⏱️ Scenario 30-C: Mock Exam Scenarios with Time Constraints</summary>


**Objective:** Practice full mock exam scenarios under timed conditions to build exam readiness.

**Prerequisites:** Scenario 30-B complete.

**Context:** Time management is critical on certification exams. Practicing under realistic time constraints builds speed and accuracy.

**Detailed Steps:**

1. Set 120-minute timer for full mock exam
2. Complete 60 scenario-based questions (2 min per question target)
3. Track time spent per question
4. Review answers and identify timing issues
5. Complete second timed run to measure improvement

**Detailed Workflow Example (Mock Exam Simulation):**

```yaml
name: Timed Mock Exam
on:
  workflow_dispatch:
jobs:
  mock-exam-session:
    runs-on: ubuntu-latest
    steps:
      - name: Start exam timer
        id: timer
        run: |
          echo "🎓 GitHub Actions GH-200 Mock Exam"
          echo "Total time: 120 minutes"
          echo "Number of questions: 60"
          echo "Time per question target: 2 minutes"
          echo ""
          echo "Exam started at: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          START_TIME=$(date +%s)
          echo "start_time=$START_TIME" >> $GITHUB_OUTPUT

      - name: Sample exam questions (timed)
        continue-on-error: true
        run: |
          echo "Question 1 (Workflow triggers): Time limit 2 min"
          echo "  A. How do you trigger a workflow on pull request?"
          echo "  B. What is the difference between 'on: pull_request' and 'on: pull_request_target'?"
          echo "  C. How do you filter pull requests by file path?"
          echo "Estimated time: 1:45"
          echo ""
          echo "Question 2 (Matrix strategy): Time limit 2 min"
          echo "  A. Build matrix for 3 Node versions × 2 OS × 2 architectures"
          echo "  B. Calculate total job count"
          echo "  C. How do you use 'include' to add a special case?"
          echo "Estimated time: 1:30"
          echo ""
          echo "Question 3 (Secrets management): Time limit 2 min"
          echo "  A. Compare GITHUB_TOKEN vs repository secrets vs org secrets"
          echo "  B. When should you use OIDC token?"
          echo "  C. How do you rotate secrets safely?"
          echo "Estimated time: 2:00"
          echo ""
          echo "[Questions 4-60 continue...]"

      - name: Track question timing
        run: |
          echo "Question Timing Report:"
          echo "Question | Category | Time Allocated | Estimated | Over/Under"
          echo "---------|----------|-----------------|-----------|----------"
          echo "1        | Triggers | 2:00           | 1:45      | ✓ Under"
          echo "2        | Matrix   | 2:00           | 1:30      | ✓ Under"
          echo "3        | Secrets  | 2:00           | 2:00      | ✓ On Time"
          echo "[...]"
          echo "Total: 120:00 allocated, estimated 115:20 used"

      - name: End exam timer
        run: |
          END_TIME=$(date +%s)
          echo "Exam completed at: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
          echo "Total time used: 118 minutes"
          echo "Time remaining: 2 minutes"

      - name: Generate exam report
        run: |
          cat > exam-results.json <<'EOF'
          {
            "exam_type": "GH-200 Mock Exam",
            "duration_minutes": 120,
            "questions_total": 60,
            "questions_answered": 58,
            "score_estimate": "86%",
            "time_management": "Excellent",
            "timing_breakdown": {
              "quick_answers_under_1_30": 15,
              "standard_1_30_to_2_30": 38,
              "extended_over_2_30": 5
            }
          }
          EOF
          cat exam-results.json
```

**Success Criteria:**

- [ ] Complete 60 questions in 120 minutes
- [ ] Average 2 minutes per question
- [ ] Avoid rushing on final questions
- [ ] Review answers and identify time sinks
- [ ] Improve timing on second attempt

**Expected Output:**

```bash
Exam started: 2026-04-19T14:00:00Z
Total time used: 118 minutes
Time remaining: 2 minutes
Score estimate: 86%
Time management: Excellent
```

**Troubleshooting:**

- **Running out of time:** Practice faster question reading and decision-making
- **Unsure about answers:** Flag for review and move on; return if time permits
- **Second-guessing answers:** Build confidence by drilling weak areas

**Time Estimate:** 120 minutes (full exam)

**Cross-Section Connections:** → All sections for comprehensive exam coverage.

</details>

---

<details>
<summary>🔍 Scenario 30-D: Gap Analysis and Targeted Review Recommendations</summary>


**Objective:** Analyze mock exam results to identify knowledge gaps and recommend focused remediation.

**Prerequisites:** Scenario 30-C complete (mock exam taken).

**Context:** Strategic review of weak areas maximizes study efficiency before real exam.

**Detailed Steps:**

1. Score mock exam
2. Categorize incorrect answers by topic
3. Identify knowledge gaps
4. Recommend specific sections for review
5. Prioritize high-impact topics
6. Create targeted study plan

**Detailed Workflow Example:**

```yaml
name: Gap Analysis and Study Recommendations
on:
  workflow_dispatch:
    inputs:
      mock_score:
        description: 'Mock exam score (0-100)'
        default: '86'
jobs:
  analyze-gaps:
    runs-on: ubuntu-latest
    steps:
      - name: Analyze mock exam results
        id: analysis
        run: |
          SCORE=${{ github.event.inputs.mock_score }}
          echo "Mock Exam Score: ${SCORE}%"
          echo ""
          echo "Performance by Category:"
          echo "  Fundamentals: 92% ✓ Strong"
          echo "  Triggers & Events: 88% ✓ Good"
          echo "  Jobs & Strategies: 78% ⚠️ Needs Review"
          echo "  Artifacts & Caching: 82% ✓ Acceptable"
          echo "  Debugging: 76% ⚠️ Needs Review"
          echo "  Security: 85% ✓ Acceptable"
          echo ""
          echo "Incorrect Questions Analysis:"
          echo "  Q15: Matrix edge cases (topic: matrix strategies)"
          echo "  Q32: Cache invalidation patterns (topic: caching)"
          echo "  Q41: Debugging workflow failures (topic: debugging)"
          echo "  Q48: Complex conditionals (topic: logic)"
          echo "  Q56: Action versioning (topic: actions)"

      - name: Identify knowledge gaps
        run: |
          echo "📊 Knowledge Gap Report:"
          echo ""
          echo "🔴 Critical Gaps (Score <75%):"
          echo "  - Section 10 (Matrix): Score 78% (14/20)"
          echo "  - Section 15 (Debugging): Score 76% (15/20)"
          echo ""
          echo "🟡 Areas for Improvement (75-85%):"
          echo "  - Section 13 (Caching): Score 82% (16/20)"
          echo "  - Section 9 (Conditional Logic): Score 80% (16/20)"
          echo ""
          echo "🟢 Strong Areas (>85%):"
          echo "  - Section 1-5 (Fundamentals): 90%+"
          echo "  - Section 8 (Secrets): 85%"

      - name: Generate study recommendations
        run: |
          cat > study-plan.md <<'EOF'
          # Targeted Study Plan for Score Improvement

          ## Priority 1: Critical Gaps (Expected +8% improvement)
          - **Section 10:** Review Scenario 10-C (Dynamic Matrix), 10-D (Matrix Bugs), 10-E (Distributed Testing)
          - **Section 15:** Review Scenario 15-C (Step Debugging), 15-D (Local Reproduction), 15-E (Log Aggregation)
          - **Estimated time:** 2-3 hours
          - **Expected score after review:** 88%

          ## Priority 2: Areas for Improvement (Expected +4% improvement)
          - **Section 13:** Review Scenario 13-C (Cache Invalidation), 13-D (Cache Debugging)
          - **Section 9:** Review Scenario 9-D (Conditional Debugging)
          - **Estimated time:** 1-2 hours
          - **Expected score after review:** 92%

          ## Priority 3: Reinforcement (Expected +3% improvement)
          - **Section 19:** Custom action edge cases
          - **Section 23:** Advanced security patterns
          - **Estimated time:** 1 hour
          - **Expected score after review:** 95%

          ## Quick Reference: Most Tested Topics
          1. Matrix strategies (appears in 8% of questions)
          2. Debugging workflows (appears in 6% of questions)
          3. Secrets management (appears in 5% of questions)
          4. Artifact handling (appears in 4% of questions)
          5. Custom actions (appears in 4% of questions)

          ## Recommended Study Schedule
          - **Today:** Complete Priority 1 (Critical Gaps) - 2-3 hours
          - **Tomorrow:** Complete Priority 2 (Improvements) - 1-2 hours
          - **Day 3:** Practice Priority 3 & take second mock exam - 2 hours
          - **Total study time:** 5-7 hours
          - **Expected final score:** 92-95%
          EOF
          cat study-plan.md

      - name: Create action items
        run: |
          echo "📋 Immediate Action Items:"
          echo "  ☐ Review Section 10 Scenarios C, D, E (2 hours)"
          echo "  ☐ Review Section 15 Scenarios C, D, E (1.5 hours)"
          echo "  ☐ Complete practice questions for matrix topics (30 min)"
          echo "  ☐ Complete practice questions for debugging (30 min)"
          echo "  ☐ Take second mock exam to verify improvement (2 hours)"
          echo ""
          echo "📈 Success Criteria:"
          echo "  ✓ Score improvement of at least 5% on second attempt"
          echo "  ✓ Confidence level 8/10 or higher"
          echo "  ✓ Ready to schedule certification exam"
```

**Success Criteria:**

- [ ] Knowledge gaps identified by category
- [ ] Specific sections recommended for review
- [ ] Study time estimated for each gap
- [ ] Quick reference of high-frequency topics created
- [ ] Action items prioritized by impact

**Expected Output:**

```bash
Knowledge Gap Report:
🔴 Critical Gaps: Section 10 (78%), Section 15 (76%)
🟡 Areas for Improvement: Section 13 (82%), Section 9 (80%)
🟢 Strong Areas: Sections 1-5 (90%+)

Recommended study time: 5-7 hours
Expected final score: 92-95%
```

**Troubleshooting:**

- **Unsure which sections to review:** Focus on Critical Gaps first (red categories)
- **Time limited:** Prioritize sections that appear most frequently on exam
- **Still struggling after review:** Consider getting mentor guidance or taking extended study break

**Time Estimate:** 30–45 minutes (analysis), plus study time based on recommendations

**Cross-Section Connections:** → All sections for comprehensive reference.

</details>

---

<details>
<summary>🎓 Scenario 30-E: Real-World Certification Exam Patterns and Strategies</summary>


**Objective:** Learn exam strategies and patterns to maximize score on certification day.

**Prerequisites:** Scenario 30-D complete (gap analysis done).

**Context:** Exam success depends not just on knowledge but also on test-taking strategy.

**Detailed Steps:**

1. Study common question patterns on GH-200 exam
2. Learn question types: scenario-based, troubleshooting, best practices
3. Develop time allocation strategy
4. Practice eliminating wrong answers
5. Plan pre-exam day routine
6. Execute exam day protocol

**Detailed Workflow Example (Exam Strategy Guide):**

```yaml
name: Certification Exam Success Strategy
on:
  workflow_dispatch:
jobs:
  exam-patterns:
    runs-on: ubuntu-latest
    steps:
      - name: Learn common exam patterns
        run: |
          cat > exam-patterns.md <<'EOF'
          # GH-200 Certification Exam Patterns

          ## Question Type Distribution
          - Scenario-based (40%): Complex real-world situations
          - Troubleshooting (30%): Fix broken workflows
          - Best practices (20%): Choose optimal approach
          - Configuration (10%): YAML syntax and setup

          ## Scenario Pattern Example
          "You're asked to optimize a 15-minute workflow that tests 3 versions of Node.js across 2 OS platforms..."
          → Look for matrix optimization opportunities
          → Consider caching strategy
          → Evaluate parallel execution

          ## Troubleshooting Pattern Example
          "A workflow fails with 'permission denied' error..."
          → Check GITHUB_TOKEN permissions
          → Review branch protection rules
          → Verify environment protection rules

          ## Best Practices Pattern
          "What is the recommended approach for managing secrets in a monorepo?"
          → Organization-level secrets
          → Environment protection rules
          → OIDC token for external services
          EOF
          cat exam-patterns.md

      - name: Time allocation strategy
        run: |
          echo "⏱️ Exam Time Allocation Strategy:"
          echo ""
          echo "Total time: 120 minutes"
          echo "Total questions: 60"
          echo ""
          echo "Allocation:"
          echo "  📖 Reading questions: 1 min per question = 60 min"
          echo "  🤔 Thinking/deciding: 0.5 min per question = 30 min"
          echo "  ✏️ Answering: 0.5 min per question = 30 min"
          echo "  🔄 Review & revise: 10 min (for flagged questions)"
          echo ""
          echo "Strategy:"
          echo "  1. First pass: Answer all easy questions (1 min each)"
          echo "  2. Second pass: Answer medium questions (2 min each)"
          echo "  3. Third pass: Work on hard questions (3+ min each)"
          echo "  4. Final pass: Review flagged questions"

      - name: Answer elimination strategy
        run: |
          echo "❌ Wrong Answer Elimination Strategy:"
          echo ""
          echo "Example Question:"
          echo "  Q: 'How do you pass data between jobs?'"
          echo "  A. Using GITHUB_ENV globally"
          echo "  B. Using outputs and needs context"
          echo "  C. Using workflow_run event"
          echo "  D. Using git commits"
          echo ""
          echo "Elimination Process:"
          echo "  ❌ A: Incorrect - GITHUB_ENV is step-level only"
          echo "  ✓ B: Correct - This is the intended pattern"
          echo "  ❌ C: Incorrect - workflow_run is for triggering, not passing data"
          echo "  ❌ D: Incorrect - Not a data passing mechanism"
          echo ""
          echo "✅ Answer: B"

      - name: Pre-exam day checklist
        run: |
          cat > pre-exam-checklist.md <<'EOF'
          # Pre-Exam Day Preparation

          ## 1 Week Before
          - [ ] Review Sections 10, 15, 23 (highest-frequency topics)
          - [ ] Take full timed mock exam
          - [ ] Score >85% to confirm readiness
          - [ ] Review gap analysis results

          ## 1 Day Before
          - [ ] Light review of quick reference (30 min)
          - [ ] Prepare testing environment
          - [ ] Get good sleep (8+ hours)
          - [ ] Avoid heavy study (rest is important)

          ## Exam Day Morning
          - [ ] Eat healthy breakfast
          - [ ] Exercise lightly (30 min walk)
          - [ ] Arrive at testing center 15 min early
          - [ ] Use bathroom before starting
          - [ ] Deep breath - you're prepared!
          EOF
          cat pre-exam-checklist.md

      - name: Exam day protocol
        run: |
          echo "📋 Exam Day Protocol:"
          echo ""
          echo "First 5 minutes:"
          echo "  1. Read exam instructions carefully"
          echo "  2. Note time start and calculate time end"
          echo "  3. Plan breaks (2 min per break × 3 = 6 min total)"
          echo "  4. Scan all questions to identify easy ones"
          echo ""
          echo "During exam:"
          echo "  ✓ Answer easier questions first (build confidence)"
          echo "  ✓ Flag uncertain answers for review"
          echo "  ✓ Don't over-think - first instinct usually correct"
          echo "  ✓ Skip very difficult questions initially"
          echo "  ✓ Budget 2 min per question on average"
          echo ""
          echo "Last 10 minutes:"
          echo "  ✓ Review flagged questions"
          echo "  ✓ Don't change answers unless certain"
          echo "  ✓ Make sure all questions answered"
          echo "  ✓ Final review of reasoning"
          echo ""
          echo "Success mantra: 'I am prepared. I can do this.'"

      - name: Post-exam reflection
        run: |
          echo "🎓 You've completed the GH-200 certification exam!"
          echo ""
          echo "Next steps:"
          echo "  1. Note which questions felt difficult"
          echo "  2. Wait for results (usually within 24 hours)"
          echo "  3. If passed: Celebrate! Your GitHub Actions expertise is certified."
          echo "  4. If not passed: Identify gaps and schedule retake (60 day waiting period)"
          echo ""
          echo "Exam strategy effectiveness:"
          echo "  ✓ Time management: Allow adequate time per question"
          echo "  ✓ Answer elimination: Help narrow down correct answers"
          echo "  ✓ Knowledge gaps: Focus study on weak areas"
          echo "  ✓ Confidence: Build through practice and preparation"
```

**Success Criteria:**

- [ ] Understand common exam question patterns
- [ ] Time allocation strategy planned
- [ ] Answer elimination techniques practiced
- [ ] Pre-exam checklist completed
- [ ] Exam day protocol understood
- [ ] Post-exam plan established

**Expected Output:**

```bash
Exam Patterns Learned
Time Allocation: 60/30/30 strategy
Answer Elimination: Practice 5-10 questions using technique
Pre-exam day: All checklist items completed
Exam day protocol: Ready to execute
```

**Troubleshooting:**

- **Anxiety before exam:** Practice relaxation techniques and remember your preparation
- **Time management during exam:** Practice with mock exams to build speed
- **Second-guessing answers:** Trust your preparation; your first instinct is usually correct
- **Need to retake:** Identify specific weak topics and focus on targeted practice

**Time Estimate:** 30–45 minutes (strategy review); then 120+ minutes (exam day)

**Cross-Section Connections:** → All sections for comprehensive exam coverage.

</details>

---

## 🎓 Curriculum Complete

You have now completed the full GitHub Actions GH-200 Certification Study Plan with **150 comprehensive practical scenarios** across all 30 sections. Each section includes foundational (A), intermediate (B), advanced (C), troubleshooting (D), and enterprise-scale (E) scenarios, providing a complete learning progression.

**Study Plan Statistics:**

- **Total Sections:** 30
- **Total Scenarios:** 150 (5 per section)
- **Total Lines of Content:** 11,900+
- **Estimated Study Time:** 60-100 hours
- **Certification Target:** GH-200

**Ready to become GitHub Actions certified!**

## Study Strategy & Tips

### Daily Checklist

- [ ] Read/watch assigned section content
- [ ] Complete hands-on practical work
- [ ] Answer quiz questions related to section
- [ ] Review and consolidate notes

### Knowledge Retention

- **Spaced Repetition:** Review topics 1 day, 3 days, 1 week later
- **Active Recall:** Quiz yourself without looking at notes
- **Practice Application:** Build actual workflows, not just theoretical work
- **Teaching Others:** Explain concepts to solidify understanding

### Weak Area Focus

- After each section quiz, identify topics scoring <80%
- Spend additional 30 mins on weak areas
- Re-do practice questions for those topics
- Build practice workflows targeting weak concepts

### Time Management

- **Intensive Track (10 days):** 3 sections/day, study 6 hours/day
- **Balanced Track (20 days):** 1.5 sections/day, study 3 hours/day
- **Relaxed Track (30 days):** 1 section/day, study 2 hours/day

### Success Criteria

- **Minimum:** >71% on practice exams consistently
- **Target:** >80% on practice exams (indicates 85%+ on actual exam)
- **Excellent:** >85% on practice exams (indicates 90%+ on actual exam)

---

## Resources & Materials

- **Study Materials:** 19 topic markdown files in root directory
- **Practice Questions:** [question-prompt.md](question-prompt.md) (generates 100 questions per iteration)
- **Hands-On Labs:** All sections include practical GitHub workflow building
- **Reference:** [GitHub-Workflows-Guide.md](GitHub-Workflows-Guide.md) and [exam-overview.md](exam-overview.md)
- **External:** Official GitHub Actions documentation and tutorials

---

## Success Tracking Template

Use this to track progress:

```plaintext
Date: ___________

Section: ___________

Time Spent: ___________

Topics Covered:
- [ ] Topic 1
- [ ] Topic 2
- [ ] Topic 3

Hands-On Work Completed:
- [ ] Practical 1
- [ ] Practical 2

Quiz Score: _____ / 100 (Target: >80%)

Weak Areas Identified:
- _________
- _________

Next Review: _________

Notes:
_________
```

---

**Good luck with your GH-200 certification! A solid 60 hours of focused study with this plan should put you well above the 71% pass threshold.**
