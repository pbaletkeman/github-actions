version: "3.9"
services:
  quiz:
    build: .
    volumes:
      - ./quiz_engine:/app/quiz_engine
    stdin_open: true
    tty: true
  test:
    build: .
    command: python -m pytest tests/ -v
    volumes:
      - .:/app
